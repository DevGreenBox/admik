# 20 — Backlog medium/low тупиков (скан 2026-06-26)

> **Назначение.** Durable-список оставшихся тупиков (что нетех-пользователь не может
> сделать/увидеть/понять через кнопки). Воссоздан мультиагентным сканом 2026-06-26
> (14 финдеров по разделам админки+витрины → дедуп → перепроверка каждой по коду:
> 35 находок → 25 CONFIRMED + 6 WONT_FIX). Прошлый список жил в scratchpad сессии и
> был утерян при обрыве — теперь он в репозитории. Рецепт закрытия: TDD → гейт →
> деплой → живой прогон, батчами (см. docs/10, docs/00).

## Трекер статуса (Batch 7+)

| ID | Сев. | Раздел | Тупик | Статус |
|----|------|--------|-------|--------|
| C3 | medium | admin/variants | Нельзя отредактировать вариант после создания | ⬜ |
| C4 | medium | admin/variants | Нет поля compareAtPrice у варианта | ⬜ |
| C6 | medium | admin/categories | Скрытую категорию нельзя показать/скрыть | ⬜ |
| C8 | low | admin/attributes | Значение характеристики нельзя удалить | ⬜ |
| C9 | low | admin/promo | Варианты не грузятся в picker таргетов | ⬜ |
| C10 | low | admin/promo | Нет колонки «Мин. кол-во» в списке | ⬜ |
| C11 | low | admin/promo | Конкретные таргеты scoped-промокода скрыты | ⬜ |
| C12 | low | admin/promo | Нет подсказки у поля «Мин. количество единиц» | ⬜ |
| C2 | low | admin/audit | IP инициатора не виден | ⬜ |
| C0 | medium | admin/audit | before/after данные не видны (нет диффа) | ⬜ |
| C1 | medium | admin/audit | Нет фильтров журнала (дата/действие/инициатор) | ⬜ |
| C13 | low | admin/cdek | Нет колонки «Назначение» в списке отправлений | ⬜ |
| C17 | low | admin/users | Нет колонки «Последний вход» | ⬜ |
| C18 | low | admin/leads | Источник заявки не показан | ⬜ |
| C19 | medium | admin/leads | Нет плашки усечения списка заявок | ⬜ |
| C20 | low | admin/leads | Нет экспорта/CSV заявок | ⬜ |
| C16 | low | admin/settings | Кнопка сброса раздела недостижима из UI | ⬜ |
| C7 | low | admin/categories | SEO/OG-поля категории недоступны для правки | ⬜ |
| C5 | low | admin/variants | Нет переупорядочивания вариантов | ⬜ |
| C15 | low | admin/cms | OG-текст CMS-страницы не сохраняется (мёртвое поле) | ⬜ |
| C21 | low | storefront/footer | Instagram-ссылка ведёт на «#» (битая) | ⬜ |
| C22 | medium | storefront/header | Выпадашка недоступна с клавиатуры (WCAG) | ⬜ |
| C24 | low | storefront/cart | «+» не блокируется на лимите остатка | ⬜ |
| C23 | low | storefront/catalog | Фасеты sale/new без индикатора/сброса в UI | ⬜ |
| C14 | medium | admin/settings | ⚠️ ОСПОРЕНО: форма навигации orphan vs «удалена намеренно» — проверить | ⬜ |

WONT_FIX: оплата account (st==='unset' by-design), ручной заказ paymentMethod (опровергнуто), форма навигации (удалена намеренно c3df1e7 — см. C14), код роли в таблице (by-design), тип контакта email/phone (свободная строка by-design), длинные сообщения заявок (показываются полностью — by-design).

---

# Batch 7 — work-order (CONFIRMED вердикты скана)

## C0 [medium/high]
**Где:** app/admin/(panel)/audit/page.tsx:110-115 — SELECT возвращает только id, created_at, actor_email, action, entity_type, entity_id; before_data/after_data НЕ выбираются. Интерфейс AuditRow (строки 33-40) их тоже не содержит, а тело таблицы (строки 146-180) рендерит лишь 4 столбца без раскрытия/детали/диффа. При этом lib/audit/log.ts:57-72 (writeAudit) реально INSERT-ит before_data/after_data, а db/migrations/0004_audit.sql:26-27 определяет эти колонки. grep по всему репо на before_data|after_data даёт только writer, миграцию и tests/audit/log.test.ts — НИ ОДНОГО потребителя в admin-UI. Данные не пустые на практике: before/after прокидывают десятки Server Actions (lib/settings/action-factory.ts:255-256,383-384,406-407,429-430,452-453,614; lib/cms/actions.ts:200-201,226,289,319; lib/newsletter/actions.ts:43-44; lib/auth/actions.ts:206). Находка НЕ дублирует закрытый #17 — тот лишь добавил подписи действий/сущностей (lib/admin/audit-labels.ts), но не дифф.

**Фикс:** 1) Новый чистый хелпер lib/admin/audit-diff.ts: diffAuditData(before: Record<string,unknown>|null, after: Record<string,unknown>|null): { key: string; from: unknown; to: unknown; kind: 'added'|'removed'|'changed' }[]. Полностью pure (без БД/Next), универсален над произвольным JSON — никакой привязки к одному магазину. Сравнение по объединению ключей; равные значения (deep-equal по JSON) отбрасываются; before=null → kind 'added', after=null → 'removed'. 2) В app/admin/(panel)/audit/page.tsx: добавить before_data, after_data в SELECT (строки 110-115) и в интерфейс AuditRow. Для строк, где before_data||after_data не null, рендерить нативный <details>/<summary> «Подробнее» (работает в server-component без client-JS) с мини-таблицей key → from → to, построенной diffAuditData. Санитизацию не трогать — она уже сделана на записи (lib/audit/sanitize.ts). Ключи можно выводить как есть (минимально) либо через существующий маппер подписей; хардкода под магазин нет. Мультитенантность сохранена: хелпер и UI работают с любой схемой данных.

**Тест:** Сначала TDD-юнит tests/admin/audit-diff.test.ts на чистый diffAuditData (без БД/Next, как образец lib/leads/status.ts-тесты): (a) changed — before {price:100,status:'draft'}, after {price:200,status:'active'} → два элемента kind 'changed' с верными from/to; (b) create — before=null, after {price:100} → kind 'added' (from=undefined); (c) delete — before {price:100}, after=null → kind 'removed' (to=undefined); (d) неизменённые ключи исключаются, вложенные объекты сравниваются по значению. После зелёного хелпера — RTL/Vitest рендер-тест страницы: для строки с before/after присутствует <details> с парой from→to, для строки без диффа блок отсутствует.

---

## C1 [medium/high]
**Где:** app/admin/(panel)/audit/page.tsx:96-98 — из searchParams читается ТОЛЬКО `sp.page` (пагинация), других параметров нет. app/admin/(panel)/audit/page.tsx:104 (SELECT count(*) ... FROM audit_log — без WHERE) и :110-115 (SELECT id, created_at, actor_email, action, entity_type, entity_id FROM audit_log ORDER BY created_at DESC LIMIT $ OFFSET $ — без WHERE) подтверждают: фильтрация по дате/действию/инициатору/сущности отсутствует, выбираются все записи. app/admin/(panel)/audit/page.tsx:118 — pageHref = `/admin/audit?page=${p}` (никаких фильтров в URL). db/migrations/0004_audit.sql:33-36 — индексы audit_log_created_at_idx, audit_log_actor_idx, audit_log_entity_idx (entity_type,entity_id), audit_log_action_idx существуют, но в UI не задействованы. Находка #17 (УЖЕ ЗАКРЫТО) — это только человекочитаемые подписи (lib/admin/audit-labels.ts: auditActionLabel/auditEntityTypeLabel), к фильтрам отношения не имеет, дубликатом не является. Готовый эталон фильтров уже есть в админке: app/admin/(panel)/orders/_components/OrderFilters.tsx + parseOrdersFilter/WHERE-сборка в app/admin/(panel)/orders/page.tsx — значит фильтрация это принятая конвенция проекта, а не by-design отсутствие.

**Фикс:** Повторить существующий паттерн фильтров заказов (OrderFilters), мультитенантно, без хардкода под магазин (словари и audit_log per-instance). 1) lib/admin/audit-filters.ts (новый, чистый, тестируемый без БД/Next): экспортировать тип AuditFilter и parseAuditFilters(sp) по образцу parseOrdersFilter из orders/page.tsx — нормализует query-параметры: dateFrom/dateTo (валидная дата иначе undefined), action (принимать только ключи из AUDIT_ACTION_LABELS, иначе drop), entityType (только ключи AUDIT_ENTITY_TYPE_LABELS), actor (trim, ILIKE по actor_email), page (>=1). 2) app/admin/(panel)/audit/_components/AuditFilters.tsx (новый 'use client', зеркало OrderFilters): поля dateFrom/dateTo (type=date), select Действие из AUDIT_ACTION_LABELS, select Тип сущности из AUDIT_ENTITY_TYPE_LABELS, input Инициатор (email); submit формирует URLSearchParams и router.push('/admin/audit?...'), reset → /admin/audit; при смене фильтров сбрасывать page. 3) app/admin/(panel)/audit/page.tsx: const filter = parseAuditFilters(sp); собрать массив параметризованных условий через фрагменты postgres.js (sql``) — actor_email ILIKE ${'%'+actor+'%'}, action = ${action}, entity_type = ${entityType}, created_at >= ${dateFrom}, created_at < ${dateTo плюс 1 день}; склеить в whereSql и применить ОДИНАКОВО к count-запросу (стр.104) и к выборке строк (стр.110-115), чтобы totalPages совпадал. Отрисовать <AuditFilters/> над таблицей. 4) ВАЖНО: pageHref (стр.118) должен сохранять текущие фильтры в querystring (иначе пагинация сбрасывает фильтр) — формировать URL из текущих параметров фильтра + page. Параметризация обязательна (никакого string-concat в SQL) — postgres.js биндит значения.

**Тест:** TDD: первым — чистый юнит tests/admin/audit-filters.test.ts на parseAuditFilters (без БД/Next, по образцу tests/admin/audit-labels.test.ts): (a) неизвестный action не из AUDIT_ACTION_LABELS → filter.action === undefined; (b) невалидный entityType → undefined; (c) невалидная дата в dateFrom/dateTo → undefined, валидная ISO → проходит; (d) actor с пробелами → trim; (e) page<1/NaN → 1. Затем (после фрагментов SQL) — тест построителя условий: при пустом фильтре массив условий пуст (эквивалент отсутствию WHERE); при заданных action+actor+dateFrom формируются ровно соответствующие фрагменты. Опционально интеграционный тест на странице (как tests/storefront/cms-routes.integration.test.ts) проверяет, что count и rows используют один и тот же набор условий.

---

## C2 [low/high]
**Где:** IP пишется, но нигде не показывается. Запись: lib/audit/log.ts:60 (INSERT (...ip, user_agent)) и :69 (${ctx.ip ?? null}); контекст заполняется в lib/server/action.ts:238,257 и lib/orders/actions.ts:676; колонка db/migrations/0004_audit.sql:28 (ip inet); round-trip подтверждён тестом tests/audit/log.test.ts:130,148. ОТОБРАЖЕНИЕ отсутствует: app/admin/(panel)/audit/page.tsx:110-115 SELECT = id, created_at, actor_email, action, entity_type, entity_id (нет ip и user_agent); интерфейс AuditRow (строки 33-40) не содержит ip; в каталоге app/admin/(panel)/audit/ только page.tsx — детального просмотра/тултипа с IP нет (`ls` показал единственный файл). В docs/04-этап1-ядро-auth-rbac.md:211 ip описан как «IP инициатора» без решения о сокрытии — значит это orphan-поле (собирается, но не имеет потребителя), а не by-design.

**Фикс:** Файл app/admin/(panel)/audit/page.tsx. 1) Вынести запрос строк в чистую функцию loadAuditRows(offset, limit) (по образцу loadShipments в cdek/page.tsx) — для тестируемости. 2) В SELECT добавить host(ip) AS ip (inet → чистый текст, как в tests/audit/log.test.ts:142) и user_agent: `SELECT id, created_at, actor_email, action, entity_type, entity_id, host(ip) AS ip, user_agent FROM audit_log ...`. 3) Расширить интерфейс AuditRow полями ip: string | null и user_agent: string | null. 4) UI без захламления таблицы: показать IP вторичной строкой под «Инициатор» (td с actor_email) мелким серым текстом {row.ip ?? ''}, а user_agent повесить в title-tooltip этой ячейки (по аналогии с уже принятым в этом файле приёмом «uuid в tooltip»). Колонку отдельную не добавлять, чтобы не ломать ширины. Правка чисто UI/SELECT — мультитенантна, без хардкода под магазин, новых прав не требует (остаётся под audit.read). Опционально продублировать тот же приём в инстансах autoparts/Uni_BD при синхронизации.

**Тест:** Сначала TDD-тест на загрузчик (vitest, реальная dev-БД, по образцу tests/audit/log.test.ts): новый tests/admin/audit-page-rows.test.ts — через writeAudit({action:'auth.login'}, {actorEmail, ip:'203.0.113.10', userAgent:'UA/1.0'}) вставить событие, затем вызвать экспортированную loadAuditRows(0, 50) и ожидать, что вернувшаяся строка содержит ip === '203.0.113.10' и user_agent === 'UA/1.0'. Тест падает на текущем коде (поля не выбираются) → добавляем host(ip) AS ip и user_agent в SELECT → тест зелёный. После — /gate и ручной прогон страницы /admin/audit (playwright, mock-режим) для второй проверки.

---

## C3 [medium/high]
**Где:** Текущий код подтверждает тупик.

1) UI варианта — только toggle + delete. /home/claudeuser/admik/app/admin/(panel)/catalog/_components/VariantsSection.tsx:126-157 — в product.variants.map для каждой строки рендерятся ровно две кнопки: «отключить/включить» → toggleActive (строки 140-146) и «удалить» → removeVariant (строки 147-153). Кнопки/инпутов для изменения name, sku, priceOverride, priceDelta, compareAtPrice, weightG/lengthCm/widthCm/heightCm нет.

2) updateVariantAction вызывается из клиента ровно один раз и только для флага активности: VariantsSection.tsx:82 — updateVariantAction({ id, isActive: !isActive }). Grep по app/ (--include tsx/ts, исключая form-actions.ts) даёт только эту единственную точку вызова — другого UI редактирования вариантов в админке нет.

3) Бэкенд при этом полностью поддерживает правку всех полей: /home/claudeuser/admik/app/admin/(panel)/catalog/_components/form-actions.ts:71-73 проксирует updateVariant; /home/claudeuser/admik/lib/catalog/schemas.ts:269-280 (VariantUpdateSchema) валидирует sku/name/priceOverride/priceDelta/compareAtPrice/isActive/sort + dimensionFields; /home/claudeuser/admik/lib/catalog/actions.ts:743-776 (updateVariant) выполняет UPDATE по всем этим колонкам. То есть серверная возможность есть, а интерфейса нет.

4) Цена ошибки реальна: единственный способ исправить опечатку — удалить вариант, а removeVariant предупреждает «Действие необратимо, остатки варианта будут потеряны» (VariantsSection.tsx:88), т.е. при пересоздании теряется инвентарь (InventorySection ведётся отдельно). Это не косметика, а потеря данных при обходном пути.

Находка НЕ входит в список уже закрытых (Batch 1-6 / ADR-018 / G-01..G-19 её не касаются).

**Фикс:** Файл: /home/claudeuser/admik/app/admin/(panel)/catalog/_components/VariantsSection.tsx (правка только клиентского компонента; бэкенд/схемы/actions трогать не нужно — всё уже готово, мультитенантно, без хардкода под магазин).

Минимальная правка — инлайн-режим редактирования строки:
1. Добавить состояние editingId: string | null и набор полей формы редактирования (editName/editSku/editOverride/editDelta/editWeight/editLength/editWidth/editHeight; при наличии compareAtPrice в типе — editCompareAt).
2. В кнопочной ячейке (VariantsSection.tsx:138-155) добавить кнопку «редактировать», которая выставляет editingId=v.id и предзаполняет поля из v (через String(v.field ?? '')).
3. В product.variants.map: если editingId===v.id — рендерить строку с инпутами вместо текста (переиспользовать разметку инпутов из формы добавления, строки 169-218), плюс кнопки «сохранить»/«отмена».
4. saveVariant(): вызвать updateVariantAction({ id: editingId, name: editName.trim(), sku: editSku.trim() || undefined, priceOverride: normalizeMoney(editOverride) || null, priceDelta: normalizeMoney(editDelta) || '0', weightG: strToNum(editWeight), lengthCm: ..., widthCm: ..., heightCm: ... }). Переиспользовать УЖЕ существующие хелперы normalizeMoney (импорт строка 7) и strToNum (строки 39-44) — новых утилит не вводить. На успех — editingId=null + router.refresh(); на ошибку — setError(result).
5. Набор полей формы редактирования держать идентичным форме добавления (name/sku/priceOverride/priceDelta/вес+габариты), чтобы UX был единым.

Замечание по типу: проверить, что ProductDetail.variants содержит compareAtPrice (в lib/catalog/types). В таблице сейчас колонки compareAtPrice нет, форма добавления его тоже не показывает — поэтому для минимальной правки compareAtPrice в форму редактирования можно не включать (паритет с createVariant UI). Если решите включить — добавить поле в тип/выборку детально.

Альтернатива (тяжелее, НЕ рекомендуется для minimal): отдельная страница/модалка редактирования варианта — избыточно, инлайн в существующей таблице достаточно.

**Тест:** TDD, первый (красный) тест — компонентный, Vitest + @testing-library/react, рядом с существующими тестами админки (искать паттерн *_components/*.test.tsx; если нет — создать VariantsSection.test.tsx в той же папке). Мокнуть ./form-actions (updateVariantAction → vi.fn() возвращает { ok: true }) и next/navigation (useRouter → { refresh: vi.fn() }).

Тест 1 (пишется первым, должен падать на текущем коде): рендер <VariantsSection product={...}> с одним вариантом { id:'v1', name:'48', sku:'ABC', priceOverride:'1500', priceDelta:'0', isActive:true, weightG:null,... }. Ожидание: в строке есть кнопка «редактировать»; клик по ней показывает текстовый инпут со значением «48» (предзаполнен) и кнопку «сохранить». Сейчас провалится — ни кнопки, ни инпута нет.

Тест 2 (после реализации): в режиме редактирования меняем имя на «50», RU-цену вводим «1 600,50», клик «сохранить» → updateVariantAction вызван ровно один раз с { id:'v1', name:'50', priceOverride:'1600.50', priceDelta:'0', ... } (проверка нормализации денег через normalizeMoney и trim sku), затем router.refresh() вызван.

Тест 3: клик «отмена» возвращает строку в режим просмотра без вызова updateVariantAction.

После зелёных юнитов — вторая обязательная проверка: ручной прогон в playwright (mock-режим) на странице товара админки: открыть вариант, исправить название/цену, сохранить, убедиться что значение обновилось без удаления/потери остатка.

---

## C4 [medium/high]
**Где:** Per-variant compareAtPrice реализован сквозь весь backend, но недостижим из UI. Бэкенд: schemas.ts:262 (VariantCreateSchema.compareAtPrice) и :275 (VariantUpdateSchema.compareAtPrice); actions.ts:710-715 (INSERT compare_at_price в createVariant) и :761-762 (CASE WHEN в updateVariant); types.ts:157 (ProductVariant.compareAtPrice доступен клиенту через ProductDetail.variants); storefront dto.ts:384-387 effectiveCompareAt(variant.compareAtPrice, product.compareAtPrice) — вариантное значение реально перебивает товарное (ровно кейс из находки). Тесты подтверждают плумбинг: tests/catalog/repository.test.ts:131-143, tests/storefront/dto.test.ts:359-362. НО единственный UI-вход — app/admin/(panel)/catalog/_components/VariantsSection.tsx: форма «Добавить вариант» не имеет поля (поля только name/override/delta/sku, строки 169-218); addVariant НЕ передаёт compareAtPrice в createVariantAction (строки 49-63); в таблице нет колонки (заголовки 109-116, ячейки 128-137 — только priceOverride и priceDelta); updateVariantAction вызывается лишь для isActive (строка 82). Grep подтвердил: createVariantAction/updateVariantAction вызываются ТОЛЬКО из VariantsSection.tsx — иного места задать вариантную «было» нет. Владелец не может установить/увидеть compareAtPrice варианта.

**Фикс:** Правка только в одном файле — app/admin/(panel)/catalog/_components/VariantsSection.tsx (бэкенд, схемы, типы и витрина уже готовы, доработка не нужна). 1) Добавить state: const [newCompareAt, setNewCompareAt] = useState(''). 2) В addVariant (стр. 49-63) добавить в payload createVariantAction: compareAtPrice: normalizeMoney(newCompareAt) || null (по аналогии с priceOverride, normalizeMoney уже импортирован), и сбросить setNewCompareAt('') в блоке result.ok (стр. 65-74). 3) Добавить input «Цена «было»» с placeholder «пусто — как у товара» рядом со «Своя цена» (стр. 176-181); сетку первого grid расширить (sm:grid-cols-4 → перенести на 2 ряда либо grid-cols-3+перенос), чтобы вместить 5-е поле. 4) В таблицу: новый <th>«Цена «было»» в шапку (после «Своя цена», стр. 111) и ячейку <td>{v.compareAtPrice ?? '—'}</td> (после стр. 130); увеличить colSpan пустого состояния 7 → 8 (стр. 121). Опционально (отдельный, более крупный таск, не обязателен для закрытия): inline-редактирование compareAtPrice/priceOverride/priceDelta у существующих вариантов через updateVariantAction — сейчас он умеет это на бэке, но UI вызывает его только для isActive. Мультитенантность: значение — обычная строка денег через normalizeMoney + валюта магазина на отображении, хардкода под один магазин нет.

**Тест:** Компонентного тест-харнеса нет (vitest environment: node, нет testing-library/jsdom, нет .tsx-тестов), поэтому первый failing-тест ставим на чистую функцию-маппер. TDD-шаг 1: вынести построение payload в чистый хелпер buildVariantCreateInput(formStrings) → VariantCreateInput (включая compareAtPrice: normalizeMoney(newCompareAt) || null) и написать unit-тест в tests/catalog/ (node-env), который сначала падает: вводу { name:'48', compareAt:'1500,50' } соответствует payload с compareAtPrice:'1500.50', а пустой строке — null. Этот тест фиксирует ранее непокрытый маппинг формы. Шаг 2: реализовать хелпер и подключить его в VariantsSection. Вторая обязательная проверка (ручная) — playwright по mock-режиму: открыть карточку товара → «Добавить вариант» с заполненной «Цена «было»» → убедиться, что вариант создан и значение видно в новой колонке таблицы; затем на витрине (THE CASE) проверить, что у этого варианта скидка считается от вариантной «было».

---

## C5 [low/high]
**Где:** Заголовок находки («нет интерфейса для переупорядочения вариантов») ПОДТВЕРЖДАЕТСЯ, но доказательство финдера частично неверно — нужна поправка.

ЧТО ВЕРНО (реальный пробел): app/admin/(panel)/catalog/_components/VariantsSection.tsx (весь файл, 1-230) НЕ содержит ни одного контрола sort — в форме добавления (169-218) нет поля sort, в колонке «Действия» (138-155) только «отключить/удалить», нет кнопок вверх/вниз и drag-and-drop. addVariant (46-63) действительно не передаёт sort. То есть владелец не имеет UI, чтобы задать/изменить порядок вариантов. Так как все варианты по умолчанию получают sort=0, эффективный порядок — алфавит по name (для размеров XS/S/M/L/XL это даёт L,M,S,XL,XS — нелогично), и исправить это владелец не может.

ЧТО В ДОКАЗАТЕЛЬСТВЕ НЕВЕРНО (over-report): (1) утверждение «варианты выводятся в порядке product.variants без сортировки по sort-полю» — ЛОЖНО: lib/catalog/repository.ts:531 явно `ORDER BY sort, name`, и админ-карточка, и витрина (app/api/storefront/v1/products/[slug]/route.ts → getProductById → toProductDetailDto) получают варианты, уже отсортированные по sort. (2) «новый вариант всегда встаёт в конец списка» — ЛОЖНО: новый вариант получает sort=0 (createVariant actions.ts:709-718, default из схемы schemas.ts:264) и встаёт по алфавиту name внутри группы sort=0, а не в конец.

Бэкенд sort поддержан полностью (схемы create/update schemas.ts:264/279, INSERT actions.ts:709, UPDATE COALESCE actions.ts:761, ORDER BY repository.ts:531, отдача на витрину) — отсутствует ТОЛЬКО админ-UI управления порядком. Готовый эталон для повторного использования уже есть для медиа: MediaReorderSchema (schemas.ts:406), reorderMedia (actions.ts:1109-1150), reorderMediaAction (form-actions.ts:118), UI MediaSection.tsx:57-59.

**Фикс:** Минимальная правка, мультитенантно, по образцу reorderMedia (без хардкода под магазин):
1) lib/catalog/schemas.ts: добавить `VariantReorderSchema = z.object({ productId: uuidSchema, order: z.array(uuidSchema).min(1) })` (зеркало MediaReorderSchema, но без primaryId).
2) lib/catalog/actions.ts: добавить `reorderVariant = defineAction({ permission: 'catalog.write', input: VariantReorderSchema, handler })` — внутри `sql.begin` цикл `UPDATE product_variants SET sort = ${i} WHERE id = ${order[i]} AND product_id = ${productId}` (нормализует существующие sort=0 в 0..n-1 при первом переносе), audit `catalog.variant.reorder` (entityType 'product', entityId productId), revalidate [productPath(productId)]. Скопировать assertCatalogEnabled() как в createVariant.
3) app/admin/(panel)/catalog/_components/form-actions.ts: экспортировать `reorderVariantAction(input)` -> reorderVariant(input) (как reorderMediaAction:118).
4) VariantsSection.tsx: в колонке «Действия» (138-155) добавить кнопки «вверх»/«вниз» (disabled для первого/последнего по индексу). Обработчик move(index, dir): построить order = product.variants.map(v=>v.id) (они уже идут в порядке sort,name), переставить index с соседом, вызвать reorderVariantAction({ productId: product.id, order }), затем router.refresh(). Состояния error/pending уже есть.
5) (опц., 1 строка) В addVariant передавать sort = product.variants.length, чтобы новый вариант детерминированно вставал в конец.
Файлы: lib/catalog/schemas.ts, lib/catalog/actions.ts, app/admin/(panel)/catalog/_components/form-actions.ts, app/admin/(panel)/catalog/_components/VariantsSection.tsx. Док: обновить docs/05 §5.3 (упомянуть переупорядочивание вариантов).

**Тест:** Сначала падающий Vitest-юнит на action reorderVariant в tests/catalog/ (рядом с repository.test.ts / actions-guard.test.ts): создать товар с 3 вариантами A,B,C (все sort=0), вызвать reorderVariant({ productId, order: [C,A,B] }); затем getProductById(productId) должен вернуть варианты в порядке C,A,B (sort C=0,A=1,B=2). Дополнить в tests/catalog/actions-guard.test.ts проверкой RBAC: reorderVariant под ролью без catalog.write -> forbidden. Также добавить в tests/catalog/schemas.test.ts кейс на VariantReorderSchema (пустой order отклоняется, валидный массив uuid принимается). После зелёных тестов — реализация, затем /gate и ручной прогон в playwright (mock-режим) карточки товара: кнопки вверх/вниз меняют порядок, витрина показывает новый порядок.

---

## C6 [medium/high]
**Где:** Находка верна по текущему коду.

1) Бейдж "(скрыта)" рендерится, но рядом НЕТ контрола видимости: app/admin/(panel)/catalog/_components/CategoryManager.tsx:173 — `{!node.isActive ? <span ...>(скрыта)</span> : null}`. Единственные действия в не-редактируемой строке (там же, строки 174-190): «Переименовать» (startRename), «Переместить» (startMove), «Удалить». Тоггла isActive нет.

2) Inline-редактирование ничего не делает с is_active: saveRename (CategoryManager.tsx:105-109) шлёт только `{ id, name }`; saveMove (117-123) — только `{ id, parentId }`. Функция create() (83-97) isActive не передаёт → новые категории всегда активны (createCategory: `data.isActive ?? true`, lib/catalog/actions.ts:140). Значит из админки категорию нельзя ни СКРЫТЬ, ни СНОВА ПОКАЗАТЬ — тупик двусторонний.

3) Бэкенд это уже поддерживает (правка чисто фронтовая): updateCategory COALESCE'ит is_active — lib/catalog/actions.ts:178; CategoryUpdateSchema содержит isActive — lib/catalog/schemas.ts:134/140; updateCategoryAction уже проксирует — form-actions.ts:154; тип CategoryTreeNode/Category экспонирует isActive — lib/catalog/types.ts:51/65.

4) Состояние «скрыта» реально значимо для витрины (его захотят отменить): /api/storefront/v1/categories отдаёт дерево через toCategoryTreeDto, который фильтрует неактивные (lib/storefront/dto.ts:256 `.filter((n) => n.isActive)`); getActiveCategoryIdBySlug и getProductCategorySlugs фильтруют is_active=true (lib/storefront/queries.ts:38,52). Т.е. скрытая категория исчезает из навигации/каталога.

5) Несогласованность с соседними сущностями: у брендов есть чекбокс isActive (BrandForm.tsx:148), у вариантов — тоггл «отключить/включить» (VariantsSection.tsx:142). У категорий — только бейдж без управления.

В списке «уже закрыто» (Batch 1-6, ADR-018/G-01..G-19) этого пункта нет.

**Фикс:** Правка ТОЛЬКО во фронте, переиспользуя существующий generic-Action (мультитенантно, без хардкода под магазин). Бэкенд/схема/миграции не трогаются.

Файл: app/admin/(panel)/catalog/_components/CategoryManager.tsx, в не-редактируемой ветке renderNode (между кнопками «Переместить» и «Удалить», строки 177-190) добавить кнопку-тоггл:

  <button type="button"
    onClick={() => void run(
      () => updateCategoryAction({ id: node.id, isActive: !node.isActive }),
      node.isActive ? 'Категория скрыта на сайте.' : 'Категория показана на сайте.',
    )}
    className={`${btn} ${node.isActive ? 'text-amber-700' : 'text-green-700'}`}>
    {node.isActive ? 'Скрыть' : 'Показать'}
  </button>

updateCategoryAction уже импортирован (CategoryManager.tsx:11→form-actions.ts:154→updateCategory). run() уже делает router.refresh(), бейдж «(скрыта)» и подпись кнопки перерисуются. Никаких изменений в lib/catalog/actions.ts (is_active уже COALESCE на :178), schemas.ts (isActive уже в CategoryUpdateSchema) и БД не требуется. Опционально: чтобы у скрытой категории сразу читался текст рядом с бейджем — оставить как есть, бейдж уже есть.

**Тест:** TDD-первым — DB-gated интеграционный тест (среда vitest = 'node', jsdom/RTL нет, поэтому контракт проверяем на уровне Action+репозиторий+DTO, не компонентом).

Добавить в tests/catalog/integration.test.ts (describe.skipIf(!hasDb)) кейс «категорию можно скрыть и снова показать, витрина это отражает»:
1. createCategory({ name }) → категория активна; getCategoryTree → node.isActive === true; toCategoryTreeDto([node]) содержит её.
2. updateCategory({ id, isActive: false }) → перечитать getCategoryTree → node.isActive === false; toCategoryTreeDto скрывает её (длина 0); getActiveCategoryIdBySlug(slug) === null.
3. updateCategory({ id, isActive: true }) → снова видна в toCategoryTreeDto; getActiveCategoryIdBySlug(slug) !== null.

Этот тест фиксирует round-trip, на который опирается новая кнопка UI (скрыть↔показать) и гарантирует синхронизацию с витриной. После него — добавить кнопку и прогнать gate (typecheck/lint/vitest) + ручной прогон админки (вторая проверка) через playwright/деплой-стенд: проверить, что «Скрыть/Показать» переключает бейдж «(скрыта)» и категория появляется/исчезает в /api/storefront/v1/categories.

---

## C7 [low/high]
**Где:** Бэкенд полностью готов принять SEO/OG категории, но UI его не отдаёт. lib/catalog/schemas.ts:140-143 — CategoryUpdateSchema подмешивает seoEntityFields (ogTitle, ogDescription, ogImageKey, canonicalUrl, noindex). lib/catalog/actions.ts:161-206 — updateCategory реально пишет og_title/og_description/og_image_key/canonical_url/noindex (+seo_title/seo_description/description/is_active). lib/catalog/types.ts:44-67 и repository.ts:273-299 (listCategories→buildCategoryTree) — все эти поля уже загружаются в CategoryTreeNode, т.е. данные уже есть в пропсах админ-UI. Однако единственный UI редактирования категорий — app/admin/(panel)/catalog/_components/CategoryManager.tsx — даёт только create(name/slug/parent), inline rename(name), inline move(parent), delete; grep по «SeoFieldset|ogTitle|noindex|canonical» в этом файле = 0. Отдельной страницы /catalog/categories/[id] нет (есть только categories/page.tsx со списком). Для сравнения BrandForm.tsx:15,152 и ProductForm.tsx переиспользуют общий компонент app/admin/(panel)/_components/SeoFieldset.tsx — категории единственное исключение. Тупик реальный: сервер принимает данные, ввести которые через интерфейс невозможно. Уточнение по импакту: витрина THE CASE (THE CASE/src/app/catalog/) — единая фильтруемая страница /catalog без маршрутов по категориям и без generateMetadata/OG для категорий, т.е. сейчас эти поля нигде не рендерятся.

**Фикс:** Чистая UI-правка, бэкенд трогать НЕ нужно (схема/экшен/репозиторий/типы уже полны, дерево уже несёт поля). Мультитенантно, без хардкода под магазин. Рекомендуемый вариант (консистентно с брендами/товарами): 1) Создать app/admin/(panel)/catalog/categories/[id]/page.tsx (guard 'catalog.read', force-dynamic) + клиентский CategoryForm.tsx, который рендерит полные поля: name, slug, description, parentId/sort, переключатель isActive, seoTitle/seoDescription и общий <SeoFieldset> (OG/canonical/noindex с уже существующим OG-аплоадом по ключу S3). Сабмит — через существующий updateCategoryAction (form-actions.ts:154). Источник данных — getCategoryTree/listCategories по id (поля уже есть; при желании добавить тонкий getCategoryById в repository, но можно достать узел из дерева). 2) В CategoryManager.tsx добавить рядом с «Переименовать/Переместить» ссылку «Изменить» на /admin/catalog/categories/{node.id}. Альтернатива (легче): инлайн-раскрывающаяся панель «SEO/Подробнее» прямо в CategoryManager, рендерящая <SeoFieldset> + description/isActive и вызывающая updateCategoryAction (данные уже в node, доп. запрос не нужен). В обоих случаях ключевое — переиспользовать общий app/admin/(panel)/_components/SeoFieldset.tsx, как в BrandForm/ProductForm, чтобы не дублировать логику.

**Тест:** Сначала падающий тест (TDD): Vitest + Testing Library на новый CategoryForm (или расширенный CategoryManager). Замокать form-actions (updateCategoryAction). (a) Тест рендера: для существующей категории отрисованы поля SeoFieldset — input ogTitle, canonicalUrl и чекбокс noindex (по тем же testid/лейблам, что использует SeoFieldset в тестах бренда/товара). (b) Тест сабмита: ввести ogTitle/ogDescription/canonicalUrl, включить noindex, нажать «Сохранить» → ожидать вызов updateCategoryAction с объектом, содержащим { id, ogTitle, ogDescription, canonicalUrl, noindex }. Изначально оба падают (UI нет) → реализовать. Серверный тест на updateCategory с SEO-полями добавлять не требуется (экшен уже реализован); при желании — smoke-проверка, что страница categories/[id] проходит guard 'catalog.read'.

---

## C8 [low/high]
**Где:** Тупик реален в текущем коде. (1) UI: /home/claudeuser/admik/app/admin/(panel)/catalog/attributes/_components/AttributeValues.tsx:86-113 — таблица словаря имеет только 3 колонки (thead 89-91: «Значение/ЧПУ/Порядок»), а рендер строк 102-110 (values.map) выводит лишь v.value/v.slug/v.sort без какой-либо кнопки/колонки удаления. Форма ниже (116-140) умеет ТОЛЬКО добавлять. (2) Server Action: в /home/claudeuser/admik/lib/catalog/actions.ts есть addAttributeValue (886-907, audit 'catalog.attribute_value.create'), но deleteAttributeValue отсутствует — grep по 'deleteAttributeValue'/'attribute_value.*delete' пуст; единственные DELETE по attribute_* — это product_attributes (919/931), не сам словарь. (3) Обёртки: /home/claudeuser/admik/app/admin/(panel)/catalog/attributes/_components/form-actions.ts экспортирует только create/update/addAttributeValueAction (20-36). (4) Схема: /home/claudeuser/admik/lib/catalog/schemas.ts:353-359 — только AttributeValueSchema, delete-схемы нет. Важно: удаление ЗАДУМАНО на уровне БД — /home/claudeuser/admik/db/migrations/0008_catalog_attributes.sql:73 `GRANT SELECT, INSERT, UPDATE, DELETE ON attribute_values TO admik_app`, а FK product_attributes.value_id объявлен ON DELETE RESTRICT (строки 10, 57), т.е. БД сама запрещает удаление используемого значения. Сейчас словарь append-only: ошибочно/лишнее значение убрать через интерфейс нельзя. Это асимметрия с остальным каталогом, где delete есть у category (260), product (472), variant (791), brand (1345), media (1062).

**Фикс:** Мультитенантно, без хардкода под магазин (повторяет существующие паттерны). 1) schemas.ts: добавить `AttributeValueDeleteSchema = z.object({ id: uuidSchema })` (рядом с AttributeValueSchema, 353). 2) lib/catalog/actions.ts: добавить `deleteAttributeValue = defineAction({ permission: 'catalog.write', input: AttributeValueDeleteSchema, handler })` по образцу deleteVariant (791-813): `await assertCatalogEnabled()`; перед удалением — дружелюбная проверка использования: `SELECT 1 FROM product_attributes WHERE value_id = ${id} LIMIT 1` → если используется, кинуть `new CatalogError('conflict', 'Значение используется товарами — сначала уберите его из товаров.')` (это даёт понятную ошибку вместо сырого PG 23503 от ON DELETE RESTRICT); иначе `DELETE FROM attribute_values WHERE id = ${id} RETURNING id`, при пустом результате — CatalogError('not_found'); revalidate: [ATTRIBUTES_PATH]; audit: action 'catalog.attribute_value.delete', entityType 'attribute_value', entityId id. 3) form-actions.ts: добавить тонкую обёртку `deleteAttributeValueAction(input: unknown)`. 4) AttributeValues.tsx: добавить 4-ю колонку в thead и в каждую строку кнопку «Удалить» с `window.confirm`, вызовом deleteAttributeValueAction, обработкой Fail в существующий setError и router.refresh() при ok (re-use add()-логики pending/error). Опционально симметрично добавить редактирование значения — но это за рамками находки.

**Тест:** Сначала TDD-юнит для серверной части. В /home/claudeuser/admik/tests/catalog/actions-guard.test.ts (где уже есть блок «deleteProduct через defineAction — guard + Schema», 113) добавить describe для deleteAttributeValue: (а) без права catalog.write → отказ guard; (б) валидация AttributeValueDeleteSchema (битый/непереданный id → ok:false). Затем в отдельном тесте с мок-sql проверить поведение хендлера: значение используется (product_attributes вернул строку) → ok:false с кодом conflict и человекочитаемым сообщением; значение свободно → выполняется DELETE ... RETURNING id, ok:true, и в audit попадает action 'catalog.attribute_value.delete'. После зелёных юнитов — ручная проверка в браузере (playwright, mock-режим): на /admin/catalog/attributes/[id] добавить значение, удалить его кнопкой с подтверждением, убедиться что строка исчезла; попытаться удалить значение, привязанное к товару, — ожидать понятную ошибку, а не падение.

---

## C9 [medium/high]
**Где:** app/admin/(panel)/promo/_components/picker-data.ts:31-42 — loadPromoPickerData загружает только listBrands()/getCategoryTree()/SELECT id,name FROM products и возвращает {category, brand, product}; ключа variant в объекте нет. При этом вариант — реально достижимый тип таргета: lib/orders/types.ts:122-128 (PromoTargetType включает 'variant', PROMO_TARGET_TYPES перечисляет его) и lib/orders/schemas.ts:278-279 (allowedTargetTypesForScope('set') возвращает весь PROMO_TARGET_TYPES, т.е. с 'variant'). Скоуп 'set' — выбираемый radio: lib/orders/types.ts:113-114 (PROMO_APPLY_SCOPES) + lib/admin/order-format.ts:160 (promoScopeLabel case 'set'). Итог в UI: PromoForm.tsx:381-383 рисует опцию «Вариант» в селекте типа, а PromoForm.tsx:386-408 при row.targetType==='variant' получает pickerData['variant']===undefined → деградирует к текстовому полю с placeholder «идентификатор» (строки 401-407), требующему ручной UUID. Для category/brand/product такие же тупики уже закрыты заменой на select по названию — variant просто упустили.

**Фикс:** Минимальная правка в одном файле app/admin/(panel)/promo/_components/picker-data.ts (мультитенантно — SQL по текущей БД магазина, без хардкода под конкретный магазин). В Promise.all добавить 4-й запрос по вариантам и положить ключ variant в результат, по аналогии с product:
  const [brands, tree, products, variants] = await Promise.all([
    listBrands(),
    getCategoryTree(),
    sql`SELECT id, name FROM products ORDER BY name LIMIT 1000`,
    sql<{ id: string; name: string }[]>`
      SELECT v.id,
             p.name || ' / ' || COALESCE(NULLIF(v.name, ''), v.sku) AS name
      FROM product_variants v
      JOIN products p ON p.id = v.product_id
      ORDER BY p.name, v.sort
      LIMIT 1000`,
  ]);
  return {
    category: flattenCats(tree),
    brand: brands.map((b) => ({ id: b.id, name: b.name })),
    product: products.map((p) => ({ id: p.id, name: p.name })),
    variant: variants.map((v) => ({ id: v.id, name: v.name })),
  };
Колонки подтверждены схемой db/migrations/0007_catalog_variants.sql:13-28 (product_variants: id, product_id, sku NOT NULL, name DEFAULT ''). NULLIF(name,'')→sku страхует пустое имя варианта. LIMIT 1000 — тот же предел, что у товаров (для очень больших каталогов в будущем — автокомплит, как уже отмечено в комментарии файла). Никаких изменений в PromoForm.tsx не нужно: он уже рендерит select для любого присутствующего ключа pickerData (строки 386-399). Тип PromoPickerData (PromoForm.tsx:88-90) уже Partial<Record<PromoTargetType, ...>>, ключ variant в нём допустим — правок типа не требуется. После правки обновить журнал/доку как обычно.

**Тест:** TDD-тест первым (vitest), unit на loadPromoPickerData с моками — паттерн уже используется в репозитории (vi.mock('@/lib/db/client') в tests/catalog/*.test.ts). Файл tests/promo/picker-data.test.ts: замокать @/lib/db/client (sql, возвращающий для запроса вариантов [{id:'v1', name:'Кресло / Красный'}]) и @/lib/catalog/repository (listBrands→[], getCategoryTree→[]); вызвать loadPromoPickerData() и проверить, что result.variant определён и равен [{id:'v1', name:'Кресло / Красный'}] (красный тест до правки — ключа variant нет). Дополнительно (опц.) лёгкий рендер-тест PromoForm: при pickerData={variant:[{id,name}]} и applyScope='set'+targetType='variant' рендерится <select> с опцией по имени, а не <input placeholder="идентификатор"> — закрепляет, что variant теперь идёт по name. Затем прогнать /gate (typecheck/lint/vitest) и ручной прогон формы промокода в браузере (playwright, mock-режим) — две обязательные проверки.

---

## C10 [low/high]
**Где:** app/admin/(panel)/promo/page.tsx:34-37 — min_qty загружается в SELECT; lib/orders/repository.ts:109 (mapPromoCode) маппит min_qty→minQty, поэтому p.minQty доступен в рендере. Однако заголовки таблицы (page.tsx:99-110, ровно 11 колонок) не содержат «Мин. кол-во», а строки (page.tsx:122-172) рендерят minOrderTotal (line 150) и НЕ рендерят p.minQty; colSpan={11} (line 115). При этом min_qty реально влияет на применимость промокода: lib/orders/promo.ts:97-98 отклоняет с причиной below_min_qty, lib/orders/pricing.ts:176-181 обнуляет скидку при нехватке единиц. minQty виден/редактируется только на странице правки (PromoForm.tsx:444 «Мин. количество единиц»), но НЕ в списке. Несогласованность: список показывает «Мин. сумма» (min_order_total, line 104) — параллельный гейт применимости — но скрывает «Мин. кол-во».

**Фикс:** Минимальная правка, данные уже загружены (доп. запрос не нужен), без хардкода под магазин — чистая презентация. 1) lib/admin/order-format.ts: добавить чистый хелпер formatMinQty(minQty: number | null): string → minQty != null ? String(minQty) : '—' (переиспользуемо, тестируемо). 2) app/admin/(panel)/promo/page.tsx: добавить <th>Мин. кол-во</th> между «Мин. сумма» (после line 104) и «Лимит» (line 105) — логично рядом с другим min-гейтом; добавить соответствующую <td className="px-4 py-2 text-gray-600">{formatMinQty(p.minQty)}</td> между ячейкой minOrderTotal (после line 151) и ячейкой limitText (line 152). 3) Поднять colSpan с 11 на 12 (line 115). Никаких изменений схемы/запросов/типов — minQty уже в PromoCode и в SELECT.

**Тест:** TDD сначала: в tests/admin/order-format.test.ts добавить юнит-тест на новый хелпер formatMinQty из lib/admin/order-format.ts — expect(formatMinQty(3)).toBe('3') и expect(formatMinQty(null)).toBe('—'). Тест падает (хелпера нет) → реализовать хелпер → зелёный. Затем подключить хелпер в page.tsx и прогнать typecheck/lint/vitest через gate; ручная проверка — playwright-снимок /admin/promo: в шапке присутствует колонка «Мин. кол-во», строка с заданным min_qty показывает число, без min_qty — «—», ширина строки/colSpan не ломается (пустое состояние colSpan={12}).

---

## C11 [low/high]
**Где:** Финдинг точен по текущему коду. (1) app/admin/(panel)/promo/page.tsx:32-42 — loadPromos() выбирает из promo_codes только apply_scope и НЕ грузит promo_targets и имена связанных категорий/брендов/товаров. (2) page.tsx:137 — колонка «Scope» рендерит только promoScopeLabel(p.applyScope); по lib/admin/order-format.ts:152-165 это возвращает обобщённые 'Категория'/'Бренд'/'Набор' без конкретного таргета. (3) Данные есть, но не выводятся в списке: app/admin/(panel)/promo/[id]/page.tsx:35-51 (loadPromoTargets) грузит promo_targets на странице редактирования; схема db/migrations/0024_promo_mechanics_nxm.sql:77-103 подтверждает target_type IN ('category','brand','product','variant') c FK на categories/brands/products/product_variants. Итог: владелец в списке видит «Область: Бренд», но не к какому бренду — чтобы узнать, надо открыть промокод на редактирование. Не дубль Batch 6 #5 (тот про строгую валидацию scope, не про вывод таргетов в списке).

**Фикс:** Минимальная мультитенантная правка (без хардкода под магазин), 2 файла + опц. деградация:

1) lib/admin/order-format.ts — добавить чистый хелпер форматирования, например:
   formatScopeWithTargets(scope: string, labels: string[], max = 3): string
   Логика: scope==='cart' → promoScopeLabel('cart') ('Вся корзина'), labels игнорируются; иначе базовая метка promoScopeLabel(scope); если labels пуст → вернуть только метку (graceful, как сейчас); если labels непуст → `${label}: ${first.join(', ')}` и при labels.length>max добавить ` и ещё ${labels.length-max}`. Хелпер чистый — легко тестируется.

2) app/admin/(panel)/promo/page.tsx — в loadPromos() (или отдельной функцией loadPromoTargetLabels()) одним batch-запросом без N+1 собрать метки таргетов по всем промокодам списка:
   SELECT pt.promo_code_id,
          COALESCE(c.name, b.name, p.name, v.name, v.sku) AS label
   FROM promo_targets pt
   LEFT JOIN categories c       ON c.id = pt.category_id
   LEFT JOIN brands b           ON b.id = pt.brand_id
   LEFT JOIN products p         ON p.id = pt.product_id
   LEFT JOIN product_variants v ON v.id = pt.variant_id
   WHERE pt.promo_code_id = ANY(${ids})
   ORDER BY pt.created_at, pt.id
   Сгруппировать в Map<promoId, string[]>; обернуть в try/catch и при ошибке/выключенном каталоге вернуть пустую Map (как loadPromoPickerData деградирует) — модульность сохраняется.
   В JSX строка 137 заменить promoScopeLabel(p.applyScope) на formatScopeWithTargets(p.applyScope, labelsByPromo.get(p.id) ?? []). Бейджи stackable/exclusive оставить как есть.

Никаких таблиц, специфичных для конкретного магазина — только общие platform-таблицы; имена берутся из БД, не хардкодятся. Локали-строки ('и ещё', разделители) — в хелпере, как остальные RU-метки order-format.

**Тест:** TDD: сначала юнит-тест чистого хелпера в существующем файле tests/admin/order-format.test.ts (Vitest; view-слой с SQL юнитами не покрывается, поэтому логику выносим в чистую функцию и тестируем её первой). Кейсы для formatScopeWithTargets:
- scope='cart' + любые labels → 'Вся корзина' (labels игнорируются);
- scope='brand', labels ['Nike','Adidas'] → 'Бренд: Nike, Adidas';
- scope='category', labels [] → 'Категория' (graceful, поведение как сейчас при отсутствии данных);
- усечение: scope='set', labels из 5 элементов, max=3 → первые 3 через запятую + ' и ещё 2';
- неизвестный scope → возвращает сам scope (как promoScopeLabel default).
После прохождения теста — реализация хелпера и подключение batch-запроса в loadPromos(); затем /gate (typecheck/lint/vitest) и ручной прогон страницы /admin/promo через playwright (вторая проверка).

---

## C12 [low/medium]
**Где:** app/admin/(panel)/promo/_components/PromoForm.tsx:442-450 — поле «Мин. количество единиц»: есть <label> (444-445) и <input placeholder="без порога"> (446-448), затем сразу только error-<p className="...text-red-600"> (449); поясняющего <p className="text-xs text-gray-500"> нет. Для контраста: «Таргеты акции» имеют подсказку (364-367), BOGO — (336-338). Семантика подтверждена: lib/orders/pricing.ts:175-181 и lib/orders/repository.ts:643,917 — minQty сверяется с суммой единиц строк ВНУТРИ scope (lineInScope), т.е. предложенная формулировка «учитываются товары в области применения» корректна. Нюанс-скепсис: соседнее поле «Мин. сумма заказа» (PromoForm.tsx:278-282) тоже без подсказки, но финдер его не отметил — паттерн формы: простые числовые поля держатся на label+placeholder. Тем не менее minQty объективно непрозрачнее (бессмысленно без понимания «области применения»).

**Фикс:** Файл: app/admin/(panel)/promo/_components/PromoForm.tsx. В блоке <div> поля minQty (442-450) после error-<p> (строка 449) добавить статичную подсказку (без хардкода под магазин): <p className="mt-1 text-xs text-gray-500">Скидка применится, только если в корзине набрано не меньше указанного числа единиц товаров из «Области применения». Оставьте пусто — без ограничения по количеству.</p>. Для консистентности и устранения той же неоднозначности рекомендуется заодно добавить аналогичную короткую подсказку полю «Мин. сумма заказа» (278-282), например «Минимальная сумма заказа для срабатывания акции. Пусто — без порога.», чтобы пара minQty/minOrderTotal различалась явно. Правка чисто презентационная (UI-текст), мультитенантно-безопасна, бизнес-логику не трогает.

**Тест:** Сначала TDD-тест на рендер PromoForm (Vitest + @testing-library/react), как делается для прочих _components: смонтировать <PromoForm/>, найти input по label «Мин. количество единиц» (getByLabelText), затем утверждать, что рядом отрисован поясняющий текст — например screen.getByText(/набрано не меньше указанного числа единиц/i) присутствует в DOM (и связан с тем же блоком поля). Тест падает на текущем коде (подсказки нет) → добавляем <p> → зеленеет. Аналогичный assert при желании на подсказку «Мин. сумма заказа». Затем /gate (typecheck+lint+vitest) и ручной прогон страницы /admin/promo через playwright в mock-режиме как вторая проверка.

---

## C13 [low/high]
**Где:** Текущий код подтверждает находку. app/admin/(panel)/cdek/page.tsx:100-105 — SELECT в loadShipments тянет только s.id, s.cdek_number, s.cdek_uuid, s.delivery_mode, s.status_*, s.is_mock, s.error, s.updated_at, s.print_url, o.id, o.number, o.customer_name, o.customer_email; нет ни s.pvz_code / s.city_code, ни o.delivery_city / o.delivery_pvz_code / o.delivery_address. Интерфейс ShipmentRow (строки 65-81) этих полей не содержит. Заголовки таблицы (строки 192-198) и тело дают колонки Трек-номер/Заказ/Покупатель/Способ/Статус СДЭК/Обновлён/Печать; ячейка «Способ» (строки 237-239 через deliveryModeLabel, строки 52-63) показывает лишь ПВЗ/Постамат/Курьер — без указания, КУДА едет посылка. Данные физически есть: db/migrations/0017_cdek_shipments.sql:36-37 (pvz_code text, city_code integer) и db/migrations/0012_orders.sql:52-54 (delivery_city, delivery_address, delivery_pvz_code). Эти же данные показываются, но только в карточке заказа — app/admin/(panel)/orders/[id]/page.tsx:314-316 (Row «Город»/«Адрес»/«ПВЗ»). Значит на странице-обзоре /admin/cdek (по докстрингу строки 14-18 — «общий обзор всех отправлений») оператор не видит назначения и вынужден открывать каждый заказ. Серьёзность low: это замедление workflow, не жёсткий тупик (данные доступны кликом в заказ).

**Фикс:** Минимальная мультитенантная правка в одном файле app/admin/(panel)/cdek/page.tsx (никаких хардкодов под конкретный магазин — только generic-колонки): 1) Расширить SELECT в loadShipments (строки 100-105), добавив s.pvz_code, o.delivery_city, o.delivery_pvz_code. (s.city_code не брать в вывод — это числовой код СДЭК, нечитаемый; человекочитаемый город — o.delivery_city, заполняется на чекауте.) 2) Дополнить interface ShipmentRow (строки 65-81) полями pvz_code: string|null; delivery_city: string|null; delivery_pvz_code: string|null. 3) Добавить чистый хелпер-форматер destinationLabel(row) рядом с deliveryModeLabel (строки 52-63): для mode 'pvz'/'postamat' → город (если есть) + 'ПВЗ ' + (row.pvz_code ?? row.delivery_pvz_code), напр. 'Москва, ПВЗ MSK123'; для 'door' → row.delivery_city (курьер по городу); пусто → '—'. Чтобы было тестируемо по TDD — вынести этот хелпер в lib/cdek/ (например lib/cdek/format.ts) и экспортировать, заодно туда же можно перенести deliveryModeLabel. 4) Добавить новый <th>Назначение</th> в заголовок (после «Способ», строки 192-198) и соответствующий <td>{destinationLabel(row)}</td> в тело строки. 5) Обновить colSpan пустого состояния с 7 на 8 (строка 204). Гейт: typecheck/lint/vitest + ручной прогон страницы /admin/cdek в браузере (mock-режим), как того требуют две проверки.

**Тест:** TDD: сначала unit-тест на чистый форматер. Создать tests/cdek/format.test.ts (рядом с существующими tests/cdek/*), импортировать новый экспорт destinationLabel из lib/cdek/format.ts. Кейсы первым тестом: (a) {delivery_mode:'pvz', pvz_code:'MSK123', delivery_city:'Москва'} → 'Москва, ПВЗ MSK123'; (b) {delivery_mode:'postamat', pvz_code:null, delivery_pvz_code:'POST7', delivery_city:null} → 'ПВЗ POST7' (фолбэк на orders.delivery_pvz_code, без города); (c) {delivery_mode:'door', delivery_city:'Казань'} → 'Казань'; (d) всё null/пусто → '—'. Тест пишется ДО реализации хелпера (red→green). После зелёного юнита — расширить SELECT/интерфейс/разметку и прогнать ручную проверку страницы.

---

## C14 [medium/high]
**Где:** Orphan is real in current code (branch foundation/etap-0-1, HEAD 1030852):
- Backend present + tested: navigationSchema at lib/settings/schemas.ts:288; updateNavigationAction (upsert 'navigation' + audit 'settings.navigation.update') at lib/settings/action-factory.ts:441-451; tested at tests/settings/home-settings.test.ts:296-314.
- Wrapper ORPHANED: app/admin/(panel)/settings/_components/form-actions.ts:47-49 defines updateNavigationContentAction, but a repo-wide grep (excl. node_modules) for that symbol returns ONLY this definition — it is never imported by any component.
- NO UI form: ls of app/admin/(panel)/settings/_components/ shows 12 files, none NavigationForm.tsx. app/admin/(panel)/settings/page.tsx builds `sections` = [branding, home, currency, contacts, catalog, modules, seo] (lines ~43-50) and renders only those <Section> blocks — no navigation. CMS does not edit nav either (cms/_components/guard.ts:14 only references admin sidebar nav.ts).
- Storefront DOES consume it: THE CASE/src/lib/store-settings.ts:40-41 (s?.navigation?.header/footer) → THE CASE/src/app/layout.tsx:135 (headerItems={nav.header}), :145 (columns={nav.footer}) → Header/Footer; empty → default nav fallback.
- Form-feed data ready: getEffectiveSettings() returns eff.navigation={header,footer} (lib/config/settings.ts:210-211, 268-270).
Net: schema+action+audit+tests+storefront-consumer all exist, but no admin UI exposes them. Owner can edit header menu / footer columns only via direct DB or API. The "already-closed G-10/G-11" claim covered only the backend+storefront halves, not the UI.

**Фикс:** Pure UI wiring — NO backend changes (action, schema, audit, href-validation already exist & tested).
1) Create app/admin/(panel)/settings/_components/NavigationForm.tsx ('use client'), mirroring HomeContentForm.tsx (useRouter + useTransition + action-result helpers). Extract a PURE helper (e.g. parseNavigationFormState(headerText, footerText): { header, footer }) so it is unit-testable without React:
   - header textarea: one line per item, format "Label | href" → [{label,href}].
   - footer: per-column block, e.g. a textarea per column where first line = title, subsequent lines = "Label | href"; or a small repeatable column editor. Keep it text-driven like HomeContentForm to stay simple/multitenant.
   - On submit build { navigation: { header, footer } } and call the EXISTING updateNavigationContentAction from form-actions.ts (already exported). Empty inputs → {header:[], footer:[]} so storefront falls back to its default nav (no hardcode under one shop).
2) Wire into app/admin/(panel)/settings/page.tsx: import NavigationForm; add { id: 'navigation', title: 'Навигация (меню и футер)' } to the `sections` array; render <Section id="navigation" title="Навигация (меню и футер)"><NavigationForm navigation={eff.navigation} /></Section>. eff.navigation is already provided by getEffectiveSettings().
3) Mandatory verifications per project rules: run /gate (typecheck/lint/vitest) and a Playwright manual pass of /admin/settings — edit a header item + footer column, save, confirm it persists and the THE CASE storefront Header/Footer reflect it (mock mode). Multitenant: no shop-specific strings; relies on per-shop shop_settings.navigation.

**Тест:** TDD first (сначала тест): add tests/settings/navigation-form.test.ts targeting the pure parser parseNavigationFormState (extracted before writing the component):
- "Каталог | /catalog\nДоставка | /delivery" → header [{label:'Каталог',href:'/catalog'},{label:'Доставка',href:'/delivery'}], and the assembled { navigation: {...} } passes navigationSchema.safeParse (reuse import from lib/settings/schemas like home-settings.test.ts:10).
- footer column block parses to { title, links:[{label,href}] }.
- empty textareas → { header: [], footer: [] } (guarantees storefront fallback path).
- invalid href (e.g. "catolog" with no scheme/leading slash) makes navigationSchema.safeParse fail (matches existing assertion at tests/settings/home-settings.test.ts:156), proving the form surfaces validation errors instead of writing bad hrefs.
Only after these fail-first tests, implement NavigationForm.tsx + page.tsx wiring to make them green; existing action-level tests (home-settings.test.ts:296-314) already cover guard/upsert/audit so no new backend test needed.

---

## C15 [low/high]
**Где:** Подтверждено по текущему коду. (1) Общий компонент рендерит поля всегда, без признака скрытия: app/admin/(panel)/_components/SeoFieldset.tsx:126-137 (input OG-заголовок, value=value.ogTitle) и :157-170 (textarea OG-описание, value=value.ogDescription); пропсов для скрытия нет (интерфейс SeoFieldsetProps :36-57). (2) Форма CMS-страницы использует этот компонент: app/admin/(panel)/cms/_components/PageForm.tsx:347-368; инициализирует ogTitle:''/ogDescription:'' (:93-94), а pageSeoPayload() (:105-115) НЕ кладёт ogTitle/ogDescription в payload — заполненные значения никуда не уходят. Комментарии :33 и :88-89 это прямо признают («ogTitle/ogDescription у страницы нет … поля скрыты не будут, но не отправляются»). (3) Серверная схема их не принимает: lib/cms/schemas.ts pageSeoFields :224-232 (seoTitle/seoDescription/ogImageUrl/canonicalUrl/noindex/sitemap*) — ogTitle/ogDescription отсутствуют, Zod по умолчанию strip их вырежет. (4) В БД нет колонок: db/migrations/0022_cms_pages.sql:22-45 — есть seo_title, seo_description, og_image_url, но НЕТ og_title/og_description. (5) Витрина жёстко возвращает null: lib/storefront/cms-dto.ts:67-68 (ogTitle:null, ogDescription:null) в pageMeta(). (6) Несогласованность реальна: те же поля у товаров и брендов проходят до БД — ProductForm.tsx:178-179, BrandForm.tsx:67-68, lib/catalog/schemas.ts:114-115, lib/catalog/actions.ts:181-184. Итог: владелец в форме CMS-страницы видит те же OG-поля, что и на карточке товара, заполняет их, не получает ошибки, данные молча исчезают. В списке уже закрытых это не значится (Batch 6 «CMS/SEO read-only + загрузка OG» закрыл OG-картинку и read-only режим, а не мёртвые OG-текст-поля).

**Фикс:** Рекомендуемый вариант (платформенно-согласованный, тестируемый в текущем node-харнессе) — дотянуть OG-текст до БД, как у товаров/брендов (общий компонент уже их поддерживает, а билдер lib/seo/meta.ts:132-141 уже умеет ogTitle/ogDescription). Минимальные правки, без хардкода под один магазин: 1) Новая идемпотентная миграция db/migrations/00XX_cms_pages_og_text.sql: ALTER TABLE cms_pages ADD COLUMN IF NOT EXISTS og_title text; ADD COLUMN IF NOT EXISTS og_description text; (стиль соответствует существующим IF NOT EXISTS-миграциям). 2) lib/cms/schemas.ts: в pageSeoFields добавить ogTitle: ogTitleSchema, ogDescription: ogDescriptionSchema (импорт из @/lib/seo/schemas — те же примитивы, что у каталога). 3) lib/cms/types.ts:77-79 — добавить ogTitle/ogDescription в CmsPage; lib/cms/repository.ts:61-63 — маппинг row.og_title/og_description; в SELECT-списках :160/:189/:208 добавить og_title, og_description. 4) lib/cms/actions.ts — в INSERT (:114-120) и в UPDATE через CASE WHEN data.ogTitle!==undefined (:166-171, по образцу og_image_url) добавить og_title/og_description. 5) lib/storefront/cms-dto.ts:67-68 — заменить хардкод null на page.ogTitle / page.ogDescription. 6) PageForm.tsx: seo init ogTitle: page?.ogTitle ?? '' / ogDescription: page?.ogDescription ?? '' (:93-94), pageSeoPayload() (:107-114) добавить ogTitle/ogDescription (trim()||undefined), fieldErrors прокинуть ogTitle/ogDescription (:353-358); убрать вводящий в заблуждение комментарий :88-89. Альтернатива (минимальная, если решено что у CMS-страниц своих OG-текстов не нужно — backend by-design): добавить в SeoFieldset проп showOgText?: boolean (default true) и в PageForm передать showOgText={false} + краткая подсказка «OG-заголовок/описание берутся из SEO-заголовка/описания»; правит только 2 файла, БД не трогает, но в текущем node-only харнессе (без jsdom/RTL) не покрывается юнит-тестом.

**Тест:** Сначала падающий тест (TDD), харнесс node — без рендера компонентов. Первый тест — на витринный контракт (он фиксирует видимую регрессию «мёртвого поля»): в tests/storefront/dto.test.ts (или новый tests/storefront/cms-dto.test.ts) — pageMeta для CmsPage с ogTitle='OG-T'/ogDescription='OG-D' должен вернуть SeoMetaDto.ogTitle==='OG-T' и ogDescription==='OG-D' (сейчас вернёт fallback из seoTitle/seoDescription из-за хардкода null на cms-dto.ts:67-68) — тест краснеет. Затем поддерживающие: (а) tests/cms схема — CmsPageUpdateSchema/CmsPageCreateSchema принимают ogTitle/ogDescription и не вырезают их, плюс max-длина (255/1000) как у каталога; (б) тест экшена/репозитория updateCmsPage — что og_title/og_description попадают в UPDATE (по образцу tests/catalog/seo-fields.test.ts). После трёх зелёных — две проверки: gate (typecheck/lint/vitest/миграции) и ручной прогон формы CMS-страницы (заполнить OG-текст, сохранить, перечитать — значения сохранились и видны в мете витрины). Для минимального варианта (скрытие поля) юнит-тест недоступен — проверка только ручная (playwright по правилам admik после правки UI).

---

## C16 [low/medium]
**Где:** Wrapper exists but is orphaned: /home/claudeuser/admik/app/admin/(panel)/settings/_components/form-actions.ts:60-62 exports the 'use server' wrapper `resetSettingAction` (file docstring lines 3-9: these wrappers exist explicitly so client forms can call them directly). Underlying action: /home/claudeuser/admik/lib/settings/action-factory.ts:598-618 (permission 'settings.manage', deleteSetting + invalidateCache + revalidate /admin & storefront + audit 'settings.reset'). Re-export /home/claudeuser/admik/lib/settings/actions.ts:61-62; tested tests/settings/actions.test.ts:375-397. Zero UI callers — grep of app/ shows `resetSettingAction` only on its own definition lines; each form imports only its own update action (e.g. /home/claudeuser/admik/app/admin/(panel)/settings/_components/ModulesForm.tsx:10 imports only updateModulesAction; Branding/Currency/Legal/CatalogOrders/Seo/Home forms likewise). Valid keys: lib/settings/schemas.ts:309-322 (branding, currency, units, contacts, legal_entity, catalog, delivery, orders, module_overrides, seo, home, navigation). Note: empty-field save (text forms → undefined → env) and ModulesForm inherit tri-state (ModulesForm.tsx:125) approximate reset but never delete the override row nor emit the settings.reset audit, so the capability is genuinely unreachable from UI.

**Фикс:** Add a reusable client button and wire it into each settings form (multi-tenant: action resolves tenant server-side from session/deps — no shop hardcode). 1) New component app/admin/(panel)/settings/_components/ResetSettingButton.tsx ('use client'): props { settingKey: SettingKey; label?: string }; on click window.confirm('Сбросить раздел к значениям по умолчанию? Переопределения будут удалены.') → call resetSettingAction({ key: settingKey }) from ./form-actions → on ok router.refresh() + status, on fail render alert via existing errorMessage from ./action-result; reuse existing form button styling. Type settingKey to SettingKey (lib/settings/schemas.ts) so it stays driven by the SETTING_KEYS enum (no per-shop branching). 2) Render <ResetSettingButton settingKey=...> in the actions row next to 'Сохранить' in each form, mapping form→key(s): BrandingForm→'branding'; CurrencyUnitsForm→'currency' and 'units'; LegalContactsForm→'contacts' and 'legal_entity'; CatalogOrdersForm→'catalog'/'delivery'/'orders'; ModulesForm→'module_overrides'; SeoSettingsForm→'seo'; HomeContentForm→'home'; navigation form (if present)→'navigation'. For forms owning multiple keys, expose one reset button per logical key matching its save payload. No business-logic change — purely UI wiring of an existing, permissioned, audited action.

**Тест:** TDD first: component test tests/settings/reset-setting-button.test.tsx (Vitest + Testing Library), mirroring existing settings form-test pattern. vi.mock('../app/admin/(panel)/settings/_components/form-actions') so resetSettingAction is a mock (avoids 'use server' exec) and mock next/navigation useRouter. Cases: (a) confirm=true → click → expect resetSettingAction called once with { key: 'branding' } and router.refresh() called + success status shown; (b) confirm=false → action NOT called; (c) action returns { ok:false } → renders alert with errorMessage. Write this red test before adding the component, then implement to green; finish with the two-checks flow (gate + manual/playwright run of /admin/settings reset path in mock mode).

---

## C17 [low/high]
**Где:** Данные есть, но в UI не показаны нигде. (1) Список: app/admin/(panel)/users/page.tsx — шапка таблицы (строки 56-63: Email/Имя/Статус/Роли/Владелец + опц. действия) и тело (строки 73-99) НЕ рендерят lastLoginAt. (2) Репозиторий действительно отдаёт поле: lib/auth/admin-repository.ts:31 (UserWithRoles.lastLoginAt: Date|null), SELECT u.last_login_at на строках 76 и 94, маппинг на строке 121 (asDateOrNull). (3) Поле реально заполняется: lib/auth/actions.ts:227 — `UPDATE users SET last_login_at = now() WHERE id = ${user!.id}` при успешном входе, т.е. это не мёртвая схема. (4) Дополнительно проверено: ни карточка app/admin/(panel)/users/[id]/page.tsx, ни app/admin/(panel)/users/_components/UserForm.tsx тоже не выводят lastLoginAt (grep по lastLogin/last_login/вход дал только строку 240 про пароль). Итог: полезные данные fetch-атся и есть в БД, но не выводятся ни в одном из 3 экранов пользователей.

**Фикс:** Минимальная мультитенантная правка (чистый рендер, без хардкода под магазин): в app/admin/(panel)/users/page.tsx добавить колонку «Последний вход». 1) импорт: `import { formatDateTime } from '@/lib/admin/order-format';` (переиспользовать канонический ru-RU хелпер, НЕ toLocaleDateString из предложения находки — он даёт серверную локаль/таймзону и расходится с остальной админкой). 2) В thead добавить `<th scope="col" className="px-4 py-2 font-medium">Последний вход</th>` (логично после «Роли» или перед колонкой действий). 3) В tbody в каждую строку добавить `<td className="whitespace-nowrap px-4 py-2 text-gray-600">{formatDateTime(row.lastLoginAt)}</td>` — formatDateTime сам отдаёт «—» для null. 4) Обновить colSpan пустого состояния (строка 68): `canManage ? 7 : 6` вместо `6 : 5`. Тип уже содержит lastLoginAt, запрос уже его тянет — изменения только в page.tsx. Опционально: показать дату и в карточке [id]/page.tsx, но это вне минимального скоупа.

**Тест:** TDD, первый тест — рендер-тест списка пользователей (Vitest + React Testing Library). Замокать `@/lib/auth/session` (requireUser → owner с правом users.read), `@/lib/auth/rbac` (can → true), и `@/lib/auth/admin-repository` (listUsersWithRoles → два пользователя: один с lastLoginAt = new Date('2026-06-20T10:00:00Z'), другой с lastLoginAt = null). Отрендерить `await UsersPage()` и проверить: (а) в шапке есть текст «Последний вход»; (б) для первого пользователя выводится `formatDateTime(...)` (например содержит «20.06.2026»); (в) для второго выводится «—». Сначала тест красный (колонки нет), затем добавить колонку до зелёного. Дополнительно дешёвый guard-тест: formatDateTime(null) === '—' (хелпер уже покрыт, но фиксирует контракт пустого значения).

---

## C18 [low/high]
**Где:** Финдинг подтверждается текущим кодом, но severity завышен. lib/leads/repository.ts:12 — LeadRow.source: string; lib/leads/repository.ts:35 — listLeads() SELECT включает source. app/admin/(panel)/leads/page.tsx:38-43 — <thead> рендерит ровно 6 колонок (Дата/Имя/Контакт/Сообщение/Статус/Действия), колонки «Источник» нет; app/admin/(panel)/leads/page.tsx:47-60 — в map по строкам используются l.created_at/l.name/l.contact/l.message/l.status/l.id, но l.source НЕ упоминается → поле реально фетчится и не показывается. Важный контекст, снижающий серьёзность до low: единственный вызыватель insertLead (app/api/storefront/v1/leads/route.ts:38) жёстко передаёт source:'contact_form', а db/migrations/0030_leads.sql:19 даёт DEFAULT 'contact_form'. То есть сегодня у ВСЕХ заявок source идентичен — колонка была бы константной и аналитической ценности пока не несёт. Польза появится только при появлении других источников (схема/репозиторий уже к этому готовы). Не by-design (нет комментария о намеренном скрытии) и не закрыто ранее.

**Фикс:** Минимальная мультитенантная правка (без хардкода под один магазин), по образцу уже существующего auditActionLabel/LeadStatusBadge: 1) Завести человекочитаемый словарь меток источника — например helper leadSourceLabel(source: string): string в lib/leads/ (или рядом со схемами), маппинг { contact_form: 'Форма контактов' } с passthrough неизвестных значений (return известная метка ?? source), чтобы не падать на будущих источниках. 2) В app/admin/(panel)/leads/page.tsx добавить <th className="px-4 py-2 font-medium">Источник</th> после колонки «Контакт» (строка 40) и соответствующую <td className="px-4 py-2 text-gray-600">{leadSourceLabel(l.source)}</td> после строки 51. Не выводить сырой 'contact_form' — только через словарь меток (как сделано для auditActionLabel/auditEntityTypeLabel). Никаких изменений репозитория/схемы не требуется — данные уже фетчатся.

**Тест:** TDD: первым — юнит-тест на чистую функцию-метку leadSourceLabel (Vitest, как для auditActionLabel): leadSourceLabel('contact_form') === 'Форма контактов'; неизвестное значение leadSourceLabel('telegram_bot') === 'telegram_bot' (passthrough, без throw); пустая строка не ломает. Это закрывает «сначала тест, потом код» и гарантирует мультиисточниковую устойчивость. Доп. (опционально) — лёгкий рендер-тест строки таблицы/компонента, что в выводе присутствует метка источника, без обращения к реальной БД (page — серверный компонент, читает БД, поэтому фокус на helper).

---

## C19 [medium/high]
**Где:** app/admin/(panel)/leads/page.tsx:21 — `const leads = await listLeads(200)`: жёсткий лимит 200, total не запрашивается, плашка усечения не рендерится, в PageHeader (строки 25-29) нет счётчика. Контраст: app/admin/(panel)/subscribers/page.tsx:31-35 — `Promise.all([listSubscribers(LIST_LIMIT), countSubscribers()])` + `listTruncationNotice(...)` (строка 35) + амбер-плашка (строки 56-63) + «Всего: N» в subtitle (строка 51). lib/leads/repository.ts:33-46 — есть только `listLeads` и `countNewLeads` (status='new'), функции `countLeads()` (общий count) НЕТ. grep по всему репо: `countLeads` не встречается нигде; `listTruncationNotice` используется только в subscribers; `countSubscribers` только в lib/newsletter/repository.ts:46. Хелпер lib/admin/list-truncation.ts:12-21 — чистый, мультитенантный, уже протестирован (tests/admin/list-truncation.test.ts). То есть принятый паттерн Batch 6 #9 к разделу «Заявки» просто не подключили; лимит 200 ниже, чем у подписчиков (500), поэтому усечение даже вероятнее.

**Фикс:** 1) lib/leads/repository.ts: добавить `countLeads()` зеркально `countSubscribers` (lib/newsletter/repository.ts:46): `const rows = await sql<{ count: string }[]>`SELECT count(*)::text AS count FROM leads`; return Number(rows[0]?.count ?? 0);`. Мультитенантно — тот же общий sql-клиент, без хардкода магазина. 2) app/admin/(panel)/leads/page.tsx: импортировать `countLeads` и `listTruncationNotice`; ввести `const LIST_LIMIT = 200;`; заменить строку 21 на `const [leads, total] = await Promise.all([listLeads(LIST_LIMIT), countLeads()]);` + `const truncation = listTruncationNotice(leads.length, total, LIST_LIMIT);`; добавить `Всего: ${total}.` в subtitle PageHeader (как у подписчиков, строка 51); перед таблицей отрендерить ту же амбер-плашку `{truncation ? (<p role="status" className="mt-4 rounded border border-amber-200 bg-amber-50 px-3 py-2 text-sm text-amber-800">{truncation}.</p>) : null}` — БЕЗ суффикса «Используйте экспорт…», т.к. у заявок нет экспорта (или добавить экспорт отдельно вне scope). Без новых констант-дублей: при желании вынести LIST_LIMIT в общий конфиг, но достаточно локальной как в subscribers.

**Тест:** TDD-first: render-тест страницы LeadsPage (Vitest + RTL) с мок-модулем `@/lib/leads/repository`: (а) `listLeads` возвращает 200 строк, `countLeads` → 250 — ассертить, что в документе есть текст плашки «Показаны последние 200 из 250» (role="status"); (б) `listLeads` возвращает 10, `countLeads` → 10 — ассертить, что плашки нет (queryByRole('status') === null). Это пиннит реальный баг (проводку страницы), а не уже покрытый чистый хелпер. Запускать через /gate (typecheck+lint+vitest). Поскольку countLeads — тонкая SQL-обёртка (репозитории здесь юнитами не покрываются, ср. отсутствие тестов newsletter/leads repo), отдельный тест на SQL не нужен; корректность count проверяется ручным прогоном (playwright/стенд) как вторая из двух проверок.

---

## C20 [medium/high]
**Где:** Раздел «Подписчики» имеет экспорт, а «Заявки» — нет, при идентичном обосновании в коде.
ЕСТЬ у подписчиков:
- /home/claudeuser/admik/app/admin/(panel)/subscribers/page.tsx:9 (import { ExportToolbar }) и :53 (action={<ExportToolbar rows={exportRows} />}); :41-45 формирует exportRows (email/status/createdAtIso).
- /home/claudeuser/admik/app/admin/(panel)/subscribers/_components/ExportToolbar.tsx:64-81 — кнопки «Скопировать адреса» + «Скачать CSV», использует subscribersToCsv из /home/claudeuser/admik/lib/newsletter/csv.ts (RFC 4180 + анти-формула-инъекция, escapeField:22-33).
НЕТ у заявок:
- /home/claudeuser/admik/app/admin/(panel)/leads/page.tsx:25-29 — PageHeader без prop `action` (grep `action=` по файлу пуст); ни CSV, ни «скопировать контакты». listLeads(200) на :21 (лимит 200 без плашки усечения, в отличие от subscribers).
- /home/claudeuser/admik/app/admin/(panel)/leads/_components/ — только LeadRowActions, LeadStatusBadge, action-result, actions, guard; ExportToolbar отсутствует.
- Нет /home/claudeuser/admik/lib/leads/csv.ts, нет export-route, нет csv-теста в /home/claudeuser/admik/tests/leads/ (есть только leads/actions/status).
Заявки несут БОЛЬШЕ контактных данных (name, contact, message), чем подписчики (только email), но экспорта лишены — реальный пробел по собственным стандартам проекта.

**Фикс:** Зеркалить готовый паттерн подписчиков (мультитенантно: данные уже scoped через listLeads, никакого хардкода под магазин; экспорт чисто клиентский — чтение того, что owner уже видит под orders.read).
1) Создать /home/claudeuser/admik/lib/leads/csv.ts — чистый модуль `leadsToCsv(rows: CsvLead[])` по образцу lib/newsletter/csv.ts: переиспользовать ту же логику escapeField (RFC 4180 квотирование + анти-CSV-инъекция для =,+,-,@ — критично, т.к. name/contact/message приходят из ПУБЛИЧНОЙ формы витрины, недоверенный ввод). Заголовок `id,name,contact,message,status,created_at`, дата ISO-8601. Тип CsvLead самодостаточен (без import repository), чтобы безопасно тянуть на клиент. Рекомендация по DRY: вынести escapeField в общий /home/claudeuser/admik/lib/admin/csv.ts и переиспользовать в обоих модулях (newsletter+leads), чтобы не дублировать защиту от инъекций.
2) Создать /home/claudeuser/admik/app/admin/(panel)/leads/_components/ExportToolbar.tsx (client) — копия subscribers ExportToolbar: «Скачать CSV» (leadsToCsv, Blob с BOM, имя leads-YYYY-MM-DD.csv) + «Скопировать контакты» (rows.map(r=>r.contact).join(', ') в clipboard с fallback на window.prompt). Принимает rows c createdAtIso:string (Date не сериализуется через границу Server→Client).
3) Подключить в /home/claudeuser/admik/app/admin/(panel)/leads/page.tsx: собрать exportRows из leads (created_at.toISOString()) и передать action={<ExportToolbar rows={exportRows} />} в PageHeader (как на subscribers/page.tsx:53). Опционально (вне находки, но того же класса) — добавить listTruncationNotice как у подписчиков, т.к. лимит 200 сейчас без предупреждения.

**Тест:** Первым — TDD-юнит на чистую функцию (она не требует БД/Next). Создать /home/claudeuser/admik/tests/leads/csv.test.ts по образцу tests/newsletter/export-unsubscribe.test.ts (describe('subscribersToCsv')): импорт leadsToCsv из @/lib/leads/csv. Кейсы: (а) заголовок `id,name,contact,message,status,created_at` + порядок строк; (б) пустой список → только заголовок; (в) RFC 4180 — запятая/двойная кавычка внутри message/name квотируются и кавычки удваиваются; (г) перенос строки в message квотируется; (д) анти-CSV-инъекция — message/name/contact, начинающиеся с =,+,-,@, префиксуются апострофом (главный риск: значения из публичной формы); (е) дата → ISO. Сначала тест (красный, модуля нет) → затем lib/leads/csv.ts → зелёный. Затем /gate (typecheck+lint+vitest) и ручной прогон через playwright (страница /admin/leads: видны кнопки, скачивание CSV, копирование контактов) как вторая из двух обязательных проверок.

---

## C21 [low/high]
**Где:** THE CASE/src/components/layout/Footer.tsx:114-120 — fallback when `c.socials.length === 0`: line 118 `{ label: "Instagram", href: "#" }` (Telegram on line 119 uses a real URL, Instagram has no real default). Rendered at Footer.tsx:177-185 inside the `ci === lastCol` block as `<a key={s.label} href={s.href} target="_blank" rel="noopener noreferrer">` — so clicking "Instagram" opens a new tab to `#` (same page), a dead link. The empty-socials branch is the platform default: THE CASE/src/lib/store-settings.ts:189 `const socials = s?.contacts.socials ?? [];` and line 197 returns it as-is, so any tenant that hasn't filled socials in admin hits this fallback. CONTACTS_FALLBACK (Footer.tsx:38-45) also has `socials: []`. This is distinct from already-closed G-08 (which made configured socials readable via line 115-116); the empty-state fallback dead link was never addressed. No pure helper exists for this logic and no test covers it (THE CASE/src/lib/site-nav.test.ts covers footer columns only).

**Фикс:** Extract the social-links fallback into a pure, tenant-neutral helper and drop links without a real URL. 1) In THE CASE/src/lib/site-nav.ts add `export function resolveSocialLinks(c: ResolvedContacts): { label: string; href: string }[]`: if `c.socials.length > 0` → `c.socials.map(s => ({ label: s.type, href: s.url }))`; else build only real entries — push Telegram `{ label: "Telegram", href: c.telegramUrl }` ONLY when `c.telegramUrl` is a non-empty http(s)/tg URL, and DO NOT emit an Instagram entry (there is no real default URL; never use `#`). Optionally filter out any entry whose href is empty or `#` as a safety net. 2) In Footer.tsx replace the inline `socialLinks` block (lines 114-120) with `const socialLinks = resolveSocialLinks(c);` and import the helper. Keep `target="_blank" rel="noopener noreferrer"` rendering unchanged. This stays multitenant: no hardcoded Instagram, Telegram comes from resolved contacts; when nothing is configured the social row simply renders no broken links instead of a dead anchor.

**Тест:** Write the failing Vitest unit first in THE CASE/src/lib/site-nav.test.ts for `resolveSocialLinks`: (a) socials empty + telegramUrl present → returns exactly one Telegram link with the real URL and `expect(result.every(l => l.href !== '#')).toBe(true)` and no item with label "Instagram"; (b) socials empty + telegramUrl null/empty → returns `[]` (no dead links at all); (c) socials non-empty → maps each `{type,url}` to `{label,href}` preserving order and never injects `#`. Run vitest (red), then implement helper + Footer wiring (green). Manual second check: render footer of a tenant with no socials configured and confirm no "Instagram → #" link appears (playwright / browser per two-verifications rule).

---

## C22 [medium/high]
**Где:** THE CASE/src/components/layout/Header.tsx:77 — панель выпадашки: `className="invisible absolute left-0 top-full pt-4 opacity-0 transition-opacity duration-300 group-hover/nav:visible group-hover/nav:opacity-100"`. Раскрытие ТОЛЬКО по hover; нет ни `group-focus-within/nav:visible`, ни `group-focus-within/nav:opacity-100`. Родитель-группа: Header.tsx:73 `<div className="relative group/nav flex items-center">`, внутри фокусируемый триггер-ссылка Header.tsx:74. Так как `invisible` = `visibility:hidden`, дочерние `<Link>` (Header.tsx:79-87) выпадают из tab-order → клавиатурой недостижимы. Подтверждение отсутствия альтернативного механизма: grep по «THE CASE/src» (включая app/globals.css) даёт 0 совпадений для `focus-within`, `aria-haspopup`, `aria-expanded`. Десктоп-меню (lg≥1024, Header.tsx:70) — единственный путь к подпунктам категорий с клавиатуры; мобильное бургер-меню (menuOpen, строки 133-192) рендерит детей всегда видимыми, поэтому проблема именно в десктоп-навигации. Митигация: триггер «Коллекция»/«Информация» (Header.tsx:74) сам фокусируем и ведёт на link.href (/catalog и страницы Контента), т.е. подпункты достижимы окольно — но это нарушение WCAG 2.1.1 (Keyboard) для самих подпунктов.

**Фикс:** Файл: THE CASE/src/components/layout/Header.tsx. (1) В className панели на строке 77 добавить focus-within-варианты рядом с hover: `... group-hover/nav:visible group-hover/nav:opacity-100 group-focus-within/nav:visible group-focus-within/nav:opacity-100`. Фокус на триггер-ссылке (строка 74), которая лежит внутри `group/nav` (строка 73), активирует focus-within → панель становится visible → её дети (visibility больше не hidden) попадают в tab-order, и Tab продолжается внутрь подменю. Минимальная, чисто CSS-правка, без JS-состояния, мультитенантно (никакого хардкода под магазин — работает для любого набора link.children из buildHeaderNav). (2) Доступность для скринридеров (WCAG 4.1.2, вторично): добавить статический `aria-haspopup="menu"` на триггер-ссылку (строка 74). Полноценный динамический `aria-expanded` требует JS-состояния (CSS-only этого не умеет) — рекомендовать как отдельную мелкую доработку (перевод триггера на компонент с useState), но НЕ в рамках минимального фикса, чтобы не раздувать правку. Ядро дефекта (операбельность с клавиатуры) закрывается пунктом (1).

**Тест:** TDD-first (под существующий стиль витрины: vitest, environment 'node', чистые юниты в src/lib — testing-library/jsdom в проекте нет, см. vitest.config). Шаг 1 (red): вынести строку классов панели выпадашки в экспортируемую константу, напр. `NAV_DROPDOWN_PANEL_CLASS` в THE CASE/src/lib/site-nav.ts (рядом с buildHeaderNav), и импортировать её в Header.tsx:77 вместо inline-строки. Написать тест THE CASE/src/lib/site-nav.test.ts: `expect(NAV_DROPDOWN_PANEL_CLASS).toContain('group-focus-within/nav:visible')` и `...toContain('group-focus-within/nav:opacity-100')` (а также сохранение hover-вариантов) — тест падает на текущем коде. Шаг 2 (green): добавить focus-within-классы в константу. Шаг 3 (вторая из двух проверок, браузер): playwright в mock-режиме — навигировать на витрину, Tab до ссылки «Коллекция», проверить browser_snapshot что панель видима и первый дочерний пункт получает фокус по следующему Tab.

---

## C23 [low/high]
**Где:** Факты подтверждены по текущему коду:
1) THE CASE/src/app/catalog/page.tsx:30-31,41 — sale/isNew читаются из URL и идут в listProducts({category,q,sale,isNew,limit:60}) → серверный фасет.
2) THE CASE/src/components/catalog/CatalogPage.tsx:26-31,33-37 — CatalogPageProps = {products, categories, activeCategory}; sale/isNew в компонент НЕ передаются.
3) CatalogPage.tsx:137-173 — модалка фильтров: только ползунок цены и client-side чекбоксы onlyNew/onlyBestseller (применяются applyCatalogView, catalog-view.ts:231-239 — фильтрация уже загруженной страницы). Кнопка «Сбросить» (CatalogPage.tsx:165-170) сбрасывает ТОЛЬКО client-state (priceMax/onlyNew/onlyBestseller), URL-фасет sale/new НЕ трогает. Бейджа/индикатора активного фасета нет.
4) Точки входа: Footer.tsx:108-109 и site-nav.ts:58-61.

ВАЖНО — находка переоценивает тяжесть. Утверждение «не может отключить, нужно вручную менять URL» опровергается: goToCategory (CatalogPage.tsx:66-68) делает router.push(slug ? '/catalog?category=${slug}' : '/catalog'), а таб «Все» имеет slug "" (catalog-view.ts:43-48) → любой клик по табу/«Все»/футер-«Коллекция» строит свежий URL и СБРАСЫВАЕТ фасет sale/new. Жёсткого тупика нет; реальна лишь UX-непрозрачность: нет индикатора активного фасета и «Сбросить» вводит в заблуждение (оставляет фасет включённым). Поэтому severity = low.

**Фикс:** Минимальная мультитенантная правка (без хардкода под THE CASE):
1) catalog-view.ts — добавить чистый хелпер построения URL фасетов, напр. buildCatalogHref(params: {category?:string; q?:string; sale?:boolean; isNew?:boolean}) и/или removeFacet(params, facet): возвращает '/catalog' либо '/catalog?...' с сохранением category/q и нужных фасетов (encodeURIComponent). Логика чистая, тестируемая, переиспользуемая для любого ИМ.
2) app/catalog/page.tsx:52-57 — пробросить sale={sale ?? false} isNew={isNew ?? false} в <CatalogPage/>.
3) CatalogPage.tsx: расширить CatalogPageProps (sale, isNew). Рядом с заголовком (после строки счётчика :76-78) отрендерить ряд removable-чипов активных фасетов («Распродажа ✕» / «Новинки ✕») — показываются только когда фасет активен; клик по ✕ делает router.push(removeFacet(...)) с сохранением category/q. Это даёт И индикацию, И отключение в один клик.
4) Сделать «Сбросить» (CatalogPage.tsx:165-170) консистентным: если активны server-фасеты — также router.push(buildCatalogHref({category, q})) (сброс фасетов вместе с client-state). Не добавлять второй чекбокс «Новинки» в модалку — он конфликтовал бы с существующим client-side onlyNew (две «Новинки» путают).
ВНИМАНИЕ на ключи перемонтирования: page.tsx использует key={category}; чипы фасетов меняют URL без смены category — состояние client-фильтров корректно сохранится. После правки UI — прогон playwright (mock) на пути footer «Распродажа» → клик по чипу ✕ → каталог разворачивается в полный.

**Тест:** Первый TDD-тест — юнит на чистый хелпер в catalog-view.test.ts (без React/сети):
- buildCatalogHref({}) === '/catalog'
- buildCatalogHref({sale:true}) === '/catalog?sale=1'
- buildCatalogHref({category:'women', sale:true, isNew:true}) сохраняет category и оба фасета (порядок зафиксировать), q пробрасывается с encodeURIComponent
- removeFacet({category:'women', sale:true, isNew:true}, 'sale') === '/catalog?category=women&new=1'
- removeFacet({sale:true}, 'sale') === '/catalog' (снятие последнего фасета даёт чистый каталог)
Затем — после реализации чипов — playwright mock-прогон затронутого пути как вторая проверка.

---

## C24 [low/high]
**Где:** Текущий код подтверждает находку. /home/claudeuser/admik/THE CASE/src/app/cart/page.tsx:95-103 — кнопка Plus вызывает updateQuantity(item.variantId, item.quantity + 1) БЕЗ атрибута disabled и без проверки item.available. /home/claudeuser/admik/THE CASE/src/lib/store.ts:72-91 — updateQuantity молча зажимает: Math.min(quantity, Math.max(1, i.available)) при заданном available, то есть лишние клики не дают визуального эффекта. Поле реально заполняется: types/index.ts:33 (available?: number), а снимок выставляется при добавлении в ProductCard.tsx:172,183 (selectedVariant.availableQty / view.availableQty) и ProductDetailClient.tsx:97,112 (availableQty). Ключевое: ровно предложенный паттерн УЖЕ применён на карточке товара — ProductDetailClient.tsx:198-200: disabled={activeAvailable != null && quantity >= activeAvailable} + классы disabled:opacity-30 disabled:cursor-not-allowed. Корзина этому паттерну не следует → подтверждённая непоследовательность UX. Тестов на этот зажим/аффорданс нет (store.test.ts покрывает только removeOrder; .test.tsx для UI отсутствуют).

**Фикс:** Минимальная мультитенантная правка (логика про остаток, без привязки к магазину): в /home/claudeuser/admik/THE CASE/src/app/cart/page.tsx на кнопке Plus (строки 95-103) добавить disabled={item.available != null && item.quantity >= item.available} и классы disabled:opacity-30 disabled:cursor-not-allowed — дословно как в ProductDetailClient.tsx:198-200, чтобы поведение страниц совпадало. Опционально, чтобы дать явный фидбэк, при достижении лимита показывать рядом со степпером подпись «Макс. {item.available} шт.» (текст вынести как обычную строку, без хардкода под конкретный магазин). Чтобы избежать дублирования предиката, лучше вынести крошечный чистый хелпер в src/lib/store.ts, напр. export function isAtStockLimit(item: Pick<CartItem,'available'|'quantity'>): boolean { return item.available != null && item.quantity >= item.available; } и переиспользовать его и в корзине, и (по желанию) при рефакторинге карточки товара. Бизнес-инвариант (фактический зажим) уже обеспечен store.ts и бэкендом (резерв/quote) — правка чисто про видимую аффордансность.

**Тест:** Сначала TDD-юнит на хелпер (стиль репо — vitest, чистые функции): новый кейс в src/lib/store.test.ts для isAtStockLimit — (a) available=3, quantity=3 → true; (b) available=3, quantity=2 → false; (c) available=undefined → false (старые записи без лимита); (d) граница quantity>available → true. Параллельно закрыть существующий пробел: тест на сам зажим updateQuantity (его сейчас нет) — addToCart({...,available:3}); updateQuantity(id, 5); expect quantity===3; и updateQuantity(id, 0) → товар удаляется. После зелёных юнитов внести JSX-правку disabled на cart/page.tsx (компонентного теста не требуется — паттерн уже верифицирован на ProductDetailClient), затем ручной прогон в playwright (mock-режим): добавить в корзину товар с малым остатком, на /cart дожать «+» до лимита и убедиться, что кнопка задизейблена и не реагирует.

---

# WONT_FIX

- (low) THE CASE/src/app/account/page.tsx:27 — текущее условие: `return (st === "pending" || st === "failed" || st === "unset" || !st) && (m === "card" || m === "sbp");` (st = order.paymentStatus?.toLowerCase
- (low) Оба фактических утверждения находки опровергаются текущим кодом. (1) Дефолт НЕ 'unset': app/admin/(panel)/orders/new/page.tsx:35-38 всегда строит paymentOptions из полного PAYMENT_METHODS (lib/orders/
- (low) Форма NavigationForm была УДАЛЕНА намеренно по решению аудита, а не «не дописана». Доказательства из git и текущего кода:

1) Коммит c3df1e7 («Batch 1 аудита»), тело: «Удалена осиротевшая форма Naviga
- (low) app/admin/(panel)/roles/page.tsx:66 действительно рендерит `<span className="ml-2 text-xs text-gray-400">{row.code}</span>` — код роли рядом с названием. Описание находки фактически верное, но это by-
- (low) Код подтверждает описанное состояние, но «тупика» нет. (1) Поле хранится как единая свободная строка: lib/leads/schemas.ts — `contact: z.string().trim().min(1).max(200)`; lib/leads/repository.ts:11 `c
- (low) app/admin/(panel)/leads/page.tsx:52 рендерит {l.message} ПОЛНОСТЬЮ inline без усечения — значит владелец видит весь текст заявки (до 5000 симв. по lib/leads/schemas.ts:16), а не урезанный фрагмент. По
