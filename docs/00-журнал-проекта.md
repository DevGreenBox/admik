# Журнал проекта (История)

> Ведётся агентом **History Manager**. Цель — в любой момент восстановить контекст проекта без анализа скрытых сессий.
> Формат: новые записи сверху внутри раздела. Даты в формате ГГГГ-ММ-ДД.

---

## Краткое резюме проекта

**Admik** — универсальная, переиспользуемая административная панель для интернет-магазинов (ИМ).
Проектируется как CMS/ERP-платформа для сети магазинов: единая кодовая база, отличия между магазинами — только в конфигурации.

**Ключевая модель развёртывания:** один ИМ = одна БД. Перенос на новый магазин — по принципу «копировать-вставить» (его будут выполнять другие люди, поэтому процесс обязан быть максимально простым). Подробно — `docs/02-модель-развёртывания.md`.

---

## История запросов пользователя

### 2026-06-15

1. **«Ознакомься и приступай»** — изучить CLAUDE.md и существующие проекты (2x2, carre), начать работу.
2. **«Отвечай на русском»** — язык общения: русский.
3. Уточнение по стеку: выбран **Next.js + PostgreSQL (как в 2x2)** с переносом логики СДЭК из carre.
4. **«Один ИМ — 1 БД — далее переносим на другой ИМ её»** — модель мультитенантности: не общая БД с tenant_id, а отдельная БД на каждый магазин; универсальность через конфигурацию.
5. **«…в будущем эту БД будут приделывать другие люди, так что всё должно быть просто копировать-вставить»** — требование: развёртывание нового магазина = простой copy-paste, доступный сторонним людям.
6. **«погоди, ещё скилы скачаем»** — пользователь планирует догрузить скиллы.
7. **«запускай все задачи поочередно»** + **«даю разрешение на все»** — проводить работы последовательно, автономно, с разрешением на все действия в рамках проекта.
8. **«Продолжай работу»** — продолжить с текущего состояния без переуточнений.
9. **«Обязательно вся документация на случай если ты упадёшь»** — требование вести полную документацию для восстановления контекста при падении сессии (crash-recovery).
10. **«Отвечай на русском»** (повторно подтверждено) — язык общения остаётся русским.
11. **«Мне нужно уехать, даю разрешение на всё, если будут вопросы — не останавливай работу»** — полная автономия: не задавать уточняющих вопросов, не останавливать работу, действовать самостоятельно.
12. **Прислал две реальные витрины-ИМ, к которым приставляется админка** — **THE CASE** (премиальная медицинская форма, https://darling-lebkuchen-9b78cc.netlify.app/) и **Gang Auto** (автозапчасти, 2000+ SKU, https://roaring-youtiao-873525.netlify.app/). Уточнил, что админка к этим витринам **«приставляется»**. Вывод (зафиксирован как ADR-008): Admik — **headless-бэкенд** + **публичный Storefront API** для внешних SPA-витрин (Netlify), а не монолит с собственной витриной. Анализ витрин и gap-анализ каталога — `docs/06-референсные-магазины.md`.
13. **Несколько раз спрашивал статус / «не зависло ли»** — работа ведётся автономно, волнами, с коммитами на вехах (crash-recovery), без остановок; статус восстанавливается из документации.
14. **Прислал 2 витрины-ИМ** (повторно подтвердил состав потребителей платформы: THE CASE и Gang Auto).
15. **«Останови работу»** — пользователь дал команду остановить активную разработку.
16. **«Подготовь всё, ввожу /compact»** — подготовить контекст к компактификации сессии: журнал должен позволить новой сессии после `/compact` восстановить состояние без анализа скрытых сессий.
17. **Предупреждение: НЕ копировать ключи СДЭК** — ключи СДЭК **принадлежат другим людям (третьим лицам)**; запрещено копировать/вносить их в репозиторий. Работаем только в mock-режиме; реальных ключей СДЭК в репо нет.
18. **Включил режим ultracode** — оркестрация через workflow (xhigh + workflow-оркестрация): задачи выполняются волнами через агентов с повышенным бюджетом рассуждений.

---

## Принятые решения

- **Стек:** Next.js 16 (App Router) + React 19 + TypeScript (strict) + PostgreSQL + Lucia Auth + Docker + Caddy + S3/MinIO. См. `docs/01-архитектурные-решения.md`.
- **Источник паттернов админки:** проект 2x2 (Server Actions, ролевая модель, CMS, аудит, безопасность).
- **Источник логики СДЭК:** проект carre (Manager→Client→Services, webhook-идемпотентность, cron-воркеры).
- **Мультитенантность:** 1 магазин = 1 БД; код single-tenant в рантайме; универсальность через конфиг. См. `docs/02-модель-развёртывания.md`.
- **Принцип:** «сначала тесты, потом код» (из CLAUDE.md).
- **Оркестрация:** ведётся через универсальный агент `claude` с встроенной ролью. Кастомные типы агентов из `.claude/agents/` не зарегистрированы в текущем харнессе — это ограничение среды, не проекта (состав агентов в `.claude/agents/` сохраняется как спецификация ролей).
- **RBAC (ADR-005):** гибкая модель «роли → права» (`users—user_roles—roles—role_permissions—permissions`); проверки в коде идут по правам, не по именам ролей.
- **Auth (ADR-006):** Lucia-подход — сессии в БД (без JWT), хеширование паролей argon2id, две роли БД (`app`/`migrator`), `audit_log` append-only на уровне прав БД.
- **Crash-recovery:** git-коммиты на вехах (на каждой волне работ) используются как механизм восстановления при падении сессии (по требованию пользователя о защите от падения). Документация (docs/00–04) — основной источник восстановления контекста.
- **e2e Playwright вынесен в отдельный скрипт `test:e2e`:** браузерные e2e-тесты (a11y/навигация каркаса админки) не входят в обязательный прогон `vitest` и гоняются отдельно в CI/проде. Это держит локальный `vitest` зелёным в окружении без браузера.
- **Криптопримитивы сессий — на `node:crypto`:** пакет `oslo` (изначально предложенный в ADR-001/спецификации Этапа 1) отклонён как deprecated; криптослучайные id сессий и константное сравнение реализованы стандартной библиотекой Node.
- **ADR-008 (Admik = headless commerce backend + публичный Storefront API):** платформа «приставляется» к внешним SPA-витринам (Netlify), а не несёт собственную витрину. Витрины ходят в платформу через публичный **Storefront API `/api/storefront/v1/*`** (read-каталог: products/categories/brands; в будущем — заказ и СДЭК-расчёт/ПВЗ). Аутентификация витрины (API-ключ/домен), CORS, версионирование, rate-limit, DTO без утечки внутренних данных (наружу отдаётся `inStock`, а не точный остаток). Подтверждено двумя разными тематиками (медформа vs автозапчасти) на одной кодовой базе — различия только в данных/настройках (ADR-003). См. `docs/06-референсные-магазины.md`.
- **ADR-009 (ценообразование/флаги/бренды каталога — минимально, универсально, опционально):**
  - **`compare_at_price`** (в `products` и `product_variants`) — каталожная цена «было»; **скидка % и предикат «со скидкой» вычисляются, не хранятся** (нет рассинхрона). Выбрано единое поле вместо пары `old_price`/`sale_price`; источник истины актуальной цены — `base_price`/вариантные поля.
  - **Флаги товара:** `is_featured` — ручной маркетинговый «Хит/Рекомендуемый» (вычислить нельзя); `is_new` — **троичная логика** (`NULL` → новизна вычисляется по `created_at` и настройке `SHOP_NEW_PRODUCT_DAYS`, дефолт 30; `true/false` → явный override редактора); «со скидкой» — производная цен, отдельным флагом не хранится.
  - **Бренды — отдельной опциональной таблицей `brands`** + `products.brand_id` (nullable, `ON DELETE SET NULL`), а не EAV: бренд — сущность с лого/описанием/страницей/фасетом; пуста для магазинов без брендов (THE CASE).
  - **VIN/совместимость (fitment)** — только точка расширения: будущий опциональный модуль `fitment` (обобщённый, не «авто»-хардкод), в `0011` не реализуется.
  - **Валюта** — настройка магазина `SHOP_CURRENCY` (дефолт `'RUB'`, ₽), форматирование — в слое представления/Storefront API; в данных каталога валюта не хардкодится; поля валюты в `0011` не добавляются (одна валюта на магазин — ADR-003).
- **Технический нюанс `'use server'`:** модуль с директивой `'use server'` может экспортировать **только async-функции**. Поэтому класс `CatalogError` вынесен из `actions.ts` в отдельный `lib/catalog/errors.ts` (а не-action-экспорты не размещаются в server-action-модулях). На Этапе 3 аналогично — `lib/orders/errors.ts`.
- **ADR-010 (anti-tamper + снимки позиций заказа):** **итог заказа считает только сервер** — витрине доверяем лишь `variantId`+`qty`+выборы доставки/промокода, все деньги пересчитываются заново из каталога (защита от подмены цены на клиенте). `POST /cart/quote` и `POST /orders` считают итог независимо; цена из тела запроса игнорируется. Позиции заказа (`order_items`) — **денормализованный снимок** (name/sku/price/attributes на момент покупки): изменение каталога не переписывает историю; ссылки `product_id`/`variant_id` — `ON DELETE SET NULL`. ADR-010 добавлен в конец `docs/01`.
- **Деньги в заказах — целые копейки (без float):** денежные расчёты домена `orders` ведутся в **целых копейках** (`lib/orders/money.ts`), что исключает ошибки округления float; в БД деньги — `numeric(14,2)` (как в каталоге).
- **Резерв остатков — атомарным `UPDATE inventory`:** резерв при создании заказа выполняется `UPDATE inventory SET reserved = reserved + qty WHERE … AND quantity - reserved >= qty` с проверкой числа затронутых строк (`= 1`), всё в одной транзакции. Конкурентные заказы на последний остаток: только один резервирует, второй получает `0` строк → откат транзакции (гонко-безопасно, без явных блокировок/дедлоков).
- **Доступ к трекингу заказа (`GET /orders/{number}`)** — по **HMAC-токену** (ключ `APP_PASSWORD`) либо по совпадению email; защита от перебора чужих номеров заказов.
- **Стоимость доставки на Этапе 3 — заглушка `0`:** реальный расчёт доставки (СДЭК) — **Этап 4**; на Этапе 3 в `quote`/создании заказа стоимость доставки фиксируется заглушкой (0 / порог бесплатной доставки), поля под доставку (тип/адрес/ПВЗ/стоимость/трек/`delivery_status`) уже заложены в схеме `0012`.
- **Способ оплаты на Этапе 3 — фиксация + mock:** на Этапе 3 фиксируются только способ и статус оплаты (`payment_method`/`payment_status`/`paid_at`/`payment_ref`); реальные платёжные провайдеры (карты/СБП/СДЭК PAY/эквайринг) — будущий под-этап, подключаются аддитивно без рефактора модели заказа.
- **ADR-011 (СДЭК: слоистый порт carre + mock-режим + идемпотентный webhook):** логика СДЭК портирована из carre (PHP/Yii2) на TS/Next.js со слоистой архитектурой **Manager → Client → Services**. `Client` инкапсулирует HTTP/OAuth и кеш токена; `Services` (calculator/pvz/order/tracking/webhook/print/status-map/delivery-status) — доменная логика; `Manager` — фасад для остального кода. **Mock-режим** определяется конфигом (`isCdekMock`): при отсутствии боевых ключей CDEK_* клиент работает на заглушках, реальная сеть не дёргается. **Webhook идемпотентен** (дедуп через таблицу `cdek_status_log`), всегда отвечает `200`, защищён IP-whitelist (+CIDR). Развязка `orders ↔ cdek` — ленивым импортом адаптера `lib/orders/delivery-cost.ts` (заменил заглушку доставки `0` из Этапа 3). ADR-011 добавлен в `docs/01`; спецификация — `docs/08-этап4-cdek.md`.
- **Никогда не копировать чужие секреты (СДЭК/платёжки):** ключи СДЭК и платёжных провайдеров **принадлежат третьим лицам** — запрещено копировать их или вносить в репозиторий. В коде только mock-режим и env-плейсхолдеры (`CDEK_*`); боевые ключи подставляют владельцы магазина при развёртывании. Реальных ключей СДЭК в репозитории нет.

---

## Список задач (роадмап)

См. актуальный статус в `docs/03-роадмап.md`. Кратко по этапам:

- [x] **Этап 0. ✅ завершён.** Фундамент репозитория: скаффолд Next.js 16, конфиг-система, тесты (Vitest 14/14 PASS), Docker/CI-инфраструктура, документация, агенты.
- [x] **Этап 1. ✅ завершён.** Ядро + Auth + RBAC + каркас админки + audit_log. Реализован по `docs/04-этап1-ядро-auth-rbac.md` (+ ADR-005/006), порядок 1.1 → 1.2 → 1.3 → 1.6 → 1.5 → 1.4 → 1.7. typecheck/lint зелёные, vitest 94 passed/13 skipped, next build OK.
- [x] **Этап 2. ✅ завершён.** Каталог товаров (категории-дерево, товары, варианты, EAV-характеристики, медиа, остатки) + расширение под витрины (миграция 0011: акционная цена/флаги/бренды) + публичный **Storefront API** (`/api/storefront/v1/*`, под-этап 2.x по ADR-008). typecheck/lint зелёные, vitest 285 passed/32 skipped, next build OK.
- [x] **Этап 3. ✅ завершён.** Заказы + корзина/чекаут. Модуль `orders`: миграции `0012–0016` (orders/order_items[снимок ADR-010]/order_status_history/customers/promo_codes/promo_redemptions/order_number_counters), `lib/orders/*` (money/pricing/promo/repository/actions/status/errors), Storefront API (`/cart/quote`, `/orders`+idempotency, `/orders/[number]`), UI `/admin/orders/*` + `/admin/promo/*`. typecheck/lint зелёные, vitest 448 passed/49 skipped, next build OK.
- [x] **Этап 4. ✅ завершён.** Интеграция СДЭК (полный порт из carre, пакеты A–G): миграции `0017–0018`, слоистый клиент+OAuth+кеш токена+mock, сервисы (calculator/pvz/order/tracking/webhook/print/status-map/delivery-status), Storefront-доставка, идемпотентный webhook (`/api/cdek/webhook`), admin-UI и cron. typecheck/lint зелёные, vitest 689 passed/60 skipped, build OK. Реальных ключей СДЭК в репо нет (mock-режим).
- [ ] **Этап 5.** CMS, промо, SEO, настройки магазина. **← следующий фокус.**
- [ ] **Этап 6.** DevOps: деплой по «copy-paste», бэкапы, мониторинг, обновления.

---

## Завершённые задачи

### 2026-06-15

- **Этап 4 (СДЭК) завершён (✅).** Интеграция СДЭК реализована полным портом логики из carre (PHP/Yii2) на TS/Next.js по спецификации `docs/08-этап4-cdek.md` + ADR-011 (слоистый порт + mock-режим + идемпотентный webhook). По пакетам задач A–G:
  - **A. Фундамент.** Миграции `0017–0018`, доменные типы СДЭК, конфиг с детектом mock-режима (`isCdekMock`), новые env-переменные `CDEK_*` (боевые ключи подставляются владельцами при деплое — в репо их нет).
  - **B. Клиент + OAuth.** `lib/cdek/client.ts` + OAuth-аутентификация + кеш токена (token-cache) + `lib/cdek/mock/` (заглушки на случай отсутствия боевых ключей; реальная сеть не дёргается в mock-режиме).
  - **C. Доменные сервисы (порт carre).** `Calculator` (расчёт стоимости), `PvzService` (пункты выдачи), `StatusMap` (маппинг статусов СДЭК → внутренние статусы доставки).
  - **D. Order/Tracking/Webhook/Print.** Сервисы создания заказа СДЭК, трекинга, обработки webhook (**идемпотентность** через таблицу `cdek_status_log`), печати накладной (`Print`) + сервис `delivery-status`.
  - **E. Storefront-доставка.** Эндпоинты `/api/storefront/v1/delivery/cdek/{pvz, calculate}` + адаптер `lib/orders/delivery-cost.ts` — **заменил заглушку доставки `0` из Этапа 3** реальным расчётом; развязка `orders ↔ cdek` через **ленивый import** (нет циклической зависимости модулей).
  - **F. Webhook + admin-UI.** `/api/cdek/webhook` (**IP-whitelist + CIDR**, идемпотентность, **всегда отвечает `200`**) + `lib/cdek/actions.ts` (право `cdek.manage`) + UI-блок `CdekBlock` в карточке заказа админки.
  - **G. Cron.** `/api/cron/cdek/[task]` + `lib/cdek/cron.ts` (синхронизация статусов) + cron-сервис в `docker-compose`.
  - **Итог:** typecheck/lint/build зелёные; vitest — **689 passed / 60 skipped**; mock-режим работает без боевых ключей; **реальных ключей СДЭК в репозитории нет** (принадлежат третьим лицам). Код Этапа 4 закоммичен (`85a0b39`, ветка `foundation/etap-0-1`); фиксация документации и гайда продолжения `docs/10` — отдельным docs-коммитом следом. Актуальный «снимок состояния и как продолжить» — в `docs/10-состояние-и-продолжение.md`.

- **Этап 3 завершён (✅).** Модуль `orders` реализован по спецификации `docs/07-этап3-заказы.md` (единая модель заказа ADR-002 + anti-tamper/снимки ADR-010). По пакетам задач:
  - **3.A. Миграции заказов `0012–0016` + доменный слой.** `0012` (`orders` + `order_items` [снимок name/sku/price/attributes — ADR-010] + `order_status_history`), `0013` (`customers` — задел под ЛК, upsert по email), `0014` (`promo_codes`), `0015` (`promo_redemptions`), `0016` (`order_number_counters` — человекочитаемый уникальный номер). Идемпотентные DDL, гранты `admik_app`. Домен `lib/orders/{types,status,schemas}.ts`: `status.ts` — **три статус-машины** (заказ/оплата/доставка) с whitelist-переходами данными; `schemas.ts` — Zod. Новые env `SHOP_FREE_DELIVERY_THRESHOLD` (default 0) и `SHOP_ORDER_PREFIX` (default '') через `lib/config/env.ts`.
  - **3.B. Бизнес-логика расчёта/создания.** `lib/orders/money.ts` (деньги в **целых копейках**, без float-ошибок), `lib/orders/pricing.ts` (anti-tamper расчёт итога: items/discount/delivery/grand + порог бесплатной доставки), `lib/orders/promo.ts` (`validatePromo`: тип/срок/лимиты/мин.сумма), `lib/orders/repository.ts` (`quoteCart`; `createOrder` в **одной транзакции** — атомарный резерв `inventory` + выдача номера + снимок позиций + идемпотентность по `idempotency_key`).
  - **3.C. Server Actions админки + промокоды.** `lib/orders/actions.ts` (`changeOrderStatus` со статус-машиной и резервом, `cancel`/`refund` с release резерва, `setPaymentStatus`/`setDeliveryStatus`, `createManualOrder`, CRUD промокодов) + `lib/orders/errors.ts` (классы ошибок вне server-action-модуля — нюанс `'use server'`). Право `orders.write`, аудит-события `orders.*`.
  - **3.D. Storefront API.** `app/api/storefront/v1/{cart/quote, orders (POST + Idempotency-Key), orders/[number] (доступ по HMAC-токену/email)}` + `lib/storefront/order-dto.ts`. **Anti-tamper** (цены считает только сервер, тело запроса не источник цены); коды `409` (нет остатка) / `422` (валидация/промокод).
  - **3.E. UI админки.** `app/admin/(panel)/{orders (список с фильтрами + карточка со статус-машиной и историей), promo (CRUD)}` + `lib/admin/order-format.ts`; новый пункт меню «Промокоды».
  - **Итог:** typecheck/lint зелёные; vitest — **448 passed / 49 skipped**; `next build` успешен (собираются маршруты `/admin/orders/*`, `/admin/promo/*`, `/api/storefront/v1/cart|orders`). Все вехи закоммичены.

- **Этап 2 завершён (✅).** Модуль `catalog` реализован по спецификации `docs/05-этап2-каталог.md` + расширен под референсные витрины (`docs/06-референсные-магазины.md`, ADR-008/009). По пакетам задач:
  - **П1. Миграции каталога `0005–0010`.** `categories` (дерево, adjacency-list, `pg_trgm`), `products` + `product_categories` (M2M), `product_variants`, EAV-характеристики (`attributes`/`attribute_values`/`product_attributes`), `product_media`, `inventory`. Идемпотентные DDL, `app`-гранты, UUID-PK, `NUMERIC(14,2)`, `citext`.
  - **П2. Хранилище медиа `lib/storage/*`.** S3/MinIO + локальный mock (авто-выбор по env), валидация по **magic-bytes**, превью/нормализация через `sharp`, анти-path-traversal-ключи.
  - **П3. Слой данных `lib/catalog/*`.** Единые файлы `types/schemas/slug/tree/repository/cache` + **19 Server Actions** через `defineAction` (право `catalog.read`/`catalog.write`, инвалидация, аудит).
  - **П5. Seed демо-каталога.** Опциональный, под гейтом `SEED_DEMO_CATALOG`; идемпотентный (`ON CONFLICT DO NOTHING`).
  - **Расширение под витрины.** Миграция **`0011_catalog_pricing_flags_brands.sql`** (`compare_at_price` в `products`/`product_variants`, флаги `is_featured`/`is_new`, таблица `brands` + `products.brand_id`); `lib/catalog/pricing.ts` (вычисление скидки/«новизны»/«со скидкой»); `lib/catalog/errors.ts` (`CatalogError` вынесен из server-action-модуля — нюанс `'use server'`).
  - **П4. UI `app/admin/(panel)/catalog/`.** Список товаров с фильтрами (поиск/статус/категория/бренд/флаги/скидка), форма товара (основное/варианты/характеристики/медиа/SEO + поля цены «было», чекбоксы «Хит»/«Новинка», выбор бренда), дерево категорий, CRUD брендов.
  - **Storefront API `app/api/storefront/v1/`** (под-этап 2.x, ADR-008). Эндпоинты `products`/`products/[slug]`/`categories`/`brands`; слой `lib/storefront` (аутентификация витрины по API-ключу/домену, CORS, rate-limit, DTO без утечки внутренних данных — наружу `inStock` вместо точного остатка).
  - **Итог:** typecheck/lint зелёные; vitest — **285 passed / 32 skipped**; `next build` успешен (собираются все `/admin/catalog/*` и `/api/storefront/v1/*`). Все вехи закоммичены.

- **Этап 1 завершён (✅).** Ядро + Auth + RBAC + каркас админки + audit_log реализованы по спецификации `docs/04-этап1-ядро-auth-rbac.md`. По подзадачам (фактический порядок 1.1 → 1.2 → 1.3 → 1.6 → 1.5 → 1.4 → 1.7):
  - **1.1. Слой БД + идемпотентные миграции.** `db/migrations/0001-0004` (расширения + `schema_migrations`, `users`/`sessions`, RBAC-таблицы, `audit_log`), `lib/db/client.ts` (драйвер `postgres.js`, tagged templates), `lib/db/migrate.ts` (runner), две роли БД `app`/`migrator` с точечными грантами (app — append-only на `audit_log`).
  - **1.2. Аутентификация.** `lib/auth/password.ts` (argon2id + timing-защита через фиктивную проверку), `lib/auth/rate-limit.ts` (Redis + mock-fallback без `REDIS_URL`), `lib/auth/session.ts` + `lib/auth/cookies.ts` (Lucia-подход: криптослучайные id сессий, сессии в БД, кука `admik_session` httpOnly/Secure/SameSite=Lax).
  - **1.3. RBAC.** `lib/auth/permissions.ts` (каталог прав и системных ролей owner/admin/manager), `lib/auth/rbac.ts` (`can`/`requirePermission`, гибкая модель «роли → права» по ADR-005; owner короткозамкнут на «всё»).
  - **1.6. Унифицированный паттерн Server Action.** `lib/server/action.ts` (`defineAction`: пайплайн guard → Zod → БД → invalidate → audit).
  - **1.5. Сквозной аудит.** `lib/audit/log.ts` (`writeAudit` + санитизация чувствительных полей `password_hash`/`password`/токены), миграция `0004` (append-only на уровне прав БД), страница `/admin/audit` под правом `audit.read`.
  - **1.4. Каркас админки.** `middleware.ts` (гвард сессии для `/admin/*`), `app/admin/(panel)/` (layout + дашборд + разделы users/roles/audit), `/admin/login`, `lib/admin/nav.ts` (меню = f(включённые модули, права)); auth-Server-Actions `login`/`logout`/`changePassword`.
  - **1.7. Seed.** `db/seed/{permissions.sql,roles.sql,owner.mjs}` (идемпотентны, владелец из `.env`), интеграция в `scripts/init-shop.sh` (закрыта заглушка seed, шаг 4), новые env-переменные (`OWNER_EMAIL`, `OWNER_PASSWORD`, `APP_PASSWORD`, `MIGRATOR_PASSWORD`).
  - **Итог:** typecheck/lint зелёные; vitest — 94 passed / 13 skipped (интеграционные помечены `skipIf` и не гонятся без `DATABASE_URL`); `next build` успешен. Все вехи закоммичены (ветка `foundation/etap-0-1`).
  - **Технические заметки.** Окружение разработки без PostgreSQL/Redis/браузера → интеграционные (БД, rate-limit) и e2e-тесты (Playwright a11y/навигация) написаны, но фактически гоняются в CI/проде. Новые зависимости: `@node-rs/argon2` (argon2id) и `ioredis` (rate-limit). Пакет `oslo` отклонён как deprecated — криптослучайные id сессий и константное сравнение реализованы на `node:crypto`.

- **Этап 0 завершён (✅).** Скаффолд Next.js 16, конфиг-система (`lib/config/env.ts`, `lib/config/modules.ts`), тесты Vitest (14/14 PASS), 9 агентов в `.claude/agents/`, документация docs/00–04.
- **Инфраструктура «copy-paste».** `Dockerfile` (multi-stage standalone), `docker-compose.yml` (app/postgres/redis/minio/caddy с healthcheck), `Caddyfile` (авто-SSL), `.dockerignore`, идемпотентный `scripts/init-shop.sh`, CI workflow (`.github/workflows/ci.yml`), дополнен `.env.example`.
- **Роадмап.** Создан `docs/03-роадмап.md` (закрыт битый линк из журнала).
- **Проектирование Этапа 1.** `docs/04-этап1-ядро-auth-rbac.md` + ADR-005 (RBAC) / ADR-006 (Auth) в `docs/01-архитектурные-решения.md`.
- **Типы lib/config починены.** `Record<string, string | undefined>` вместо `NodeJS.ProcessEnv` — typecheck PASS.
- **Линтинг на ESLint 9 flat config.** `eslint.config.mjs` (удалён `.eslintrc.json`, lint-скрипт = `eslint .`) — `pnpm lint` PASS, CI зелёный.

## Отложенные задачи

- Подключение реального первого магазина и боевых ключей интеграций — после готовности скелета (работаем на mock/demo).
- Догрузка скиллов пользователем — ожидается.
- Регистрация кастомных типов агентов из `.claude/agents/` — заблокирована ограничением текущего харнесса; работаем через универсальный агент `claude`.
