# 17 — Волна 3 поиска багов: находки на разбор (НЕЗАВЕРШЕНО)

> **Статус: ПРЕРВАНО рестартом устройства (2026-06-18).** Workflow `admik-bughunt-wave3`
> (11 доменных финдеров → по 2 адверсариальных верификатора на находку) **не довёл фазу
> верификации до конца** (≈33 из ~45 агентов). Этот файл — checkpoint, чтобы новая сессия
> продолжила БЕЗ повторного дорогого прогона.
>
> **ВАЖНО про методологию.** Финдеры намеренно over-report (лучше лишнее, чем пропуск).
> Верификаторы (скептики) опровергают спекулятивное. **Промежуточная сводка вердиктов**
> (на момент прерывания, ~22 вердикта): `true/high` — **4** (3 major + 1 minor);
> `false/high (not-a-bug)` — **18**. То есть БОЛЬШИНСТВО находок финдеров было опровергнуто —
> это норма. **НЕ чинить вслепую: сначала верифицировать каждую находку (TDD-репро по полю
> `repro`), чинить только подтверждённые.**

## Как продолжить (для новой сессии)

1. Прогнать гейт (`pnpm typecheck && pnpm lint && pnpm test`) — должно быть зелено
   (**1396 passed / 84 skipped** на HEAD; третья серия фиксов уже закоммичена).
2. Для каждой находки ниже: открыть `file:line`, проследить РЕАЛЬНЫЙ текущий код,
   написать падающий тест по `repro`. Если репро НЕ воспроизводится → находка опровергнута
   (записать в `docs/00` как отклонённую). Если воспроизводится → минимальный фикс по TDD,
   ручной прогон с фактическими значениями, коммит `fix(<домен>): …`.
3. Можно перезапустить и сам workflow заново: `Workflow({scriptPath: ".../admik-bughunt-wave3-wf_4ee37dcb-07e.js"})`
   (скрипт сохранён в session workflows/scripts) — он переотберёт и переверифицирует.
4. Обновить `docs/00` (Завершённые задачи) и `docs/10` (снять верхний блок про незавершённую волну 3).

## Предварительная приоритизация (МОЯ оценка, требует верификации)

**Похоже на РЕАЛЬНЫЕ (верифицировать первыми):**
- **#5 tbank webhook idempotency** (critical) — при сбое `applyPaymentStatus` после `insertPaymentLog`
  повтор webhook видит дубль и НЕ применяет статус → платёж навсегда `pending`. Деньги. Сильный кандидат.
- **#11 IDOR attachMedia** (critical) — `variantId` не проверяется на принадлежность `productId`. Сильный кандидат.
- **#15 gift compare_at_snapshot** (major) — пишется `giftLine.unitPrice` (0.00) вместо `giftLine.compareAt`. Сильный кандидат.
- **#16 dashboard без module-gate orders** (major) — дашборд читает `orders` без `isModuleEnabled('orders')`.

**Под сомнением (вероятно опровергнуто верификаторами — перепроверить критически):**
- **#1 self-lockout сброса пароля** — админ ЗНАЕТ новый пароль, которым сбросил свой → может войти заново.
  Это НЕ настоящий lockout (в отличие от снятия своих ролей). Вероятно not-a-bug.
- **#2 token-cache single-flight finally** — очистка `inFlight` в `finally` на ошибке, скорее всего,
  КОРРЕКТНА (иначе закешировался бы отклонённый promise навсегда). Вероятно by-design.
- **#13/#14 console.error утечка** — зависит от того, реально ли объект ошибки postgres.js содержит
  connection string/пароль (обычно НЕТ). Перепроверить, что именно попадает в лог.
- **#7/#9 compareAt в variant DTO** — финдер сам пишет «logic is correct / no fix needed». Скорее not-a-bug.

**Гонки/TOCTOU (нужна оценка by-design vs реальный риск):**
- **#3 tracking TOCTOU**, **#4 refresh-active без advisory-lock**, **#6 moveCategory TOCTOU** —
  оценить, атомарны ли уже нижележащие операции (как в orders/payments guarded UPDATE).

**Мелочь/полировка:** #8, #10 (robots_extra валидация), #12, #17.

---

## Все 17 находок (как выдали финдеры — severity ФИНДЕРА, не финальная)

| # | Sev(финдер) | Файл:строка | Заголовок |
|---|---|---|---|
| 1 | major | lib/auth/admin-actions.ts:238 | Self-lockout при сбросе своего пароля |
| 2 | critical | lib/cdek/token-cache.ts:275 | Token cache single-flight race на ошибке |
| 3 | major | lib/cdek/services/tracking.ts:127 | Tracking TOCTOU: shipment update не атомарен с delivery status |
| 4 | major | lib/cdek/cron.ts:360 | refresh-active cron без per-order advisory lock |
| 5 | critical | lib/payments/tbank/service.ts:223 | Idempotency webhook нарушена при сбое applyPaymentStatus |
| 6 | major | lib/catalog/actions.ts:204 | TOCTOU в moveCategory (устаревшая проверка циклов) |
| 7 | minor | lib/storefront/dto.ts:299 | compareAtPrice precision в variant DTO (финдер: вероятно не баг) |
| 8 | minor | lib/catalog/actions.ts:706 | createVariant inventory init ON CONFLICT без RETURNING |
| 9 | minor | lib/storefront/dto.ts:295 | compare_at_price null-safety в variant DTO (финдер: not needed) |
| 10 | minor | lib/settings/schemas.ts:174 | robots_extra без валидации длины/формата |
| 11 | critical | lib/catalog/actions.ts:992 | IDOR attachMedia — variantId не проверяется на принадлежность productId |
| 12 | minor | lib/storage/image.ts:146 | Нет обработки ошибки генерации thumbnail в generatePreviews |
| 13 | major | lib/server/action.ts:278 | console.error отдаёт сырой объект ошибки (возможна утечка) |
| 14 | major | lib/audit/log.ts:75 | console.error в writeAudit отдаёт сырой объект ошибки |
| 15 | major | lib/orders/repository.ts:996 | gift compare_at_snapshot = unitPrice(0.00) вместо compareAt |
| 16 | major | app/admin/(panel)/page.tsx:44 | Дашборд читает orders без module-gate |
| 17 | minor | app/admin/(panel)/page.tsx:49 | getDashboardSeries без module-gate (финдер: возможно by-design) |

> Полные `description` / `repro` / `impact` / `proposedFix` по каждой находке — сохранены в
> session-журнале workflow (`.../subagents/workflows/wf_4ee37dcb-07e/journal.jsonl`) и были
> зафиксированы в чате сессии 2026-06-18. При необходимости перезапустить workflow (см. шаг 3).
