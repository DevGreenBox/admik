# 21 — Сводный трекер правок витрины THE CASE (Саша + Аня + Мадина)

> Источник: запрос Саши в чате (2026-06-29) + Google-док
> `docs/document/d/1axtyITLioeUB7Tb-OTEYMEK6VRrekCAEY__ji5cT1kc` (вкладки «Бриф»,
> «Правки от Мадины 1» (16 пп.), «Правки от Ани» (12 пп.), «Доступы»).
> Этот файл — единый дедуплицированный план; статусы сверяются по коду при реализации.

## ✅ СТАТУС (2026-07-20) — СНЯТЫ БЛОКЕРЫ РАЗДЕЛА C: РАЗМЕРНАЯ СЕТКА И ВЫБОР ЦВЕТА

Раздел **C** («ждёт контента владельца») висел блокером с 2026-06-22. Контент пришёл 2026-07-15, но при
реализации из него взяли **только имена вариантов** — сетку и выбор цвета не сделали, а задачу закрыли.
Владелец 2026-07-20 переспросил: «я давал таблицу размеров и говорил что нет функционала выбора цвета».
**Кодовая часть закрыта в тот же день** (подробности и уроки — `docs/00-журнал-проекта.md`, записи 2026-07-20).

- **Размерная сетка (Аня-9, «Правки от Ани 2» п.2) — ✅ СДЕЛАНО, `bec24e0`.** Сетки больше **не константа
  в компоненте витрины** (была `SIZE_TABLE` в `SizeGuide.tsx` с **выдуманными** данными), а **настройка
  магазина** `size_charts` в `shop_settings` с экраном редактирования в админке. Данные владельца из
  `razmernaya_setka.docx` перенесены **дословно**: российские размеры 42/44/46/48 (жен.) и 48/50/52/54 (муж.),
  точные значения, диапазон роста. Устранено противоречие на проде: кнопки размеров («44 / S») и модалка
  «Таблица размеров» теперь согласованы.
  **Уточнение владельца:** сеток **две на магазин** — женская и мужская; товар выбирает свою по полу.
  **Подтверждено владельцем:** обхват бёдер **90 см у 42/XS — верное значение**, не опечатка.
- **Выбор цвета (Мадина-15 часть, Аня-9, «Правки от Ани 2» п.1) — ✅ СДЕЛАНО, `ddda796` + `87fe88f`.**
  Раньше свотч был **неинтерактивным** `<span aria-label="Графит">` (несмотря на название коммита `b239f79`
  от 2026-07-16). Теперь цвет — **вариантный атрибут**: HEX в `attribute_values`, матрица цвет × размер
  в админке, цвет протащен через DTO → `StorefrontVariant` → корзину → снимок позиции заказа, свотчи
  на карточке кликабельны.
  **Уточнение владельца:** цвет — **вариант ВНУТРИ товара**, не отдельная карточка.
  `87fe88f` — фикс регрессии, пойманной **живым прогоном в браузере**: блок цвета исчезал у товаров
  со «старой» раскладкой (цвет заведён атрибутом товара, а не варианта).

**⛔ ОСТАЁТСЯ ЗА ВЛАДЕЛЬЦЕМ (контент, автономно не делается):** **6 фотографий из 8 сочетаний
«товар + цвет + пол»**. Обе модели выпускаются в **двух полах**, у каждой **два цвета** (ORDO — белый/dune,
ALTERA — белый/graphite), а фото есть только на **2** сочетаниях (ORDO белый, ALTERA графит). Заводить
цветовой вариант без снимка **нельзя** — покупатель увидит чужое фото при выборе цвета. Заготовка данных
готова: `db/seed/the-case-catalog-full.sql`. После получения фото — публикация черновиков.

---

## ✅ СТАТУС (2026-06-29, вечер) — ФАЗА 1 КОДА ЗАКРЫТА
Все 9 кодовых пунктов Фазы 1 сделаны (TDD → код-гейт → коммит), HEAD=`5197b9d`:
- **B1** лента редактируема/скрыта + **B3** Philosophy редактируем (layout 2 колонки) — `902e2b0`
- **B9** однопользовательский режим (меню+guard страниц+серверный блок+UI «Доступ») — `f854d26`
- **B2** ритм блоков + **B4** About (фото справа ~40%) + **B6** рус. главная + **B7** футер без «Service» — `5a7a7a0`
- **B8** второй ряд подкатегорий на `/catalog` + добивка **B6** (каталог/карточки) — `7f5e164`
- **B5** «Коллекция» → реальные featured-товары по табам жен/муж (фолбэк на ракурсы) — `5197b9d`

Гейт обе стороны: admik tsc0/lint0/vitest 1976; THE CASE tsc0/lint0/vitest 265.

**❗ИСПРАВЛЕНИЕ по пункту Саши (дословно, позапрошлая сессия 684be117):** «Заведи в каталог по 3 тестовых
товара муж/жен с фото обложки коллекции» — это НЕ контент-блокер (контент дан: фото обложки), а отдельная
задача. ОСТАЁТСЯ: сид 6 товаров (3 жен в категорию `*-zhen`, 3 муж в `*-muzh`, `is_featured=true`,
`status=active`) на СТЕНДЕ — выполняется на шаге деплоя; тогда «Коллекция» покажет их вместо фолбэк-ракурсов.

**ОСТАЛОСЬ:** (1) сид 6 тест-товаров (стенд); (2) **Фаза 2 — карточка товара B10–B12** (НЕ начата);
(3) Фаза 3 контент (C, за владельцем); (4) финальная сквозная проверка: security-review (B9=RBAC) + деплой +
живой playwright. Деплоя/живого прогона ещё НЕ было (директива «сначала ВСЕ правки, потом проверка»).

## ⚠️ Безопасность (СРОЧНО, owner-gated)
В гугл-доке (вкладка «Доступы») лежат **боевые пароли в открытом виде**: учётка СДЭК
(`pazovamadina@mail.ru`) и Timeweb (`cx309763`). Документ открыт по ссылке → утечка.
**Действие владельца:** сменить пароли (особенно Timeweb), убрать из дока. Секреты в репозиторий
НЕ вносятся — только из `.env` на сервере. Связано: [[the-case-brief-and-monorepo]].

---

## A. Уже сделано прошлыми волнами (СВЕРИТЬ, не переделывать)
- Hero кликабелен целиком → `/catalog`, кнопка «Смотреть коллекцию» (Аня-1, Мадина-3).
- «Качество ткани» внутри блока о бренде, без слайдера (Аня-2; `CoverSlides`).
- Коллекция: табы «Для женщин / Для мужчин», слово «Shop» убрано из секции (Аня-4, Мадина-6/7-часть).
- «Categories» → «Категории» (Аня-5).
- Philosophy-заголовок «Comforts + Medicine = THE CASE» (Мадина-10-часть).
- Убраны блоки «Medical Fashion», «Editorial», «Bestsellers» (Мадина-4/8/9; `page.tsx:19`).
- «Service» убран из секции «Доставка и оплата» (Аня-8) — НО в футере колонка «Service» осталась.
- Иконка человека → `/account` «Личный кабинет» (Аня-11).
- Страницы 200: `/returns /payment /care /privacy /terms /reviews /contacts /faq /delivery /about`
  (Мадина-14, Аня-10/12). Содержимое некоторых — заглушки (сверить).

## B. К реализации КОДОМ (контент владельца не нужен)
1. **Лента `ValuesStrip` «Форма / Функция / Дисциплина»** (Мадина-1 убрать ↔ Мадина-7 заменить слова).
   **РЕШЕНИЕ Саши 2026-06-29:** вынести текст ленты в настройки админки (`settings.home`) +
   переключатель показа. Сейчас **скрыть** (выполняет Мадина-1); слова Саша поменяет сам из админки
   и включит обратно (выполняет Мадина-7) — без правок кода. Единый паттерн с Philosophy (B3).
2. Сократить вертикальный промежуток между блоками (Саша-3, Аня-3).
3. **Philosophy**: убрать пустоту справа (переработать layout «уже линию»), **сделать редактируемым
   из админки** (вынести в `settings.home`, зеркало About) (Саша-7, Аня-6, Мадина-10).
4. **About**: фото меньше и справа от текста (Саша-8, Аня-7).
5. **Коллекция → реальные товары**: вместо статичных ракурсов Front/Side/Back показывать выбранные
   для главной товары каталога, по табам жен/муж (Саша-4/5). Механизм «показать на главной» = флаг
   товара (is_featured) + признак пола (категория/атрибут — решить при реализации).
6. **Всё на русском** (Саша-6): эйлброу `About`/`Philosophy`, ракурсы `Front/Side/Back`,
   заголовки футера `Shop/Service/Legal/Newsletter/Information`.
7. Футер: убрать колонку «Service» (Аня-8), слово «Shop»; вычистить мусорные категории
   `test 123` / `test 12333` (Саша-1-смежное).
8. **Подкатегории**: диагностировать и починить (Саша-1; инфраструктура дерева в коде есть —
   симптом ищу сам).
9. **Однопользовательский режим** через настройку/флаг (Саша-2): скрыть/заблокировать разделы
   «Пользователи»/«Роли» и `createUser` для этого магазина, сохранив универсальность платформы.
10. Карточка товара: главные фото меньше, помещаются на экран (Мадина-15-часть, Аня-7).
11. Карточка товара: секция «Состав и уход» (Мадина-15; текст состава/ухода есть в доке).
12. Карточка товара: баг — «Таблица размеров» опускается вниз и не видна (Мадина-16).

## C. Ждёт контента/решения владельца (⛔ блокеры)
- Мадина-2/11: фото эскизов + фактура ткани (серая) для hero-слайдов «О бренде» / «Качество ткани».
- ~~Мадина-15 «выбор цвета» + Аня-9: реальные товары Мадины (размерная сетка, цвета, фото каталога).~~
  → **КОД ЗАКРЫТ 2026-07-20** (`bec24e0` — сетки, `ddda796`+`87fe88f` — цвет; см. блок «СТАТУС (2026-07-20)»
  вверху файла). **Остаток блокера — только фото:** 6 снимков из 8 сочетаний «товар + цвет + пол».
- Мадина-13 / Аня-10: телеграм + телефон поддержки (пока заглушка — приемлемо).
- ~~Мадина-7: «слова для ленты заменю»~~ → **РЕШЕНО** (см. B1): лента редактируемая из админки,
  сейчас скрыта; Саша сам впишет новые слова и включит. Контент-блокер снят.
- Состав/уход (текст из дока): `72% полиэфир, 21% вискоза, 7% спандекс`; уход — стирка ≤30 °C
  наизнанку, без химчистки/отбеливания, глажка с изнанки ≤110 °C. Можно завести дефолтом.

## D. План фаз
- **Фаза 1 (код, автономно):** B1–B9 (главная: лента/промежутки/Philosophy/About/Коллекция/язык/
  футер) + подкатегории + однопольз. режим.
- **Фаза 2 (код карточки):** B10–B12.
- **Фаза 3 (контент):** C — по мере поступления материалов от владельца.
- Каждая правка: TDD → код-гейт → деплой на стенд → живой прогон playwright (две проверки).
  Сначала ВСЕ правки, потом сквозная проверка/тесты (директива Саши 2026-06-29).
