# 09 — Состояние проекта и гайд продолжения

> **Назначение этого файла.** Это единый «гайд продолжения» для новой сессии Claude
> после `/compact` (когда детальный контекст чата сжат) или при старте «с нуля».
> Самый важный артефакт восстановления: прочитав только его, можно понять, где проект,
> как проверить его здоровье, как устроен код и что делать дальше — без анализа
> скрытых сессий. Если что-то в репозитории расходится с этим файлом — источником
> правды считать `git`, `docs/00-журнал-проекта.md` и `docs/03-роадмап.md`, а этот файл обновить.

---

## 1. Где проект сейчас

**Admik** — headless commerce backend (Next.js App Router + TypeScript + PostgreSQL)
с публичным Storefront API. Один экземпляр обслуживает один магазин (мультитенантность
через конфиг, см. §5). Две витрины-потребителя: **THE CASE** (медицинская форма) и
**Gang Auto** (автозапчасти).

Снимок состояния:

| Параметр | Значение |
|---|---|
| Ветка | `foundation/etap-0-1` |
| Последний коммит | `85a0b39 feat(этап4): storefront-доставка, webhook, admin-UI, cron — Этап 4 завершён (E/F/G)` |
| Всего коммитов | 25 |
| Рабочее дерево | чистое (`git status --short` пуст) |
| Тесты | **689 passed / 60 skipped** |
| Build | **OK** |

Этапы:

| Этап | Содержание | Статус |
|---|---|---|
| 0 | Фундамент: стек, конфиг, миграции, тестовый каркас | ✅ done |
| 1 | Ядро + Auth + RBAC + audit_log + каркас админки | ✅ done |
| 2 | Каталог + витрины (брендинг/референсы) + Storefront API | ✅ done |
| 3 | Заказы + корзина/чекаут + промокоды | ✅ done |
| 4 | Интеграция СДЭК (полностью, пакеты A–G) | ✅ done |
| **5** | **CMS + промо + SEO + настройки магазина** | ⏳ **todo (следующий)** |
| 6 | DevOps: copy-paste деплой, бэкапы, мониторинг, обновления | ⏳ todo |

Последние коммиты (свежие сверху):

```
85a0b39 feat(этап4): storefront-доставка, webhook, admin-UI, cron — Этап 4 завершён (E/F/G)
1b68241 feat(этап4): СДЭК Order/Tracking/Webhook/Print сервисы (пакет D)
9878875 feat(этап4): СДЭК сервисы Calculator/PvzService/StatusMap (пакет C)
91e6138 feat(этап4): СДЭК клиент+OAuth+кеш токена+mock (пакет B)
0bfe6db feat(этап4): СДЭК фундамент — миграции 0017-0018, типы, конфиг, mock-детект (пакет A)
3e7c9b5 docs(этап4): спецификация СДЭК (docs/08, порт carre) + ADR-011
d3be665 docs: зафиксировать завершение Этапа 3 (Заказы) в журнале и роадмапе
0c6875b feat(этап3): UI заказов и промокодов (3.E) — Этап 3 завершён
```

---

## 2. Как проверить здоровье

Менеджер пакетов — **pnpm**. ⚠️ На свежем клоне сначала `pnpm install` —
`node_modules` не хранятся в git (есть только `pnpm-lock.yaml`), без установки
ни одна из команд ниже не запустится.

```bash
pnpm install     # обязательно на свежем клоне (node_modules не в репозитории)
pnpm typecheck   # tsc --noEmit — типы
pnpm lint        # eslint .
pnpm test        # vitest run — юнит/интеграционные
pnpm build       # next build
pnpm test:e2e    # playwright test (только при наличии браузера)
```

**Что считать нормой:**

- `pnpm test` → **689 passed, 60 skipped**. Цифра 60 skipped — это **ожидаемо и здорово**.
  Интеграционные и e2e тесты обёрнуты в `skipIf` и пропускаются, если в окружении нет
  `DATABASE_URL` / Redis / браузера. В dev-окружении PostgreSQL/Redis/Docker/браузера нет,
  поэтому они и должны быть skipped (гоняются в CI/проде). Падений (failed) быть не должно.
- `pnpm typecheck` → без ошибок.
- `pnpm lint` → без ошибок.
- `pnpm build` → успешно.

Если число passed упало или появились failed — это регрессия; разбираться до коммита.

---

## 3. Карта кода

Назначение ключевых каталогов.

### `lib/` — бизнес-логика и инфраструктура

| Каталог | Назначение |
|---|---|
| `lib/config` | `env.ts` (валидация переменных окружения), `modules.ts` (флаги модулей — какие фичи включены для текущего магазина) |
| `lib/db` | `client.ts` (клиент **postgres.js**), `migrate.ts` (раннер идемпотентных миграций) |
| `lib/auth` | аутентификация и доступ: `password` (argon2), `session`, `cookies`, `rbac`, `permissions`, `rate-limit`, `actions` |
| `lib/server` | `action.ts` — обёртка `defineAction` для серверных действий (см. §5) |
| `lib/audit` | `log.ts` — запись в журнал аудита (`audit_log`) |
| `lib/admin` | хелперы админки: `nav`, `format`, `order-format` |
| `lib/catalog` | каталог: `types`, `schemas`, `repository`, `actions`, `pricing`, `cache`, `slug`, `tree` (дерево категорий), `errors` |
| `lib/orders` | заказы: `types`, `status`, `schemas`, `money` (копейки), `pricing`, `promo` (промокоды), `repository`, `actions`, `errors`, `delivery-cost` |
| `lib/cdek` | СДЭК: `config`, `client`, `token-cache` (OAuth), `manager`, `mock`, `repository`, `errors`; `services/{calculator,pvz,status-map,order,tracking,webhook,print,delivery-status}`; `actions`, `cron` |
| `lib/storefront` | публичный Storefront API: `auth`, `cors`, `response`, `env`, `dto`, `order-dto`, `queries` |
| `lib/storage` | хранилище изображений: `local`/`s3`, `image` (sharp), `validate`, `types` |

### `app/` — маршруты Next.js

| Каталог | Назначение |
|---|---|
| `app/admin/(panel)/*` | админка: каркас + `catalog`, `orders`, `promo`, `roles`, `users`, `audit` + СДЭК-блок; `app/admin/login` — вход |
| `app/api/storefront/v1/*` | публичный Storefront API: `brands`, `cart`, `categories`, `delivery`, `orders`, `products` |
| `app/api/cdek/webhook` | приём webhook СДЭК (идемпотентный) |
| `app/api/cron/cdek` | cron-задачи СДЭК (синхронизация статусов) |
| `app/api/health` | health-check эндпоинт |

### `db/` — база данных

- `db/migrations/0001…0018` — идемпотентные SQL-миграции:
  `0001` extensions+таблица миграций · `0002` auth · `0003` rbac · `0004` audit ·
  `0005`–`0011` каталог (категории, товары, варианты, атрибуты, медиа, инвентарь, цены/флаги/бренды) ·
  `0012` orders · `0013` customers · `0014` promo_codes · `0015` promo_redemptions ·
  `0016` order_number_counter · `0017` cdek_shipments · `0018` product_weight_dims.
- `db/seed/*` — сидовые данные.

---

## 4. Индекс документации (`docs/`)

| Файл | Содержание |
|---|---|
| `00-журнал-проекта.md` | Журнал/история проекта; восстановление контекста без анализа скрытых сессий (ведёт History Manager, новые записи **сверху**) |
| `01-архитектурные-решения.md` | Реестр **ADR-001…011** (см. ниже); каждое решение с обоснованием и последствиями |
| `02-модель-развёртывания.md` | Модель развёртывания и переноса на новый магазин по принципу «копировать-вставить» (БД разворачивают другие люди) |
| `03-роадмап.md` | Единый источник правды по статусу: пройденные/текущие/будущие этапы и задачи |
| `04-этап1-ядро-auth-rbac.md` | Проектный документ Этапа 1: ядро, Auth, RBAC, каркас админки, audit_log |
| `05-этап2-каталог.md` | Проектный документ Этапа 2: модель каталога товаров |
| `06-референсные-магазины.md` | Этап 2.x: референсные магазины (THE CASE, Gang Auto) + расширение каталога + gap-анализ |
| `07-этап3-заказы.md` | Полная спецификация Этапа 3: заказы, корзина/чекаут, модуль orders |
| `08-этап4-cdek.md` | Проектный документ Этапа 4: интеграция СДЭК, порт логики из carre (PHP/Yii2) на TS/Next.js |
| `09-инструкция-разворачивания.md` | *(зарезервировано, ещё не создан)* — пошаговая инструкция развёртывания для не-разработчика; будет создана в Этапе 6.5 (анонс в `docs/02`, п. 7) |
| **`10-состояние-и-продолжение.md`** | **Этот файл** — гайд продолжения для новой сессии после `/compact` |

Реестр ADR (в `docs/01`):

```
ADR-001 — Технологический стек
ADR-002 — Источники паттернов (что берём из carre)
ADR-003 — Модель мультитенантности
ADR-004 — «Сначала тесты, потом код»
ADR-005 — Модель прав доступа (RBAC): гибкие права, не enum-роли
ADR-006 — Аутентификация, хеширование паролей и роли БД
ADR-007 — Модель каталога: характеристики (гибридная EAV) и связь товар↔категория (M2M)
ADR-008 — Admik как headless commerce backend + публичный Storefront API
ADR-009 — Модель ценообразования, флагов товара и брендов каталога
ADR-010 — Заказы: серверный расчёт корзины (anti-tamper) и снимки позиций
ADR-011 — СДЭК: слоистый порт carre + mock-режим + идемпотентный webhook
```

---

## 5. Ключевые архитектурные принципы

- **1 магазин = 1 БД.** Мультитенантность реализуется конфигом (отдельный экземпляр на магазин),
  а не разделением данных внутри одной БД (ADR-003).
- **Флаги модулей.** Что включено для конкретного магазина — задаётся в `lib/config/modules`.
  Фичи проверяют флаг, а не хардкодят наличие.
- **`defineAction` (`lib/server/action.ts`).** Единый конвейер серверного действия:
  **guard (проверка прав) → Zod-валидация → работа с БД → revalidate → запись в audit**.
  Все мутации идут через него.
- **RBAC по правам, не по enum-ролям.** Доступ проверяется по гранулярным правам (ADR-005),
  роли — это наборы прав.
- **Headless backend + Storefront API.** Витрины (THE CASE, Gang Auto) — отдельные потребители
  публичного API `/api/storefront/v1` (ADR-008).
- **Anti-tamper: цены считает сервер.** Клиент не присылает итоговые суммы; сервер пересчитывает
  корзину и сохраняет снимки позиций (ADR-010).
- **Деньги в копейках.** Никаких float; целые числа (модуль `lib/orders/money`).
- **Mock-режим всех внешних интеграций.** СДЭК и платёжки умеют работать в mock-режиме
  (детект по отсутствию реальных ключей), чтобы dev/CI не зависели от внешних сервисов (ADR-011).
- **Идемпотентные миграции.** Каждая SQL-миграция безопасна к повторному запуску.
- **Идемпотентность СДЭК-webhook.** Повторная доставка одного события не ломает состояние (ADR-011).

---

## 6. КРИТИЧНО про секреты

**Реальных ключей СДЭК и платёжных систем в репозитории НЕТ и быть НЕ должно.**
Эти ключи принадлежат третьим лицам (владельцам магазинов). В репо допустимы только:

- плейсхолдеры в `.env.example`;
- mock-режим интеграций (работает без реальных ключей).

Никогда не коммить реальные креды, токены, секреты СДЭК/платёжек. Если они где-то всплыли —
это инцидент: убрать из истории и ротировать на стороне владельца.

---

## 7. Что делать дальше

**Следующий шаг — Этап 5: CMS + промо + SEO + настройки магазина.**

Рекомендованный порядок:

1. **Сначала проектный документ** — `docs/10-этап5-cms-промо-seo-настройки.md`
   (составляет архитектор): объём, модель данных, миграции, API/админка, влияние на витрины,
   при необходимости новый ADR. Без него к коду не переходить.
2. **Реализация по пакетам**, в духе предыдущих этапов, по принципу **«сначала тесты, потом код»** (ADR-004):
   миграции и типы → repository/схемы → actions/сервисы → Storefront API → админ-UI.
3. **Коммит после каждой волны** (crash-recovery): каждый завершённый пакет — отдельный коммит
   на ветке `foundation/etap-0-1`. Это страхует от потери прогресса при сжатии контекста/сбое.
4. **Обновлять документацию** по ходу: `docs/00-журнал-проекта.md` (новая запись сверху) и
   `docs/03-роадмап.md` (статус этапа/задач). Завершив этап — зафиксировать это отдельным `docs(...)`-коммитом.
5. После Этапа 5 — **Этап 6 (DevOps)**: copy-paste деплой, бэкапы, мониторинг, обновления.

Перед любой работой прогнать §2 (health-check), чтобы стартовать с зелёного состояния.

---

## 8. Как ведётся работа

- **Оркестрация через агентов Claude со встроенной ролью.** Роли (Solution Architect,
  Documentation Manager, History Manager, инженеры) задаются в промпте/инструкциях агента.
  Кастомные типы агентов из `.claude/agents` в текущем харнессе **не зарегистрированы** —
  полагаться на них нельзя; роль передаётся явно при запуске.
- **Работа волнами.** Этап дробится на пакеты (волны); каждая волна — самодостаточный кусок
  с тестами.
- **Коммит после каждой волны** на ветке `foundation/etap-0-1`. Сообщения коммитов — на русском,
  в формате `feat(этапN): …` / `docs(этапN): …`, как в истории выше.
