# 09 — Состояние проекта и гайд продолжения

> **Назначение этого файла.** Это единый «гайд продолжения» для новой сессии Claude
> после `/compact` (когда детальный контекст чата сжат) или при старте «с нуля».
> Самый важный артефакт восстановления: прочитав только его, можно понять, где проект,
> как проверить его здоровье, как устроен код и что делать дальше — без анализа
> скрытых сессий. Если что-то в репозитории расходится с этим файлом — источником
> правды считать `git`, `docs/00-журнал-проекта.md` и `docs/03-роадмап.md`, а этот файл обновить.

---

## 1. Где проект сейчас

**Admik** — headless commerce backend (Next.js App Router + TypeScript + PostgreSQL)
с публичным Storefront API. Один экземпляр обслуживает один магазин (мультитенантность
через конфиг, см. §5). Две витрины-потребителя: **THE CASE** (медицинская форма) и
**Gang Auto** (автозапчасти).

Снимок состояния:

| Параметр | Значение |
|---|---|
| Ветка | `foundation/etap-0-1` (запушена в `origin`, CI идёт) |
| Последний коммит | **волна 4 адверсариального аудита завершена — 6 фиксов** (HEAD `ad1537b`): `0acff5e` fix(orders/cdek) — корректность доставки (2 CRITICAL undercharge + 1 minor консистентность); `1edb17b` fix(security) — storefront rate-limit (major) + tbank IP-whitelist (minor); `ad1537b` fix(catalog) — URL логотипа бренда (major). Поверх волны 3 (`870c766`) |
| Рабочее дерево | чистое (`git status --short` пуст); ветка НЕ запушена (локальные коммиты поверх origin, HEAD `ad1537b`); пуш за владельцем — нет GitHub-креда в среде |
| Тесты | **1426 passed / 87 skipped** (база), часть с БД — на CI/стенде |
| Build | **OK** (в dev не гоняется — OOM; прогон на CI) |
| **Боевой стенд** | **✅ РАЗВЁРНУТ** — единый VPS Timeweb (Ubuntu 24.04, 2 ядра / ~4 ГБ / 59 ГБ, IPv4 `130.49.129.248`), `docker compose`, swap +2 ГБ |
| **Витрина** | `https://erfgq.website` (+`www`→редирект) — TLS Let's Encrypt, `200` |
| **Админка/API/медиа** | `https://admin.erfgq.website` — TLS Let's Encrypt; `admin/login` `200`, Storefront API `200`, deep-health `ok` (db/redis/s3) |
| **БД/медиа** | `init-shop`: **28 миграций** (вкл. `0027` Т-Банк; `0028` storefront_pageviews — ✅ накатана на стенд 2026-06-17), права/роли, владелец `akekbol@gmail.com`; бакет MinIO `admik-media` (публичное чтение) |
| **Дашборд-графики** | ✅ задеплоены 2026-06-17 — заказы (есть данные) + посещения (beacon проверен: `{ok:true}`/401; пуст, пока витрина не шлёт `POST …/events/pageview`) |
| **Раздел «Доставка»** | ✅ задеплоен 2026-06-17 — `/admin/cdek` отдаёт страницу (раньше 404); образ `app` пересобран, deep-health `ok` |
| **СДЭК** | на стенде заданы `CDEK_ACCOUNT/SECRET` + `CDEK_BASE_URL`/`CDEK_TEST_MODE` (тестовый контур edu) |

> # ⭐⭐⭐⭐⭐⭐ НОВАЯ СЕССИЯ (2026-06-18) — волна 4 ЗАВЕРШЕНА
>
> **Волна 4 адверсариального аудита (деньги / публичный API / auth + регресс) — ✅ ВЕРИФИЦИРОВАНА
> И ЗАКРЫТА.** Находки прошли мульти-агентную верификацию (verify → второй скептик-рефьютер) +
> ручную сверку кода. Итог: **подтверждено и исправлено — 6, отклонено — 4** (1 overturned 2-м
> скептиком, 3 refuted). Гейт после фиксов: typecheck 0, lint 0, **vitest 1426 passed / 87 skipped**.
> Коммиты поверх волны 3 (HEAD был `870c766`), HEAD теперь `ad1537b`:
>
> **Подтверждено и исправлено (6):**
> - **CRITICAL ×2** (`0acff5e`, fix(orders/cdek)) — anti-undercharge доставки: два пути, где стоимость
>   доставки занижалась/терялась → недосбор денег с заказа;
> - **minor** (`0acff5e`, тот же коммит) — консистентность расчёта доставки (orders/cdek);
> - **major** (`1edb17b`, fix(security)) — storefront rate-limit (публичный API без должного троттлинга);
> - **minor** (`1edb17b`, тот же коммит) — tbank IP-whitelist (ужесточение приёма webhook);
> - **major** (`ad1537b`, fix(catalog)) — некорректный URL логотипа бренда.
>
> **Отклонено (4):**
> - **overturned** — 1 находка подтверждена первым verify, но **опровергнута вторым скептиком-рефьютером**
>   (ложная тревога при углублённой сверке);
> - **refuted ×3** — 3 находки опровергнуты как by-design / уже закрыто / мисрид финдера.
>
> **Noted-minor (НЕ баги — опровергнуты; возможная будущая косметическая полировка):**
> - (a) `updatePromoCode` не транслирует `check_violation` БД в аккуратную доменную ошибку → наружу
>   уходит raw 500 (поведение корректное по сути, лишь некрасивое сообщение);
> - (b) before-снимок аудита в `updateRole` не включает прежний набор прав роли (журналируется новое
>   состояние; diff прав можно обогатить в будущем).
>
> **Активный действенный бэклог снова пуст.** Роадмап 0–6 закрыт. Дальнейшие нити — прежние owner-gated
> (см. §7): живой прогон Т-Банк по тест-картам, ключи СДЭК, наполнение каталога, безопасность. Автономное
> хардненинг ведётся **волнами адверсариального поиска** (волны 1–4 суммарно нашли и закрыли десятки
> реальных багов, включая критичные денежные). Ветка НЕ запушена (нет GitHub-креда — пуш за владельцем).
>
> ---
>
> # ⭐⭐⭐⭐⭐ ПРЕДЫДУЩАЯ СЕССИЯ (2026-06-18, ultracode) — волна 3 ЗАВЕРШЕНА (исторический след)
>
> **Волна 3 поиска багов — ✅ ВЕРИФИЦИРОВАНА И ЗАКРЫТА (2026-06-18).** Все 17 находок финдеров
> прошли мульти-агентную верификацию (verify → второй скептик-рефьютер) + ручную сверку кода.
> Итог: **подтверждено и исправлено — 3, опровергнуто — 14** (by-design / уже закрыто / мисрид финдера):
> - **#5** (critical, `0e8e1ed`) — атомарность webhook Т-Банк: при сбое после `insertPaymentLog` повтор
>   видел дубль и НЕ применял статус → платёж навсегда `pending` (потеря денег);
> - **#6** (major, `5589957`) — TOCTOU в `moveCategory` (устаревшая проверка циклов);
> - **#16** (minor, `870c766`, HEAD) — дашборд читал `orders` без module-gate `isModuleEnabled('orders')`
>   (поглощает #17).
>
> Гейт после фиксов: typecheck 0, lint 0, **vitest 1405 passed / 84 skipped**. Полный разбор всех 17
> находок с финальными вердиктами и причинами опроверждений — **`docs/17`** (статус ✅ ЗАВЕРШЕНО) и
> `docs/00` (Завершённые задачи, 2026-06-18). Ветка НЕ запушена (нет GitHub-креда — пуш за владельцем).
>
> **Активный действенный бэклог снова пуст.** Следующие нити — прежние (см. §7): живой прогон Т-Банк по
> тест-картам, ключи СДЭК (тестовый контур edu / боевые), наполнение каталога, безопасность.
>
> ---
>
> # ⭐⭐⭐⭐ ПРЕДЫДУЩАЯ СЕССИЯ (2026-06-18) — третья серия фиксов (исторический след)
>
> **Что сделано:** восстановлен контекст; незакоммиченная **третья серия** прерванной сессии (4 правки)
> доведена до зелёного и зафиксирована **4 коммитами** (`4758220`, `a33c8ed`, `096c9ed`, `29ac6ba`, HEAD
> `096c9ed`): валидация client IP перед `inet` (+ закрыта пропущенная точка в storefront POST /orders);
> **anti-undercharge доставки** (была «красной» — дописано тело `computeDeliveryCost`: createOrder бросает,
> quote softFail); confirm удаления медиа/варианта + ресинк секций CMS; единый 404 в `tbank/init`
> (anti-enumeration). Гейт: typecheck 0, lint 0, **vitest 1396 passed / 84 skipped**. Разбор — `docs/00`
> (Завершённые задачи, 2026-06-18). Поверх неё — волна 3 (3 фикса, см. блок выше).
>
> ---
>
> # ⭐⭐⭐ НОВАЯ СЕССИЯ (2026-06-17, после падения сервера) — ревизия + поиск багов
>
> **Что сделано:** восстановлен контекст; незакоммиченная адверсариальная ревизия прерванной сессии
> («ручная проверка») доведена до зелёного (доделаны 2 TDD-пробы: `slugifyOrFallback`, `computeInStock`
> учитывает `reserved`) и зафиксирована **8 логическими коммитами** (HEAD `07f6ae6`). Гейт: typecheck 0,
> lint 0, vitest **1244 passed / 81 skipped**. Полный разбор — `docs/00` (Завершённые задачи, 2026-06-17,
> верхняя запись). **⚠️ Деплой-чувствительно:** коммит `a626bd7` переводит рантайм на роль БД `admik_app`
> (least-privilege) — при следующем деплое проверить достаточность грантов на стенде.
>
> **Поиск новых багов — ЗАВЕРШЁН.** Мульти-агентный адверсариальный ревью (8 доменов) дал **20 находок,
> 16 подтверждено** (4 опровергнуто как by-design/уже закрыто). Все 16 исправлены фикс-агентами по TDD
> и зафиксированы 8 коммитами (`0a513b3`..`23e81fe`, HEAD `23e81fe`). Гейт: typecheck 0, lint 0, vitest
> **1302 passed / 84 skipped**. В т.ч. **CRITICAL** #1 (частичное обновление промокода обнуляло скидку) и
> security: эскалация привилегий (роли↔roles.manage), утечка deep-health. Полный разбор — `docs/00`
> (Завершённые задачи, 2026-06-17, верхняя запись).
>
> **Далее:** ветку при готовности запушить в `origin` (CI; gh локально не залогинен — статус на GitHub).
> ⚠️ Деплой-чувствительно остаётся least-privilege роль БД `admik_app` (коммит `a626bd7`) — проверить
> гранты на стенде. Затем — нить «живая проверка админки» ниже.

> # ⭐⭐ ПРОДОЛЖИТЬ ОТСЮДА (новая сессия, 2026-06-17) — живая проверка админки
>
> **Что уже сделано (этой сессией):** закрыт «Новый этап правок» Prevki.md (5 багов) + найден/исправлен
> per-product SEO витрины. Всё **задеплоено** на стенд (`app` + `storefront` пересобраны, deep-health `ok`).
> Storefront-сторона подтверждена ВЖИВУЮ: «Халат» (товар без размеров, остаток на уровне товара) теперь
> показывает «В корзину» (был «Нет в наличии»), `cart/quote` считает корректно, per-product `<title>` у каждого
> товара свой. Коммиты `8fe5aeb`→ (docs) запушены в `origin`. Тесты: Admik **1178 passed**, THE CASE **73**.
>
> **ЧТО ОСТАЛОСЬ — интерактивная проверка админки через браузер** (просьба владельца «прокликать админку»).
> Раньше блокировалась гардом прод-окружения (чтение `OWNER_PASSWORD`/мутации прод-БД). Владелец включает
> **`bypassPermissions`** (`.claude/settings.local.json` → `{"permissions":{"defaultMode":"bypassPermissions"}}`)
> и перезапускает сессию — тогда гарда нет.
>
> **КАК ПРОДОЛЖИТЬ (по шагам):**
> 1. Взять учётку владельца в env (в память, НЕ печатать):
>    `export OWNER_EMAIL=$(ssh -i ~/.ssh/admik_deploy root@admin.erfgq.website 'grep -E "^OWNER_EMAIL=" /opt/admik/.env | cut -d= -f2-')`
>    `export OWNER_PASSWORD=$(ssh -i ~/.ssh/admik_deploy root@admin.erfgq.website 'grep -E "^OWNER_PASSWORD=" /opt/admik/.env | cut -d= -f2-')`
> 2. Прогнать сценарий: `PLAYWRIGHT_BROWSERS_PATH=/root/.cache/ms-playwright node scripts/verify-admin.mjs full`
>    (фаза `login` и `catalog` уже реализованы; фазу `full` — чек-лист 2–9 в шапке скрипта — допилить, прокликивая
>    UI; селекторы форм — в `app/admin/(panel)/catalog/_components/ProductForm.tsx` и promo-форме).
> 3. Чек-лист «полной проверки» (Prevki «Как сделать проверку»): создать товары разных вариаций (с вариантом/без,
>    с SEO/без, со скидкой), проверить отображение в админке и на витрине, завести промокоды на разных условиях
>    (percent/fixed+min_order/free_delivery) и проверить их в `cart/quote`, оформить тест-заказ и увидеть его в
>    `/admin/orders`, проверить кнопку «Удалить навсегда» (#1), а также загрузку фото (#2 — вкладка «Медиа» после
>    создания товара, файл >1 МБ должен теперь грузиться).
> 4. **ОБЯЗАТЕЛЬНО убрать тест-данные** после проверки (товары/промокоды с префиксом `ZZ-QA-`, тест-заказ).
> 5. Записать результат в `docs/00` (Завершённые задачи) и обновить этот блок.
>
> **Деплой витрины при необходимости** (если правил THE CASE): синхронизировать `src/` по SSH + пересобрать
> `storefront` — см. память `admik-stand-deploy`.

> **🔭 ГДЕ ПРОДОЛЖИТЬ ЗАВТРА (активные нити).** Роадмап 0–6 закрыт, **весь действенный бэклог закрыт**.
> **БОЕВОЙ СТЕНД РАЗВЁРНУТ** (✅ 2026-06-16) — Admik + витрина THE CASE на одном VPS под доменом `erfgq.website`,
> доступны по https (витрина `erfgq.website`, админка/API/медиа `admin.erfgq.website`); реальные TLS, deep-health
> `ok`. Код на сервере: `/opt/admik` + `/opt/admik/THE CASE`; `.env` с секретами — только на сервере, доступ по
> SSH-ключу. Дальше — наполнение и подстановка боевых ключей:
> - **Касса Т-Банк** — ✅ **волны 1–2 реализованы** (`lib/payments/tbank/*` + роуты + миграция `0027`, ADR-017,
>   `docs/15`): mock-first + боевой клиент + БД-аудит, зелёный typecheck/lint/тесты. **Осталось:** живой прогон по
>   тест-картам на ТЕСТОВОМ терминале Т-Бизнес (`rest-api-test.tinkoff.ru`) — нужны `TBANK_TERMINAL_KEY`/
>   `TBANK_PASSWORD` из ЛК (только в `.env` стенда), затем боевой запуск (волна 4).
> - **Ключи СДЭК** — подставить (тестовый контур edu или боевые) в `.env` сервера для реальных расчётов и
>   автонакладных; пока СДЭК в mock. Подробности — **§7, п. 0**.
> - **Наполнение каталога** — владелец заводит товары/категории/варианты из админки (`docs/14`).
> - **Безопасность** — сменить `OWNER_PASSWORD` и root-пароль сервера, **ротировать засветившиеся боевые ключи
>   СДЭК** (см. §6).
> - **Витрина THE CASE сделана** (волны A–D; `docs/13`) и подключена к Admik. По аналогии далее — **Gang Auto**.
>   Подробности — **§7 «Что делать дальше», п. 0a**.

> **Пост-роадмап (2026-06-16) — сводка по коммитам (поверх роадмапа, ветка `foundation/etap-0-1`).**
> Роадмап 0–6 остаётся закрытым; ниже — что добавлено после него (свежие снизу):
> - `a2500af` **деплой-фикс рантайм-образа** — `init-shop.sh` исполним ВНУТРИ контейнера `app`
>   (`bash`+`postgresql-client` + явный `COPY` `scripts/`+`db/`, не входят в standalone-трассировку Next.js).
> - `b23dfa6` **CI `docker-smoke`** (`.github/workflows/ci.yml` + оверлей `docker-compose.ci.yml`):
>   `docker build` → стек → `make init` → `make smoke` (бэклог **e**). Попутно устранены 2 бага деплоя:
>   (A) `init-shop` требовал ФАЙЛ `.env`, которого нет в контейнере → сделан опциональным с фолбэком на env;
>   (B) `. .env` ломался на пробелах/`*` в значениях (`SHOP_NAME`, `BACKUP_CRON`) → безопасный `load_env_file`.
> - `b638fa4` **(b)** скраб URL-кредов в значениях логов (`scrubSecretsInString`) + **(d)** nit `backup.sh`.
> - `c0f55cd` **(N1)** атомарный `per_customer_limit` в `createOrder` — закрыта гонка (re-count под блокировкой
>   строки `promo_codes`).
> - `2bdd581` **gift-позиция** (ADR-016): выдача товара-подарка `gift_*` отдельной строкой `order_items.is_gift`
>   (миграция `0025`), цена 0, вне итога, best-effort резерв. `b353cd7` — бейдж «Подарок» в админ-карточке.
> - `49e01ad` **(a)** честный откат `update.sh` через образ-снимок без пересборки (`image:` в compose +
>   `snapshot_current_image`/retag).
> - `dec7318` синхронизация роадмапа/журнала. `2280420` **СДЭК тестовый контур** как пресет в `.env.example`
>   + регресс-тест.
> - **(не закоммичено)** **сращивание витрины THE CASE** с Admik (волны A–D; `docs/13`): витрина — чистый
>   потребитель Storefront API, свой бэкенд выведен из обращения; на стороне Admik аддитивно добавлены
>   поиск городов СДЭК (`CityService` + роут `…/delivery/cdek/cities`) и параметр `GET /products?category=<slug>`.
>   Код THE CASE и эти артефакты Admik **пока вне git** — версионирование витрины за пользователем.
>
> Здоровье: typecheck/lint/shellcheck/YAML зелёные, **vitest 1026 passed / 80 skipped**. Docker/БД-части
> (CI `docker-smoke`, интеграционные кейсы заказов) валидируются на CI/стенде (в dev docker/БД нет).
> Полные записи (с эмпирическим воспроизведением багов A/B) — в `docs/00` (Завершённые задачи, 2026-06-16).

Этапы:

| Этап | Содержание | Статус |
|---|---|---|
| 0 | Фундамент: стек, конфиг, миграции, тестовый каркас | ✅ done |
| 1 | Ядро + Auth + RBAC + audit_log + каркас админки | ✅ done |
| 2 | Каталог + витрины (брендинг/референсы) + Storefront API | ✅ done |
| 3 | Заказы + корзина/чекаут + промокоды | ✅ done |
| 4 | Интеграция СДЭК (полностью, пакеты A–G) | ✅ done |
| 5 | CMS + промо + SEO + настройки магазина | ✅ done (5.D-1/5.D-2 настройки, 5.S-1 SEO, 5.C-1/5.C-2/5.C-3 CMS, 5.P-1+5.P-2 промо N×M; QA-вердикт «релиз можно») |
| 6 | DevOps: copy-paste деплой, бэкапы, мониторинг, обновления | ✅ done (6.1–6.5: smoke/deploy/Makefile, бэкапы+restore-drill, JSON-логи+deep-health, аддит. миграции+health-gate+откат, `docs/09`+ADR-015; QA-вердикт «релиз можно») |

> **Роадмап (Этапы 0–6) завершён полностью.** Все этапы ✅ done. Дальнейшее — вне роадмапа (см. §7).

Последние коммиты (свежие сверху; пост-роадмап выше `e814fec`):

```
2280420 docs(cdek): тестовый контур СДЭК (sandbox edu) как первоклассный пресет конфигурации
dec7318 docs: статус пост-роадмап-бэклога (действенный закрыт; остаток — by-design/extension)
b353cd7 feat(admin): бейдж «Подарок» для gift-позиции в карточке заказа
49e01ad feat(devops): честный откат update.sh через образ-снимок (бэклог a)
2bdd581 feat(orders): выдача товара-подарка gift_* отдельной позицией (ADR-016)
c0f55cd fix(orders): атомарный per_customer_limit в createOrder — закрыта гонка N1
b638fa4 fix(этап6-бэклог): скраб URL-кредов в значениях логов (b) + комментарий backup.sh (d)
b23dfa6 feat(ci): docker-build smoke + починка .env-handling init-shop/smoke
a2500af fix(deploy): рантайм-образ под init-shop внутри контейнера app
e814fec docs(этап6): завершение Этапа 6 и всего роадмапа (0–6) — роадмап закрыт
5de28e2 feat(этап5): промо N×M интеграция — quote/order + Admin UI + /promotions (5.P-2)
2df324d docs(этап5): проектный документ docs/11 + ADR-012/013/014
```

---

## 2. Как проверить здоровье

Менеджер пакетов — **pnpm**. ⚠️ На свежем клоне сначала `pnpm install` —
`node_modules` не хранятся в git (есть только `pnpm-lock.yaml`), без установки
ни одна из команд ниже не запустится.

```bash
pnpm install     # обязательно на свежем клоне (node_modules не в репозитории)
pnpm typecheck   # tsc --noEmit — типы
pnpm lint        # eslint .
pnpm test        # vitest run — юнит/интеграционные
pnpm build       # next build
pnpm test:e2e    # playwright test (только при наличии браузера)
```

**Что считать нормой:**

- `pnpm test` → **1026 passed, 80 skipped** (база Этапа 6 — 1013/76; пост-роадмап: +9 санитайзер, +4 подарок;
  +4 skip — интеграционные кейсы per_customer_limit и выдачи подарка, гоняются в CI с БД). Цифра skipped —
  это **ожидаемо и здорово**.
  Интеграционные и e2e тесты обёрнуты в `skipIf` и пропускаются, если в окружении нет
  `DATABASE_URL` / Redis / браузера. В dev-окружении PostgreSQL/Redis/Docker/браузера нет,
  поэтому они и должны быть skipped (гоняются в CI/проде). Падений (failed) быть не должно.
- `pnpm typecheck` → без ошибок.
- `pnpm lint` → без ошибок.
- `pnpm build` → успешно.

Если число passed упало или появились failed — это регрессия; разбираться до коммита.

---

## 3. Карта кода

Назначение ключевых каталогов.

### `lib/` — бизнес-логика и инфраструктура

| Каталог | Назначение |
|---|---|
| `lib/config` | `env.ts` (валидация переменных окружения), `modules.ts` (флаги модулей — какие фичи включены для текущего магазина) |
| `lib/` (корень) | `logger.ts` (структурированные JSON-логи, Этап 6), `health.ts` (health-проверки + deep-режим — БД/Redis/S3, Этап 6) |
| `lib/db` | `client.ts` (клиент **postgres.js**), `migrate.ts` (раннер идемпотентных миграций) |
| `lib/auth` | аутентификация и доступ: `password` (argon2), `session`, `cookies`, `rbac`, `permissions`, `rate-limit`, `actions` |
| `lib/server` | `action.ts` — обёртка `defineAction` для серверных действий (см. §5) |
| `lib/audit` | `log.ts` — запись в журнал аудита (`audit_log`); `sanitize.ts` — санитайзер чувствительных полей (вынесен на Этапе 6, переиспользуется логгером) |
| `lib/admin` | хелперы админки: `nav`, `format`, `order-format` |
| `lib/catalog` | каталог: `types`, `schemas`, `repository`, `actions`, `pricing`, `cache`, `slug`, `tree` (дерево категорий), `errors` |
| `lib/orders` | заказы: `types`, `status`, `schemas`, `money` (копейки), `pricing` (+`giftQuoteLine`, ADR-016), `promo` (промокоды), `repository` (+`resolveGiftLine`, атомарный `per_customer_limit`), `actions`, `errors`, `delivery-cost` |
| `lib/cdek` | СДЭК: `config`, `client`, `token-cache` (OAuth), `manager`, `mock`, `repository`, `errors`; `services/{calculator,pvz,status-map,order,tracking,webhook,print,delivery-status}`; `actions`, `cron` |
| `lib/storefront` | публичный Storefront API: `auth`, `cors`, `response`, `env`, `dto`, `order-dto`, `queries` |
| `lib/storage` | хранилище изображений: `local`/`s3`, `image` (sharp), `validate`, `types` |
| `lib/payments/tbank` | онлайн-эквайринг Т-Банк (Этап 7, ADR-017, `docs/15`): `config`, `types`, `errors`, `token` (sign/verify), `status-map`, `manager`, `mock`, `service`, `repository`, `receipt`, `client`; mock-first, идемпотентный webhook |
| `lib/analytics` | самохостовая аналитика дашборда (Prevki.md): `repository` — `recordPageview` (UPSERT суточного счётчика `storefront_pageviews`, миграция `0028`), чистый `buildDailySeries` + устойчивый `getDashboardSeries` (ряды для графиков заказов/посещений за 14 дней) |

### `app/` — маршруты Next.js

| Каталог | Назначение |
|---|---|
| `app/admin/(panel)/*` | админка: каркас + `catalog`, `orders`, `promo`, `cdek` (раздел «Доставка» — сводка отправлений), `roles`, `users`, `audit`, `settings`, `cms`; СДЭК per-order в `orders/[id]` (`CdekBlock`); `app/admin/login` — вход |
| `app/api/storefront/v1/*` | публичный Storefront API: `brands`, `cart`, `categories`, `delivery`, `orders`, `products` |
| `app/api/storefront/v1/payments/tbank/init` | витрина: инициация онлайн-оплаты Т-Банк (module-gate `payments`, anti-tamper, Этап 7) |
| `app/api/payments/tbank/webhook` | приём webhook Т-Банк (server-to-server, проверка `Token`, идемпотентный, Этап 7) |
| `app/api/cdek/webhook` | приём webhook СДЭК (идемпотентный) |
| `app/api/cron/cdek` | cron-задачи СДЭК (синхронизация статусов) |
| `app/api/health` | health-check эндпоинт (+ **deep-режим** `?deep=1` — проверка БД/Redis/S3, Этап 6) |

### `db/` — база данных

- `db/migrations/0001…0024` — идемпотентные SQL-миграции:
  `0001` extensions+таблица миграций · `0002` auth · `0003` rbac · `0004` audit ·
  `0005`–`0011` каталог (категории, товары, варианты, атрибуты, медиа, инвентарь, цены/флаги/бренды) ·
  `0012` orders · `0013` customers · `0014` promo_codes · `0015` promo_redemptions ·
  `0016` order_number_counter · `0017` cdek_shipments · `0018` product_weight_dims ·
  `0019` shop_settings · `0020` shop_settings_seed · `0021` seo_entity_fields ·
  `0022` cms_pages · `0023` cms_page_sections · `0024` promo_mechanics_nxm (N×M, 5.P-1) ·
  `0025` order_items_is_gift (пост-роадмап, выдача подарка gift_*, ADR-016) ·
  `0026` order_items_dims_snapshot (вес/габариты в снимке позиции, FF.md-1) ·
  `0027` tbank_payments (Этап 7: `orders.payment_provider` + `tbank_payment_log` идемпотентность, ADR-017) ·
  `0028` storefront_pageviews (пост-роадмап: суточный счётчик посещений витрины для графика дашборда; beacon `POST …/events/pageview`).
  *(Этап 6 новой схемы БД не добавлял; `0025`–`0028` — пост-роадмап.)*
- `db/seed/*` — сидовые данные.

### `scripts/` — DevOps-скрипты (Этап 0 + Этап 6)

| Скрипт | Назначение |
|---|---|
| `init-shop.sh` | развёртывание магазина: создание БД, идемпотентные миграции, seed, владелец (Этап 0) |
| `smoke.sh` | smoke-проверка `/api/health` и ключевых эндпоинтов после деплоя (6.1) |
| `deploy.sh` | сценарий развёртывания (`docker compose up` + init + smoke), цели в `Makefile` (6.1) |
| `backup.sh` | резервная копия PostgreSQL (логический дамп) + медиа; сервис `backup` в compose (6.2) |
| `restore.sh` | восстановление из бэкапа (6.2) |
| `restore-drill.sh` | «учебное» восстановление — регулярная проверка, что бэкап рабочий (6.2) |
| `update.sh` | обновление кода: применяет за **health-gate** `?deep=1`, при провале — автоматический откат (6.4) |
| `check-migrations.sh` | линтер аддитивности миграций (запрет деструктивных `DROP`/`ALTER`; statement-level, fix `484e41e`) (6.4) |
| `healthcheck-monitor.sh` | мониторинг доступности — алерт на падение `/api/health` через `ALERT_WEBHOOK_URL` (6.3) |

---

## 4. Индекс документации (`docs/`)

| Файл | Содержание |
|---|---|
| `00-журнал-проекта.md` | Журнал/история проекта; восстановление контекста без анализа скрытых сессий (ведёт History Manager, новые записи **сверху**) |
| `01-архитектурные-решения.md` | Реестр **ADR-001…016** (см. ниже); каждое решение с обоснованием и последствиями |
| `02-модель-развёртывания.md` | Модель развёртывания и переноса на новый магазин по принципу «копировать-вставить» (БД разворачивают другие люди) |
| `03-роадмап.md` | Единый источник правды по статусу: пройденные/текущие/будущие этапы и задачи |
| `04-этап1-ядро-auth-rbac.md` | Проектный документ Этапа 1: ядро, Auth, RBAC, каркас админки, audit_log |
| `05-этап2-каталог.md` | Проектный документ Этапа 2: модель каталога товаров |
| `06-референсные-магазины.md` | Этап 2.x: референсные магазины (THE CASE, Gang Auto) + расширение каталога + gap-анализ |
| `07-этап3-заказы.md` | Полная спецификация Этапа 3: заказы, корзина/чекаут, модуль orders |
| `08-этап4-cdek.md` | Проектный документ Этапа 4: интеграция СДЭК, порт логики из carre (PHP/Yii2) на TS/Next.js |
| `09-инструкция-разворачивания.md` | **Создан (Этап 6.5).** Пошаговая инструкция развёртывания для не-разработчика: clone → `.env` → up → init → проверка (анонс в `docs/02`, п. 7) |
| **`10-состояние-и-продолжение.md`** | **Этот файл** — гайд продолжения для новой сессии после `/compact` |
| `11-этап5-cms-промо-seo-настройки.md` | Проектный документ Этапа 5: CMS, промо N×M, SEO, настройки магазина (+ ADR-012/013/014) |
| `12-этап6-devops.md` | Проектный документ Этапа 6: DevOps (деплой/бэкапы/мониторинг/обновления); содержит точки расширения после роадмапа (offsite-бэкапы, внешний APM/Prometheus) |
| `13-сращивание-the-case.md` | Контракт сращивания витрины THE CASE с Admik (чистый потребитель Storefront API; волны A–D) |
| `14-наполнение-каталога-витрины.md` | Инструкция владельцу: наполнение каталога витрины из админки (без демо-сида) |
| `15-платежи-tbank.md` | Проектный документ Этапа 7: онлайн-эквайринг Т-Банк (ADR-017); волны 1–2 реализованы (см. `docs/00`, 2026-06-17) |
| `17-волна3-находки-на-разбор.md` | **✅ ЗАВЕРШЕНО (2026-06-18):** волна 3 поиска багов разобрана — все 17 находок верифицированы (подтверждено и исправлено 3: #5/#6/#16; опровергнуто 14). Финальные вердикты и причины опроверждений — внутри |

Реестр ADR (в `docs/01`):

```
ADR-001 — Технологический стек
ADR-002 — Источники паттернов (что берём из carre)
ADR-003 — Модель мультитенантности
ADR-004 — «Сначала тесты, потом код»
ADR-005 — Модель прав доступа (RBAC): гибкие права, не enum-роли
ADR-006 — Аутентификация, хеширование паролей и роли БД
ADR-007 — Модель каталога: характеристики (гибридная EAV) и связь товар↔категория (M2M)
ADR-008 — Admik как headless commerce backend + публичный Storefront API
ADR-009 — Модель ценообразования, флагов товара и брендов каталога
ADR-010 — Заказы: серверный расчёт корзины (anti-tamper) и снимки позиций
ADR-011 — СДЭК: слоистый порт carre + mock-режим + идемпотентный webhook
ADR-012/013/014 — Этап 5 (CMS / SEO+настройки / промо N×M) — см. docs/01 и docs/11
ADR-015 — Стратегия бэкапов/обновлений/мониторинга для single-VPS (Этап 6): логический дамп + restore-drill; аддитивные миграции + health-gate + откат; self-hosted JSON-логи
ADR-016 — Выдача товара-подарка (gift_*) отдельной позицией заказа (пост-роадмап): order_items.is_gift, цена 0, вне итога, best-effort резерв
ADR-017 — Платежи Т-Банк (Этап 7): слоистый порт паттерна СДЭК + mock-режим + Token-подписанный идемпотентный webhook; payment_provider на orders; статусы покрыты существующим PaymentStatus (см. docs/15)
```

---

## 5. Ключевые архитектурные принципы

- **1 магазин = 1 БД.** Мультитенантность реализуется конфигом (отдельный экземпляр на магазин),
  а не разделением данных внутри одной БД (ADR-003).
- **Флаги модулей.** Что включено для конкретного магазина — задаётся в `lib/config/modules`.
  Фичи проверяют флаг, а не хардкодят наличие.
- **`defineAction` (`lib/server/action.ts`).** Единый конвейер серверного действия:
  **guard (проверка прав) → Zod-валидация → работа с БД → revalidate → запись в audit**.
  Все мутации идут через него.
- **RBAC по правам, не по enum-ролям.** Доступ проверяется по гранулярным правам (ADR-005),
  роли — это наборы прав.
- **Headless backend + Storefront API.** Витрины (THE CASE, Gang Auto) — отдельные потребители
  публичного API `/api/storefront/v1` (ADR-008).
- **Anti-tamper: цены считает сервер.** Клиент не присылает итоговые суммы; сервер пересчитывает
  корзину и сохраняет снимки позиций (ADR-010).
- **Деньги в копейках.** Никаких float; целые числа (модуль `lib/orders/money`).
- **Mock-режим всех внешних интеграций.** СДЭК и платёжки умеют работать в mock-режиме
  (детект по отсутствию реальных ключей), чтобы dev/CI не зависели от внешних сервисов (ADR-011).
- **Идемпотентные миграции.** Каждая SQL-миграция безопасна к повторному запуску.
- **Идемпотентность СДЭК-webhook.** Повторная доставка одного события не ломает состояние (ADR-011).

---

## 6. КРИТИЧНО про секреты

**Реальных ключей СДЭК и платёжных систем в репозитории НЕТ и быть НЕ должно.**
Эти ключи принадлежат третьим лицам (владельцам магазинов). В репо допустимы только:

- плейсхолдеры в `.env.example`;
- mock-режим интеграций (работает без реальных ключей).

Никогда не коммить реальные креды, токены, секреты СДЭК/платёжек. Если они где-то всплыли —
это инцидент: убрать из истории и ротировать на стороне владельца.

---

## 7. Что делать дальше

**Роадмап (Этапы 0–6) завершён полностью.** Этап 6 (DevOps) — последний этап — закрыт по
`docs/12-этап6-devops.md` (ADR-015), 6 волн W1–W5 + fix:
**6.1** copy-paste-деплой (`scripts/{smoke,deploy}.sh` + `Makefile` + CI `scripts-lint`),
**6.2** бэкапы с restore-drill (`scripts/{backup,restore,restore-drill}.sh` + сервис `backup`),
**6.3** мониторинг/JSON-логи (`lib/logger.ts`, `lib/health.ts` + deep-режим `/api/health?deep=1`,
`scripts/healthcheck-monitor.sh`, ротация логов),
**6.4** обновления через аддитивные миграции + health-gate + откат (`scripts/{check-migrations,update}.sh`),
**6.5** инструкция `docs/09-инструкция-разворачивания.md` + ADR-015.
typecheck/lint/shellcheck зелёные, **vitest 1013 passed / 76 skipped**. Адверсариальная QA/Security —
вердикт **«релиз можно»**; единственная major (обход линтера аддитивности) исправлена коммитом `484e41e`.

**БОЕВОЙ СТЕНД РАЗВЁРНУТ (✅ 2026-06-16) — первый продакшн.** Admik + витрина THE CASE подняты на одном VPS
Timeweb (Ubuntu 24.04, `130.49.129.248`) через `docker compose` (swap +2 ГБ), под доменом `erfgq.website`:
витрина `https://erfgq.website` (+`www`→редирект), админка/API/медиа `https://admin.erfgq.website` — реальные
TLS Let's Encrypt от Caddy, deep-health `ok` (db/redis/s3). `init-shop` накатил 26 миграций (вкл. `0026`),
права/роли, владелец `akekbol@gmail.com`; бакет MinIO `admik-media` создан. СДЭК — в mock. Код на сервере:
`/opt/admik` + `/opt/admik/THE CASE`; `.env` с секретами — только на сервере, доступ по SSH-ключу. Подробности
деплоя и серии фиксов — `docs/00` (Завершённые задачи, 2026-06-16) и `docs/09`.

**Активные нити (что делать дальше на боевом стенде):**
1. **Касса Т-Банк** — ✅ **волны 1–2 реализованы** (`lib/payments/tbank/*`, роуты, миграция `0027`, ADR-017): mock-first +
   боевой клиент + БД-аудит, зелёный typecheck/lint/тесты (+98 тестов). **Осталось:** живой прогон по тест-картам на
   ТЕСТОВОМ терминале (`rest-api-test.tinkoff.ru`, ключи в `.env` стенда) → боевой запуск (волна 4). Детали — `docs/15` §8.
2. **Ключи СДЭК** — подставить в `.env` сервера (тестовый контур edu или боевые ключи владельца) для реальных
   расчётов и автонакладных; пока СДЭК в mock (см. п. 0/1 ниже).
3. **Наполнение каталога** — владелец заводит товары/категории/варианты из админки (`docs/14`).
4. **Безопасность** — сменить `OWNER_PASSWORD` и root-пароль сервера; **ротировать засветившиеся боевые ключи
   СДЭК** (засветились в канале связи — см. §6).

**Пост-роадмап:** весь ДЕЙСТВЕННЫЙ бэклог закрыт (см. сводку коммитов в §1 и `docs/00`). typecheck/lint/
shellcheck зелёные, vitest зелёный (база 1026 passed / 80 skipped; БД-кейсы — на CI/стенде). Дальнейшие
направления:

### 0a. ⭐ АКТИВНАЯ НИТЬ — интеграция референсных витрин (THE CASE сделана)

Новая активная тема — **сращивание референсных витрин с платформой Admik**.

- **THE CASE — ✅ done (2026-06-16).** Витрина (Next.js 15) переведена в режим **чистого потребителя**
  Storefront API (`/api/storefront/v1`), её собственный коммерческий бэкенд выведен из обращения (волны
  A–D). На стороне Admik аддитивно добавлены поиск городов СДЭК (`lib/cdek/services/city.ts` `CityService`
  + роут `app/api/storefront/v1/delivery/cdek/cities/route.ts`) и параметр `GET /products?category=<slug>`
  (резолвер `getActiveCategoryIdBySlug` в `lib/storefront/queries.ts`). Здоровье: THE CASE — `tsc`/`lint`
  чисто, `vitest` 60 passed; Admik (затронутое) — storefront 92 + cdek 223 passed. Полный контракт и
  статус — **`docs/13-сращивание-the-case.md`**.
- **Следующий шаг:** поднять **стенд** — Admik + витрина THE CASE с `NEXT_PUBLIC_ADMIK_API_URL` (и
  `ADMIK_API_URL`/`STOREFRONT_API_KEY` при необходимости), домен витрины в `STOREFRONT_ALLOWED_ORIGINS`/
  ключ в `STOREFRONT_API_KEYS` на Admik; **наполнить каталог из админки** (без демо-сида — пара позиций
  владельцем); прогнать **e2e-проверку** (каталог/карточка → корзина(`variantId`) → `cart/quote` →
  СДЭК-расчёт/ПВЗ → `orders` → трекинг). (Локально в dev это НЕ гоняется: нет docker/БД/сети — норма.)
- **Решить версионирование THE CASE.** Код волн A–D **пока НЕ закоммичен**; способ хранения витрины
  (отдельный репозиторий vs `.gitignore` vs подкаталог монорепо Admik) пользователь ещё не выбрал — нужен
  его выбор; до него код витрины и сопутствующие артефакты Admik вне git-истории.
- **Затем — Gang Auto.** Вторая референсная витрина (автозапчасти, 2000+ SKU) сращивается по той же схеме.

### 0. ⭐ АКТИВНАЯ НИТЬ — СДЭК на тестовом контуре (продолжить ОТСЮДА)

Пользователь (2026-06-16): **«пока на тестовых ключах СДЭК API»** — работаем НЕ на боевых ключах владельца,
а на **тестовом контуре** СДЭК (sandbox «edu», `https://api.edu.cdek.ru`). Память: `admik-cdek-test-contour`.

- **Что уже сделано (коммит `2280420`):** код тестовый контур ПОЛНОСТЬЮ поддерживает (менять не нужно) —
  `isCdekMock()`=false при заданных `CDEK_ACCOUNT`/`CDEK_SECRET` → реальный клиент; OAuth/запросы идут на
  `CDEK_BASE_URL`; `CDEK_TEST_MODE=true` разрешает пустой webhook IP-whitelist. В `.env.example` секция СДЭК
  переписана под 3 режима (mock / тестовый контур edu / боевой) с copy-paste-пресетом; добавлен регресс-тест
  `tests/cdek/config.test.ts` (edu-URL+ключи+testMode → не mock).
- **Включить (в `.env` стенда, НЕ в репозиторий):**
  ```dotenv
  CDEK_BASE_URL=https://api.edu.cdek.ru
  CDEK_TEST_MODE=true
  CDEK_ACCOUNT=<тестовый client_id>     # публичные тестовые ключи — из офиц. документации СДЭК
  CDEK_SECRET=<тестовый client_secret>  # (раздел «тестовый аккаунт»); это НЕ ключи владельца
  ```
- **Следующий шаг (где продолжить):** получить от пользователя тестовые `client_id`/`client_secret` (или
  подтверждение взять публичные сэндбокс-ключи СДЭК) → вписать в `.env` стенда → прогнать **живой
  СДЭК-сценарий** на развёрнутом стенде: расчёт доставки → список ПВЗ → создание тест-отправления →
  приём webhook. (Локально в dev это НЕ гоняется: нет docker/БД/сети к СДЭК — норма.) Опционально подготовить
  пошаговый чек-лист проверки тестового контура.

### 1. Подключение реального магазина (первый продакшн) — ✅ СТЕНД РАЗВЁРНУТ

**Стенд развёрнут (2026-06-16): `erfgq.website` / `admin.erfgq.website`, доступен по https** (см. вводку §7
и снимок §1). Прогон деплоя выполнен по `docs/09`. **Остаётся:** боевые ключи интеграций (СДЭК/платёжки —
подставляют владельцы в `.env` сервера, в репо их нет), реальные данные/настройки магазина через UI,
наполнение каталога. Боевой СДЭК: `CDEK_BASE_URL=https://api.cdek.ru` + ключи владельца. Касса Т-Банк — по
`docs/15` (ключи завтра).

### 2. Остаток бэклога — намеренно НЕ реализуется автономно (нужен сигнал/решение)

- **M1 (множественные промокоды на заказ)** — отложен **решением ADR-014** (MVP = один промокод на заказ);
  `combineDiscountsMinor` готов как задел. Реализация развернула бы продуктовое решение → нужен явный запрос.
- **(c)** deep-health отдаёт 200 при деградации Redis/S3 — **by design** (критична только БД).
- **offsite-бэкапы** и **внешний APM/Prometheus** — точки расширения, намеренно вне базовой поставки
  (ADR-015: self-hosted, «copy-paste без аккаунтов»).

Перед любой работой прогнать §2 (health-check), чтобы стартовать с зелёного состояния. Изменения —
коммитами на ветке `foundation/etap-0-1` + синхронизация документации (`docs/00`, `docs/03`, этот файл).
Пуш в `origin` запускает CI (job `docker-smoke` валидирует деплой; `gh` локально не залогинен — статус
прогона смотреть на GitHub).

---

## 8. Как ведётся работа

- **Оркестрация через агентов Claude со встроенной ролью.** Роли (Solution Architect,
  Documentation Manager, History Manager, инженеры) задаются в промпте/инструкциях агента.
  Кастомные типы агентов из `.claude/agents` в текущем харнессе **не зарегистрированы** —
  полагаться на них нельзя; роль передаётся явно при запуске.
- **Работа волнами.** Этап дробится на пакеты (волны); каждая волна — самодостаточный кусок
  с тестами.
- **Коммит после каждой волны** на ветке `foundation/etap-0-1`. Сообщения коммитов — на русском,
  в формате `feat(этапN): …` / `docs(этапN): …`, как в истории выше.
