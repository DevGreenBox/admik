# Аудит: что НЕЛЬЗЯ отредактировать из админки

**Дата:** 2026-06-23 · **Заказчик:** Саша · **Объём:** платформа Admik (backend + админка) + витрина THE CASE (потребитель Storefront API)

> Источник: read-only аудит (24 агента, адверсариальная проверка каждого пробела по коду). Запрос владельца: «выяснить, что именно нельзя отредактировать из админки» — блок «О бренде» был лишь одним примером из многих. Контекст прерванной сессии — память `crashed-session-2026-06-23-admin-gaps`.

---

## 1. TL;DR

**Подтверждено 18 пробелов** (G-01…G-16, G-18, G-19). Один заявленный пробел **опровергнут** (G-17 — на самом деле МОЖНО).

**Корневая причина №1 (G-01):** витрина THE CASE **вообще не запрашивает** `GET /api/storefront/v1/settings`. В её API-клиенте (`THE CASE/src/lib/admik/client.ts`) есть `listProducts/getProduct/getCategories/quoteCart/createOrder/initPayment/getOrder/cdek*` — но **нет `getSettings`** (`grep -i settings` по `THE CASE/src` = 0). Поэтому даже то, что админка УЖЕ умеет редактировать и API УЖЕ отдаёт (брендинг, контакты, реквизиты, SEO), физически не доходит до сайта.

| Область | Пробелов | Тип проблемы |
|---|---|---|
| Архитектура (корень) | 1 (G-01) | end-to-end разрыв: витрина не читает /settings |
| Главная (home-контент, hero, philosophy, изображения, вкладки) | 6 (G-02…G-05, G-18, G-19) | хардкод в коде витрины |
| Брендинг (лого/название) | 1 (G-06) | админка готова, витрина игнорирует |
| Контакты / соцсети / форма заявок | 3 (G-07, G-08, G-09) | хардкод + нет приёма заявок |
| Шапка / Футер / Newsletter | 3 (G-10, G-11, G-12) | хардкод навигации/футера, нет ключа настроек |
| CMS / юр.страницы / отзывы | 2 (G-13, G-14) | CMS-подсистема не подключена к витрине |
| Личный кабинет (статусы) | 1 (G-15) | хардкод словаря |
| SEO (метаданные) | 1 (G-16) | админка готова, витрина игнорирует |

**WIP ADR-018 (незакоммичено):** полностью не закрывает ни одного; **5 частично** (G-01, G-02, G-03, G-05, G-06). Добавил схему `home`, `updateHomeAction`, DTO-поле `home`, дефолты `lib/config/home-defaults.ts`, server-side загрузку изображений — но **без UI-формы** и **без чтения на витрине**. Для владельца пробелы актуальны на 100%.

**Нюанс приоритезации:** ~5 пробелов (G-01, G-06, G-07, G-08, G-16) — это НЕ дыры в админке, а разрыв интеграции на стороне витрины. Бэкенд и формы готовы — нужно научить витрину читать API. Это дешевле, чем строить новые схемы/формы.

---

## 2. Что НЕЛЬЗЯ отредактировать из админки

### Архитектура (корень всего)

| ID | Что | Evidence (витрина) | Почему нельзя | Sev | WIP |
|---|---|---|---|---|---|
| **G-01** | Витрина не делает HTTP-запрос за настройками — брендинг/контакты/реквизиты/SEO/home не доходят до сайта | `THE CASE/src/lib/admik/client.ts` — нет `getSettings`; `layout.tsx:50-56`, `page.tsx:25-33` тянут только `getCategories()` | API (`app/api/storefront/v1/settings/route.ts`) уже отдаёт `branding/contacts/legalEntity/seo/home`, но потребитель их не читает | high | частично |

### Главная

| ID | Что | Evidence | Почему нельзя | Sev | WIP |
|---|---|---|---|---|---|
| **G-02** | Блоки «О бренде», «Качество ткани», «Доставка и оплата», «Форма/Функция/Дисциплина» — весь текст | `Sections.tsx`: FABRIC_FEATURES(46-51), About(72-74,536-544), ValuesStrip(141-153), values(555), Delivery(568-572); `page.tsx:37-45` без контент-пропсов | Литералы в компоненте. В админке нет секции «Главная», нет `HomeContentForm`. WIP `home` есть, но без UI и витрина её не читает | high | частично |
| **G-05** | Все изображения главной/футера (30+ путей) | `THE CASE/src/lib/images.ts:2-57` (IMAGES, локальные `/images/*.webp`); `Sections.tsx:27/227/238/249/446-449`, `Footer.tsx:49` | Витрина рендерит статику из `public/images`, не резолвит S3-ключи. Виджета загрузки файла в `app/admin` нет (`grep type="file"`=0). WIP-uploadActions только server-side, ни к одной форме не подключены | high | частично |
| **G-03** | CTA «Смотреть коллекцию», `/catalog`, «Создано вместе с врачами», «Эскиз» | `Sections.tsx:38`(CTA), :21-23(href), :70, :80 | Зашито в HomeBanner/CoverSlides. WIP `home.hero` частично, но витрина не читает. «Создано вместе с врачами»/«Эскиз» — без ключа настроек | medium | частично |
| **G-18** | Подписи вкладок «Для женщин/мужчин», темы women/men, ракурсы Front/Side/Back | `Sections.tsx:305-308`(COLLECTION_TABS), :273(viewLabels) | Категории динамичны, но текст вкладок зашит. Нет ключа настроек. Нарушает мультитенантность | low | нет |
| **G-19** | 12 подписей деталей товара (Воротник/Карман/Молния…) | `lib/images.ts:39-43`(detailLabels), `Sections.tsx:449` | Литералы в коде, нет ключа/поля/UI. WIP `home` их не покрывает | low | нет |
| **G-04** | Блок «Comforts + Medicine = THE CASE» (Philosophy) | `Sections.tsx:395-415`(EditorialStatement), `page.tsx:42` | Нет ключа/поля/формы. *Поправка:* 3 из 4 заявленных блоков — мёртвый код (`page.tsx:18-19`), не рендерятся — в аудит не входят | low | нет |

### Брендинг

| ID | Что | Evidence | Почему нельзя | Sev | WIP |
|---|---|---|---|---|---|
| **G-06** | Текст логотипа «THE CASE» и подзаголовок «Medical Uniform» | `THE CASE/src/components/ui/Logo.tsx:29/34` | *Поправка:* в админке МОЖНО — `BrandingForm.tsx` даёт `shopName/logoUrl/faviconUrl`→`updateBrandingAction`; API отдаёт `branding.*`. Дефект целиком на витрине (тот же корень, что G-01) | medium | частично |

### Контакты / Соцсети / Заявки

| ID | Что | Evidence | Почему нельзя | Sev | WIP |
|---|---|---|---|---|---|
| **G-07** | Контакты на `/contacts` (Telegram `@thecase`, телефон `+70000000000`, email `hello@thecase.ru`) | `contacts/page.tsx:9-13`(CONTACT_* TODO), :84-87, :49/57/65 | Админка готова (`LegalContactsForm`), API отдаёт `contacts.*`. Но страница использует свои константы. Реальный телефон владельца не попадёт | high | нет |
| **G-08** | Соцссылки в футере (Instagram `href='#'`, Telegram) | `Footer.tsx:107-116`, :27(SUPPORT_TELEGRAM TODO) | **Двойной разрыв:** поле `contacts.socials[]` есть в схеме/DTO/бэкенде, но в `LegalContactsForm` НЕТ UI-редактора соцсетей, И витрина читает локальные константы | medium | нет |
| **G-09** | Форма обратной связи на `/contacts` — заглушка, заявки теряются | `contacts/page.tsx:19-26`(handleSubmit=`setSent(true)`, TODO) | Нет эндпоинта приёма заявок, нет модели Lead, нет раздела заявок в админке, нет настройки «куда слать» | medium | нет |

### Шапка / Футер / Newsletter

| ID | Что | Evidence | Почему нельзя | Sev | WIP |
|---|---|---|---|---|---|
| **G-10** | Пункты меню шапки, href, aria-labels, «Личный кабинет» | `Header.tsx:14-17`(NAV_RIGHT), :28-34(NAV_LEFT), :52/96/99/102/108, :166-168 | В `SETTING_KEYS` нет ключа navigation/menu. Только дети «Коллекции» динамичны. Переименовать/добавить пункты из админки нельзя | medium | нет |
| **G-11** | Колонки футера (Service 6, Legal 2), «Поддержка», Newsletter-тексты, wordmark, © | `Footer.tsx:11-24`(FOOTER_LINKS), :84-92(SUPPORT_PHONE), :120-123/:145/:149-151 | Нет ключа настроек для футера. Только Shop частично из категорий. «Поддержка» дублирует хардкод, не читает `settings.contacts` | medium | нет |
| **G-12** | Форма подписки в футере — заглушка | `Footer.tsx:120-138`(onSubmit=`setEmail('')`) | Нет эндпоинта подписки, нет таблицы подписчиков, нет настройки провайдера рассылки | low | нет |

### CMS / Юр.страницы / Отзывы

| ID | Что | Evidence | Почему нельзя | Sev | WIP |
|---|---|---|---|---|---|
| **G-13** | `/terms`, `/privacy`, `/returns`, `/care`, `/payment` — весь юр./инфо-текст | `terms/page.tsx:63,81/54-60`; `care/page.tsx:18`(состав ткани); `payment/page.tsx:21-37` | Админка ПОЛНОСТЬЮ умеет CMS (PageForm, 7 типов секций, `/api/storefront/v1/pages`), но витрина рендерит статические React-страницы и не вызывает CMS API. Правка юр.контента требует деплоя кода | high | нет |
| **G-14** | `/reviews` (заголовок, описание, 6 плейсхолдеров фото) | `reviews/page.tsx:11,20-25,29-37`(REVIEWS_PLACEHOLDER TODO) | Сущности «отзывы» нет нигде (нет ключа, нет API, нет CMS-секции testimonial). Статический файл | medium | нет |

### Личный кабинет

| ID | Что | Evidence | Почему нельзя | Sev | WIP |
|---|---|---|---|---|---|
| **G-15** | Словарь 21 статуса (заказ/оплата/доставка) + тексты ЛК | `account/page.tsx:13-34`(STATUS_LABELS), :281-284/:317/:330/:336 | DTO отдаёт сырой `status`, локализованной подписи в API нет, ключа настроек нет. Админ-словарь захардкожен и расходится с витриной (shipped «Отгружен» vs «Отправлен») | low | нет |

### SEO

| ID | Что | Evidence | Почему нельзя | Sev | WIP |
|---|---|---|---|---|---|
| **G-16** | Title/description/keywords/OpenGraph/Twitter/og-картинка | `layout.tsx:13-39`(литералы, og `'/images/home/banner-main.webp'`); `siteUrl/noindex` только из env (`site-url.ts`) | Админка готова (`SeoSettingsForm` + `settings/seo`), API отдаёт `seo.*`. Но `layout.generateMetadata` не читает settings; `siteUrl/noindex` — только env | high | нет |

---

## 3. WIP ADR-018 (незакоммичено) — что добавила фича и чего ей не хватает

**git status:** M `lib/settings/schemas.ts`, `lib/settings/actions.ts`, `lib/storefront/settings-dto.ts`, `lib/cms/actions.ts`, `lib/cms/schemas.ts`, `lib/cms/section-form.ts`, `lib/config/settings.ts`, `lib/settings/action-factory.ts`, `app/admin/(panel)/settings/_components/form-actions.ts`; ?? `lib/config/home-defaults.ts` + 4 теста.

**Добавлено:** `homeSchema`+ключ `home`; `updateHomeAction`/`updateHomeContentAction`; DTO-поле `home`+дефолты; server-side `uploadSettingsImageAction`(logo/favicon/og) и `uploadCmsImageAction`; `kind:'image'` в section-form.

**Частично трогает:** G-01, G-02, G-03, G-05, G-06.

**Чего критически не хватает (пробелы НЕ закрыты):**
1. Нет UI-формы «Главная» — `settings/page.tsx` рендерит 6 секций, `HomeContentForm.tsx` не существует, ни одна форма не импортирует новые actions.
2. Нет виджета загрузки файла (`grep type="file"` по `app/admin` = 0) — uploadActions висят в воздухе.
3. Витрина не читает `settings.home`.
4. Всё незакоммичено.
5. Охват только `hero/about/quality/delivery` — philosophy/details/вкладки/футер/навигация вне схемы.

---

## 4. Опровергнуто (на самом деле МОЖНО — не чинить)

| ID | Заявлено | Реальность |
|---|---|---|
| **G-17** | «`seo.robots_extra` и `seo.noindex_site` нет в UI» | **НЕВЕРНО.** `SeoSettingsForm.tsx` содержит ОБА: `robots_extra` (textarea, стр.116-117), `noindex_site` (checkbox, стр.120-124), оба → `updateShopSeoAction`. Из админки редактируются. (Витрина их не потребляет — это уже G-01/G-16.) |

---

## 5. Приоритезация (для универсальной мультитенантной платформы)

**P0 — корневой фикс, малой кровью разблокирует остальное:**
1. **G-01:** добавить `getSettings()` в `client.ts` витрины + потребление в `layout/page/Header/Footer/Logo`. Автоматически закрывает интеграционную часть **G-06, G-07, G-16** (бэкенд и формы готовы). Прямо реализует «новый магазин = конфигурирование, а не код».

**P1 — контент и юр.обязательность:**
2. **G-13:** подключить витрину к CMS (generic-роут `app/[slug]/page.tsx` + `/api/storefront/v1/pages/[slug]`). Движок CMS в админке готов.
3. **G-02 + G-05:** докоммитить WIP ADR-018, добавить UI-форму «Главная» + виджет загрузки изображений, научить витрину читать `settings.home`; расширить на философию (G-03/G-04).

**P2 — структурная конфигурируемость:**
4. **G-10 + G-11:** новый ключ настроек `navigation`/`footer` + UI-редактор. Главный барьер переноса на другой магазин.
5. **G-08:** достроить UI-редактор `contacts.socials[]` (схема/бэкенд есть) + чтение на витрине.

**P3 — функциональные дыры:**
6. **G-09:** эндпоинт приёма заявок + модель Lead + раздел в админке + канал.
7. **G-12:** эндпоинт подписки + хранилище/провайдер.
8. **G-14:** сущность «отзывы» (CMS-секция testimonial или модель) + наполнение.

**P4 — косметика / i18n:**
9. **G-15:** локализованные подписи статусов в DTO/настройках; устранить расхождение admin↔витрина.
10. **G-18, G-19:** вынести в расширенную схему `home`.

**Ключевой вывод:** ~5 из 18 пробелов — это не недоработка админки, а то, что витрина THE CASE не была написана как настоящий потребитель Storefront API. Один архитектурный фикс (P0) даёт непропорционально большой результат. Остальное — достройка платформы до «новый магазин запускается конфигурированием».
